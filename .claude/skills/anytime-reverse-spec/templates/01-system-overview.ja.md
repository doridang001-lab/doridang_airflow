---
title: "システム概要"
date: "{{today}}"
updated: "{{today}}"
type: "spec"
lang: "ja"
author: "Claude Code v{{cliVersion}}"
category: "basic-design/overview"
c4Scope:
    - "{{systemId}}"
excerpt: "システム {{repoName}} を構成する {{N}} コンテナ（パッケージ）の役割と主要な依存関係。"
---


# システム概要


## 1. 目的とスコープ


### 1.1 目的

<!--
ガイダンス: このシステムが解決する課題・提供価値を 2〜4 段落で記述する。
- README.md / package.json description / 主要ドキュメントから導出
- 「N コンテナのモノレポ構成」「コアライブラリ / ビューア / ホスト の N 段構造」等の構造的特徴を含める
NG: 単に repo 名を繰り返すだけ
-->

`{{repoName}}` は、{{ドメイン / 主要機能カテゴリ}}を扱う {{N}} コンテナ（パッケージ）から構成される {{構成種別: モノレポ / マイクロサービス / 単一パッケージ}}型システムである。

{{システムの構造的特徴を 1〜2 段落: コアライブラリ層 / ビューア層 / ホスト層の積み上げ等}}


### 1.2 想定読者

| 役割 | 読む目的 |
| --- | --- |
| {{役割1}} | {{目的}} |
| {{役割2}} | {{目的}} |


## 2. Container 一覧

<!--
ガイダンス: C4 モデルの Container 要素を「役割グループ」で分類して表に展開する。
役割グループ例: コアライブラリ / ビューア / VS Code 拡張 / MCP サーバ / アプリ / エージェント / その他
責務は以下の優先順で導出:
1. C4 要素 description（存在すれば）
2. packages/<name>/README.md 冒頭
3. packages/<name>/{CLAUDE,AGENTS,GEMINI}.md 冒頭
4. packages/<name>/package.json description（または pyproject.toml / Cargo.toml / go.mod）
5. パッケージ名規則によるフォールバック（*-core / *-viewer / *-extension / mcp-* 等）
-->

| 役割グループ | コンテナ | 責務 |
| --- | --- | --- |
| {{グループ名}} | `{{pkg-name}}` | {{1 文責務}} |


## 3. Container 構成図

<!--
ガイダンス: pkg→pkg 依存（imports）を集約した flowchart を描く。
- ノード数 15 超なら subgraph で役割グループに分割
- エッジは出現回数 N 以上（3 など）のみ表示、ラベルは集約多重度
- 主要依存は実線、補助依存は点線で区別
- 描画幅 900px 以内（縦長 TB を優先）
-->

> [!NOTE]
> エッジは pkg→pkg 依存（`imports`）を集約し、出現回数 {{threshold}} 以上のもののみ表示している。\
> 数字は集約後のエッジ多重度。

```mermaid
flowchart TB
    {{subgraph 定義（役割グループ）}}
    {{ノード定義}}
    {{エッジ定義}}
```


## 4. 主要依存関係（上位 {{N}} 件）

| # | from | to | 多重度 |
| --- | --- | --- | ---: |
| 1 | `{{from}}` | `{{to}}` | {{N}} |


## 5. 観測される構造的特徴

<!--
ガイダンス: C4 モデルから機械的に観測された事実のみを箇条書きで列挙する。
- N 段構造（コア → ビューア → ホスト 等）
- 依存集中点（最も参照されるパッケージ）
- 役割グループ内の責務分担
- 共通基盤（vscode-common / shared utils 等のハブ）
NG: 設計の良し悪しを評価する表現（"優れた"等）/ コードから読み取れない意図の推測
-->

> [!NOTE]
> 以下は C4 モデルから機械的に観測された事実のみ。詳細な責務分担や設計意図は章 2 以降で個別機能ごとに展開する。

- **{{特徴 1}}**: {{根拠となる数値・依存関係}}
- **{{特徴 2}}**: {{根拠}}


## 6. 技術スタック概観

<!--
ガイダンス: package.json / pyproject.toml / Cargo.toml / go.mod から検出した主要ランタイム・フレームワーク・ストレージを表形式で。
バージョンは exact 固定値を記載。
NG: 検出できない技術を推測で書く（書かない方が良い）
-->

| レイヤー | 技術 | バージョン |
| --- | --- | --- |
| {{レイヤー}} | {{技術名}} | {{version}} |
