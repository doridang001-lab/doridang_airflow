---
title: "データモデル"
date: "{{today}}"
updated: "{{today}}"
type: "spec"
lang: "ja"
author: "Claude Code v{{cliVersion}}"
category: "basic-design/data"
c4Scope:
    - "{{systemId}}"
    - "{{primary data container 1}}"
    - "{{primary data container 2 以降}}"
excerpt: "{{検出された DB 種別}}の主要テーブル {{N}} 件・関係・インデックス・整合性ルール。"
---


# データモデル

<!--
ガイダンス:
- 検出された DB 種別（dbKind）= postgres / mysql / sqlite / prisma / drizzle / typeorm / sequelize / sqlalchemy / activerecord
  ごとに「ER 図 → テーブル別詳細 → マッピング → インデックス → 整合性 → 既知の差分」の順で並べる
- 1 dbKind しかない場合は dbKind ラベルセクションを省略してフラットに記述してよい
- 2 階層以上の永続化（ローカル ↔ ミラー）がある場合は冒頭の永続化階層表を残す

# 章番号の安定化ルール（重要）
本テンプレでは 1〜8 番のセクションを固定で出力する:
1 永続化階層
2 ER 図
3 テーブル別詳細
4 階層間マッピング
5 主要インデックス（横断視点）
6 データ整合性ルール
7 セキュリティ / アクセス制御
8 既知の差分・要レビュー事項

検出 0 件のセクションでも **見出しは削除せず、本文を「(該当なし)」と書く**こと。
理由: 章番号や見出し集合が変わると evaluate=true で過去 commit と Jaccard 比較した際に
heuristic completeness が大幅低下するため (golden の見出し 37 → candidate 46 で 0.33 の事例あり)。

# テーブル全件網羅ルール（必須）
§3 テーブル別詳細は **検出された全テーブルを 1 件も省略せずに掲載**する (サンプリング禁止)。
§3 サブセクションの順序は以下の優先順位で固定:
1. ER 図 (§2) のサブセクション順 (機能領域グループ単位)
2. グループ内は **テーブル名 ABC 順** (大文字小文字無視)
理由: テーブル選択が run ごとに変わると §3.x.{name} 見出し集合が崩れ design スコアが急落する。

# ER 図サブセクション名の標準化（強く推奨）
§2 ER 図は以下の機能領域グループ名から選んで使う:
- 「セッション・メッセージ系」
- 「リリース・カバレッジ系」
- 「コード解析・C4 モデル系」
- 「メモリ系」
- 「ミラー / 同期系」(2 階層構成の場合のみ)
- 「マスタ / 設定系」
新規グループ名を作る際は既存と被らない命名にし、一度確定したら以後の run で同じ名前を維持する。
-->

> [!IMPORTANT]
> 本章は §3 で検出された全テーブルを網羅する。一覧抜粋・サンプリングは禁止する。
> ER 図サブセクション名は run 間で安定させる (gold との見出し一致を保つため)。


## 1. 永続化階層

<!--
ガイダンス: 検出された永続化が 2 階層以上（例: ローカル SQLite ↔ Supabase ミラー）の場合のみ残す。
1 階層なら本セクション削除。
-->

| 階層 | 役割 | 物理 / スキーマ定義 |
| --- | --- | --- |
| {{階層1: 主データストア}} | {{役割}} | {{ファイルパス}} |
| {{階層2: ミラー / キャッシュ}} | {{役割}} | {{ファイルパス}} |

> [!IMPORTANT]
> {{階層間の同期方式の前提を 1〜2 文で記述。例: 「ミラーは洗い替え同期のみ。書き込みは主データストア経由」}}


## 2. ER 図

<!--
ガイダンス: テーブル数が多い場合は機能領域ごとにサブセクション分割する。
- 関係系: セッション・メッセージ等の業務トランザクション系
- 解析系: コードグラフ・C4・カバレッジ等の解析結果系
- マスタ系: 設定・ルックアップ系
mermaid erDiagram で記述、ラベル内 HTML タグは <br/> のみ可。
-->


### 2.1 {{機能領域1: 例 セッション・メッセージ系}}

```mermaid
erDiagram
    {{TABLE_A}} ||--o{ {{TABLE_B}} : has
    {{TABLE_A}} {
      TEXT id PK
      TEXT name
    }
```


### 2.2 {{機能領域2}}

```mermaid
erDiagram
    {{...}}
```


## 3. テーブル別詳細

<!--
ガイダンス: 検出された **全テーブルを 1 件も省略せず** 1 件ずつ
「説明 + カラム表 + FK + インデックス」の形式で展開する。
各テーブルはアンカー目的で `### 3.x {{table_name}}` の見出しを必ず付ける（章 3 機能設計書がアンカーリンクする）。

カラム表の列構成:
- カラム: 物理カラム名
- 型: 物理型（INTEGER / TEXT / REAL / BLOB / SERIAL / UUID 等）。STRICT TABLE なら 6 種に正規化
- NULL: YES / NO
- DEFAULT: リテラル DEFAULT 値（無ければ「-」）
- 制約: PK / FK / UNIQUE / CHECK / GLOB 等
- 説明: 1 文要約

# サブセクション順序（必須）
3.1, 3.2, ... のサブセクション番号と内容を **run 間で安定**させるため、以下の順で割り振る:
1. §2 ER 図 のサブセクション順 (機能領域グループ単位)
2. グループ内は **テーブル名 ABC 順** (大文字小文字無視、`_` も含める)
3. グループ非所属のテーブル (例: マスタ・設定) は末尾にまとめて並べる
4. 新規テーブル追加時は該当グループ末尾に追加し、後続の番号を繰り上げない (既存 3.x の意味を維持)

NG:
- テーブル名一覧で済ませる / カラム列を省略する
- サブセクション順序を毎回シャッフルする
- 検出済みテーブルを「重要でない」と判断して省略する
-->


### 3.1 {{table_name_1}}

{{1 文〜2 文の説明: 何を保存するか・1 行が何を表すか}}

#### カラム

| カラム | 型 | NULL | DEFAULT | 制約 | 説明 |
| --- | --- | :---: | --- | --- | --- |
| `{{col}}` | `{{TYPE}}` | NO | - | PK | {{1 文}} |

#### 外部キー

<!-- ガイダンス: 該当無しなら本サブセクション削除 -->

| カラム | 参照 | ON DELETE |
| --- | --- | --- |
| `{{col}}` | `{{ref_table}}({{ref_col}})` | {{CASCADE / SET NULL / NO ACTION}} |

#### インデックス

<!-- ガイダンス: 該当無しなら本サブセクション削除 -->

| 名前 | 列 | 用途 |
| --- | --- | --- |
| `{{idx_name}}` | `({{col1}}, {{col2}})` | {{用途}} |


### 3.2 {{table_name_2}}

{{同様の構造を繰り返す}}


## 4. 階層間マッピング

<!--
ガイダンス: §1 で 2 階層以上の永続化を宣言した場合のみ本章を含める。
ローカル ↔ ミラー / API ↔ DB のテーブル対応・同期方式・差分（片方にしかないカラム）を表で示す。
-->

| {{階層1テーブル}} | {{階層2テーブル}} | 同期方式 | 差分 |
| --- | --- | --- | --- |
| `{{local}}` | `{{mirror}}` | {{wash-away / diff sync / 単方向}} | {{ローカルのみ列 / ミラーのみ列 / 無し}} |


## 5. 主要インデックス（横断視点）

<!--
ガイダンス: 性能観点で「複数テーブルにまたがる検索パターン」を支えるインデックスを抽出する。
- 期間検索（timestamp / created_at 系）
- セッション集計（session_id 単位）
- レポート集計（tag / period 単位）
EXPLAIN QUERY PLAN で SEARCH USING INDEX に乗ることを前提とした重要インデックスのみ列挙。
個別テーブル固有のインデックスは §3 の各テーブル詳細に書き、本章では繰り返さない。
-->

| インデックス | 対象列 | 用途 |
| --- | --- | --- |
| `{{idx_name}}` | `({{cols}})` | {{用途}} |


## 6. データ整合性ルール

<!--
ガイダンス: FK / CHECK / GLOB / NOT NULL から導出可能な整合性ルールと、コード側で担保されている不変条件を列挙する。
- ON DELETE CASCADE が貼られているテーブル
- 時刻フォーマット規約（ISO 8601 + Z / GLOB CHECK 等）
- JSON 列の妥当性検査（json_valid() / アプリ側検証）
- 集約・冪等性のルール（洗い替え / upsert / 増分）
-->

- **{{ルール 1}}**: {{根拠と適用範囲}}
- **{{ルール 2}}**: {{根拠と適用範囲}}


## 7. セキュリティ / アクセス制御

<!--
ガイダンス: 検出された RLS / ロール / 暗号化対象を列挙する。
- Supabase RLS ポリシー（permissive / restrictive）
- 列レベルの暗号化（暗号化対象列）
- 認証経路（anon key / service role / JWT）
該当ない DB 種別では本章を削除可。
-->

- {{セキュリティ規約 1}}


## 8. 既知の差分・要レビュー事項

<!--
ガイダンス: §4 階層間マッピングで判明した差分・型の不一致・移植時の注意点を表で列挙する。
追加で:
- TODO / FIXME / 非推奨カラム
- レガシー扱いになっている DROP のみのテーブル
- マイグレーション未完了の差分
該当無しならセクション自体を省略する。
-->

| # | 内容 | 影響 |
| ---: | --- | --- |
| 1 | {{差分・課題}} | {{影響範囲}} |
