---
title: "機能一覧"
date: "{{today}}"
updated: "{{today}}"
type: "spec"
lang: "ja"
author: "Claude Code v{{cliVersion}}"
category: "basic-design/features"
c4Scope:
    - "{{systemId}}"
excerpt: "コードグラフのコミュニティから抽出した {{N}} 機能の一覧と、孤立モジュール群の概況。"
---


# 機能一覧


## 1. 概要

<!--
ガイダンス: コミュニティ検出結果と「機能」として扱う基準を 1〜2 段落で説明する。
- 検出アルゴリズム（Louvain 系）
- 全体コミュニティ数 / うち命名済み件数 / カバレッジ %
- 「機能」として扱う条件（サイズ ≥ N + 個別命名）
- 章 3 への展開条件

# 章番号の安定化ルール（重要）
本テンプレでは 1〜6 番のセクションを固定で出力する:
1 概要
2 命名済み機能一覧
3 役割グループ別の分布
4 孤立モジュール（未命名コミュニティ）
5 未命名コミュニティ詳細
6 命名済みだが size < 3 のコミュニティ

検出 0 件のセクションでも **見出しは削除せず、本文を「(該当なし)」と書く**こと。
理由: 章番号や見出し集合が変わると evaluate=true で過去 commit と Jaccard 比較した際に
heuristic completeness が大幅低下するため (golden の見出し 7 → candidate 5 で 0.71 → 0.5 の事例あり)。
-->

`{{repoName}}` のコードグラフを Louvain 系コミュニティ検出にかけた結果、合計 {{TotalN}} コミュニティが識別された。\
サイズ ≥ {{minSize}} の {{LargeN}} 件と、サイズ 1〜2 で個別に名前付与された {{SmallN}} 件を合わせた {{NamedN}} 件を「機能」として扱い、章 3 で個別の設計書に展開する。

> [!NOTE]
> 命名済みカバレッジ: {{NamedN}} / {{TotalN}} ({{NamedPct}}%)。\
> 未命名コミュニティを充足するには `/anytime-reverse-codegraph` を実行する。


## 2. 命名済み機能（コミュニティ）一覧

<!--
ガイダンス: name が非空のコミュニティを ID 昇順で全件列挙する。
概要は summary を 60〜80 字程度に圧縮した **プレーンな日本語の 1 文** を入れる。
primary 役割の C4 要素を最大 3 件まで列挙。
詳細列には章 3 のファイルへの相対リンクを張る。

⚠️ 「概要」セル生成上の禁止事項（過去事故あり）:
- 文字単位で `\|` を挟まない（例: `\|ノ\|ー\|ド\|・\|エ\|ッ\|ジ` は誤り）。
- パイプエスケープ (`\|`) は summary 本文中に literal な `|` が含まれる時のみ適用する。
- summary を中間表現で `char1|char2|char3` のようにパイプ区切りで扱わない。`.split('').join(...)`
  相当の処理で文字を分解してはいけない。
- 圧縮は **文字列を意味単位で短縮**する。例: 「ノード・エッジ描画、衝突判定、ビューポート…」のように
  読点で区切られた連続した日本語文として 1 セル内に収める。
-->

| ID | 名称 | 概要 | 主要 C4 要素 | 詳細 |
| ---: | --- | --- | --- | --- |
| {{id}} | {{name}} | {{1 文要約}} | `{{pkg_xxx/yyy}}` | [→](03.feature-detail/feature-{{slug}}.ja.md) |


## 3. 役割グループ別の分布

<!--
ガイダンス: 命名済み機能を C4 Container の役割グループで分類した分布表。
グループ名は §1.system-overview と一致させる（コアライブラリ / ビューア / VS Code 拡張 / MCP サーバ / アプリ等）。
-->

| 役割グループ | 機能数 | コミュニティ ID |
| --- | ---: | --- |
| {{グループ}} | {{N}} | {{id1, id2, ...}} |


## 4. 孤立モジュール（未命名コミュニティ）

<!--
ガイダンス: name が空のコミュニティをサイズ別に集計する。
サイズ別の件数とノード数を表で示し、章末に「label のみ列挙」ではなく集計のみ。
個別の未命名コミュニティを全件列挙したい場合は §5 として別表を追加する。
-->

| サイズ | コミュニティ数 | 占有ノード数 |
| ---: | ---: | ---: |
| 2 | {{N}} | {{Nodes}} |
| 1 | {{N}} | {{Nodes}} |
| **合計** | **{{Total}}** | **{{TotalNodes}}** |

> [!NOTE]
> 孤立モジュールは単独ファイル or 2 ファイル単位で構成され、ドメイン的に「機能」と呼べる粒度に満たない。コードグラフのコミュニティ検出パラメータ調整、または手動グループ化により他のコミュニティに吸収されることが望ましい。


## 5. 未命名コミュニティ詳細

<!--
ガイダンス: 未命名（name が空）のコミュニティのうち、サイズ ≥ 3 の重要候補を列挙する。
サイズ 1〜2 の未命名コミュニティを §4 と二重列挙しない。
**該当 0 件でも見出しは残し、本文を「(該当なし)」と書く** (章番号安定化ルールに従う)。
-->

| ID | label | サイズ | 暫定主要パッケージ |
| ---: | --- | ---: | --- |
| {{id}} | `{{label}}` | {{size}} | `{{pkg}}` |


## 6. 命名済みだが size &lt; 3 のコミュニティ

<!--
ガイダンス: name が付与済みでサイズ < 3 のコミュニティを列挙する。
章 3 では個別ファイル化されないが、機能としては命名されているため一覧に残す価値がある。
**該当 0 件でも見出しは残し、本文を「(該当なし)」と書く** (章番号安定化ルールに従う)。
-->

| ID | 名称 | サイズ | 概要 | 主要パッケージ |
| ---: | --- | ---: | --- | --- |
| {{id}} | {{name}} | {{size}} | {{1 文要約}} | `{{pkg}}` |
