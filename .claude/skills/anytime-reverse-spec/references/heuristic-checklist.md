# ヒューリスティック評価チェックリスト（48 項目）

Phase 4（評価レポート）で使う項目レベルの評価観点。Nielsen の 10 原則を実務のチェック項目へ展開したもので、出典は `<docsRoot>/tech/UI_UXテスト観点リスト.md`（原典は note「実案件から学んだ、本当に役立つUIデザインの法則50」ほか）。

> 出典は「50 項目」と称するが、表の実数は **48 項目**である（原則別 9/4/4/4/4/4/4/9/3/3）。本書は実数に合わせる。

## 検出手段の分類

**この分類が本書の中核である。** 項目を並べただけのチェックリストは、外形観測で判定できない項目まで「未確認」として無言で落ちる。各項目がどこまで自動で判定できるかを先に決めておく。

| 区分 | 意味 | Phase 4 での扱い |
| --- | --- | --- |
| `auto` | `measure-ux.js` が判定する（実装済み） | 件数を実測し、根拠の種類は「規範照合」または「実測」 |
| `auto-todo` | 自動計測できるが未実装 | 現状は判定しない。**レポートに「未実装のため未判定」と書く**（2026-08-03 時点で該当 0 件） |
| `static` | 静的観測（スクリーンショット・DOM）で人が判定する | 判定は解釈を含むため、根拠の種類は「推定」 |
| `interactive` | クリック・入力・エラー誘発などの操作が要る | **現行 Phase 4 の範囲外**。レポートの「計測から除外した範囲」へ列挙する |
| `user-test` | 実ユーザーでしか判定できない | 未計測。必要な調査を書く |

内訳は `auto` 10 / `auto-todo` 0 / `static` 16 / `interactive` 21 / `user-test` 1（合計 48。`auto` の 10 には部分判定 2 件（1-8・8-3）とページ横断が要る 1 件（4-1）を含む）。

原則ごとの内訳は偏っている。

| 原則 | 項目数 | `auto` | `auto-todo` | `static` | `interactive` | `user-test` |
| --- | --- | --- | --- | --- | --- | --- |
| 1 システムステータスの可視化 | 9 | 1 | 0 | 1 | 7 | 0 |
| 2 実世界との一致 | 4 | 0 | 0 | 3 | 1 | 0 |
| 3 コントロールと自由度 | 4 | 0 | 0 | 2 | 2 | 0 |
| 4 一貫性と標準化 | 4 | 1 | 0 | 3 | 0 | 0 |
| 5 エラーの防止 | 4 | 2 | 0 | 1 | 1 | 0 |
| 6 記憶に頼らせない認識 | 4 | 2 | 0 | 0 | 2 | 0 |
| 7 柔軟性と効率性 | 4 | 0 | 0 | 2 | 2 | 0 |
| 8 美的で最小限 | 9 | 4 | 0 | 2 | 2 | 1 |
| 9 エラー回復の支援 | 3 | 0 | 0 | 0 | 3 | 0 |
| 10 ヘルプとドキュメント | 3 | 0 | 0 | 2 | 1 | 0 |
| **合計** | **48** | **10** | **0** | **16** | **21** | **1** |

**静的巡回で判定できるのは 26 項目（`auto` 10 ＋ `static` 16）にとどまり、残り 22 項目は判定できない。** 21 項目（44%）は操作を要し、1 項目は実ユーザーでしか判定できない。「チェックリストで評価した」と書くとき、この 22 項目が空欄であることを隠さない。

とくに**原則 1 は 9 項目中 7 項目、原則 9 は 3 項目すべてが `interactive`** であり、静的評価では原則 9 が丸ごと未判定になる。

## 1. システムステータスの可視化（9 項目）

| ID | 評価観点 | 検出手段 | 判定の手がかり |
| --- | --- | --- | --- |
| 1-1 | 入力項目が多い場合はステップを分けているか | `static` | 1 画面のフォーム項目数（`cognitive.formFields`）とステッパー要素の有無 |
| 1-2 | 特定のモードに入っていることが明示されているか | `interactive` | モードへ入る操作が要る |
| 1-3 | 画面変化に適切なアニメーションを用いているか | `interactive` | 遷移の観察が要る |
| 1-4 | モーダル表示時に背景を透過させているか | `interactive` | モーダルを開く操作が要る |
| 1-5 | 絞り込み検索の適用状態が明示されているか | `interactive` | フィルタ適用の操作が要る |
| 1-6 | データ非存在時のエンプティステートが準備されているか | `interactive` | 空データ状態を作る必要がある。ただし巡回中に 0 件の一覧へ当たれば観測できる |
| 1-7 | フィードバックが操作トリガーの近傍に表示されるか | `interactive` | 操作後の表示位置の観察が要る |
| 1-8 | サイトやアプリ内の現在地を常に把握できるか | `auto`（部分） | `doc.title` の実在とページ固有性は自動判定できる。パンくずの有無は `static` |
| 1-9 | トグルやスイッチの状態表示で混乱を招いていないか | `interactive` | 切り替え前後の比較が要る |

## 2. システムと現実世界の一致（4 項目）

| ID | 評価観点 | 検出手段 | 判定の手がかり |
| --- | --- | --- | --- |
| 2-1 | ユーザーが直感的に理解できる言葉を使用しているか | `static` | DOM から文言は取れるが、妥当性の判断はドメイン知識を要する。`ux-concept.md` の語彙表が材料 |
| 2-2 | ボタンやリンクの文言で「押した後の結果」を明示しているか | `static` | 「次へ」「OK」など結果を予見できない汎用ラベルの出現を数える |
| 2-3 | ダイアログの選択肢に明確な動詞を用いているか | `interactive` | ダイアログを開く操作が要る |
| 2-4 | 日付や数値の表記が一般的慣例に従っているか | `static` | 一覧の日付表記の統一（`YYYY/MM/DD`・ゼロパディング） |

## 3. ユーザーのコントロールと自由度（4 項目）

| ID | 評価観点 | 検出手段 | 判定の手がかり |
| --- | --- | --- | --- |
| 3-1 | 操作のキャンセルや修正が自由に行えるか | `interactive` | 入力途中からの離脱・Undo の実行が要る |
| 3-2 | カルーセル UI に直感的な操作ボタンが用意されているか | `static` | 送り矢印の有無とサイズ（`targetBelow24px` と併読） |
| 3-3 | スクロール可能であることが視覚的に示唆されているか | `static` | ファーストビュー境界での要素の見切れ。スクリーンショットで判定 |
| 3-4 | クリック・タップ可能領域（アフォーダンス）が明白か | `interactive` | hover 時の変化を含むため操作が要る |

## 4. 一貫性と標準化（4 項目）

| ID | 評価観点 | 検出手段 | 判定の手がかり |
| --- | --- | --- | --- |
| 4-1 | ナビゲーションの文言と遷移先画面のタイトルが一致しているか | `auto`（ページ横断） | `cognitive.navLinkTargets`（リンク文言 + href）を各ページの `doc.title` と突合する。突合は Phase 4 手順 2 で行う |
| 4-2 | タブパーツが標準的なメンタルモデルに沿っているか | `static` | タブの視覚表現とアクティブ状態の強調 |
| 4-3 | 類似する外観を持つ異なる機能に明確な差をつけているか | `static` | 同一スタイルで挙動が異なる要素の対を探す |
| 4-4 | 色の意味付けがプロダクト全体および社会的慣例と合致しているか | `static` | 削除・警告に緑、完了に赤などの反転。`DESIGN.md` の色トークンと突合 |

## 5. エラーの防止（4 項目）

| ID | 評価観点 | 検出手段 | 判定の手がかり |
| --- | --- | --- | --- |
| 5-1 | 取り返しのつかない破壊的操作が他操作と区別されているか | `static` | 削除ボタンの配置距離と配色 |
| 5-2 | フォームの入力幅を入力内容の長さに合わせているか | `auto` | `findings.inputWidthOversized`。想定文字数（`maxlength` か `type`）× フォントサイズ × 0.6 の 2 倍を超える幅を検出。上限が緩い欄（30 字超・`maxlength` なし）は判定しない |
| 5-3 | 制約のある入力形式に対して事前制御を行っているか | `auto` | `findings.inputFormatUnconstrained`。ラベル・placeholder・name が形式（メール・電話・郵便・日付・URL・数値）を示唆するのに `type="text"` で `pattern` も `inputmode` も無いものを検出 |
| 5-4 | 送信前の最終確認ステップが適切に組み込まれているか | `interactive` | 送信フローの実行が要る |

## 6. 記憶に頼らせない認識（4 項目）

| ID | 評価観点 | 検出手段 | 判定の手がかり |
| --- | --- | --- | --- |
| 6-1 | 前の画面で提示された情報を次の画面でも参照できるか | `interactive` | 画面遷移をまたぐ操作が要る |
| 6-2 | 自由入力を強制せず選択肢をサジェストしているか | `interactive` | 入力操作が要る |
| 6-3 | アイコンの意味がテキストなしで容易に認識できるか | `auto` | `findings.svgUnlabelled` / `findings.interactiveMissingName` |
| 6-4 | プレースホルダーの使い方を誤っていないか | `auto` | `findings.formControlUnlabelled`（ラベル無しで `placeholder` のみの入力欄） |

## 7. 柔軟性と利用の効率性（4 項目）

| ID | 評価観点 | 検出手段 | 判定の手がかり |
| --- | --- | --- | --- |
| 7-1 | 頻繁に使用するタスクへのショートカットが存在するか | `interactive` | キーボード操作の試行が要る |
| 7-2 | 一括処理（バルクアクション）機能が用意されているか | `static` | 一覧行のチェックボックスと一括操作 UI の有無 |
| 7-3 | 段階的開示（Progressive Disclosure）が実践されているか | `static` | 「詳細設定」等の折りたたみの有無と初期表示項目数 |
| 7-4 | 前回の操作状態や設定が保存されているか | `interactive` | 再訪して状態を比較する操作が要る |

## 8. 美的で最小限のデザイン（9 項目）

| ID | 評価観点 | 検出手段 | 判定の手がかり |
| --- | --- | --- | --- |
| 8-1 | 行間および行長が可読性に適した設定になっているか | `auto` | `findings.readabilityOutOfRange`。行間比 1.4 未満、または 1 行 50 字超を検出。行数は `Range#getClientRects()` の矩形数で数える（`scrollHeight` では折り返しを検知できない） |
| 8-2 | 錯視による視覚的ズレを意図的に補正しているか | `user-test` | 幾何学的中心と視覚的中心の差は人の目でしか判定できない |
| 8-3 | 強調表現の多用による優先度のインフレを防いでいるか | `auto`（部分） | `cognitive.distinctTextColors` / `distinctFontFamilies`。太字比率は未計測（本項目は色数・書体数のみの部分判定） |
| 8-4 | 情報や機能が隠されずに明示されているか | `interactive` | hover でのみ出現する操作の検出には操作が要る |
| 8-5 | ボタンの優先度に応じたスタイリングがなされているか | `static` | 主・副アクションの塗り／枠線／テキストの差 |
| 8-6 | 関連するフォームや情報が近接の原則で整理されているか | `static` | ラベルと入力欄の距離。スクリーンショットで判定 |
| 8-7 | 情報の識別を「色」のみに依存していないか | `interactive` | エラー状態を発生させる操作が要る |
| 8-8 | コントラスト比がアクセシビリティ基準を満たしているか | `auto` | `findings.contrastBelowAA`（WCAG 2.1 AA 1.4.3・4.5:1／大きい文字 3:1） |
| 8-9 | 情報チャンクの数が人間の短期記憶容量内に収まっているか | `auto` | `cognitive.navLinkCount`（Miller 7±2） |

## 9. エラーからの認識・診断・回復の支援（3 項目）

| ID | 評価観点 | 検出手段 | 判定の手がかり |
| --- | --- | --- | --- |
| 9-1 | エラーメッセージが該当フィールドのすぐ側に表示されるか | `interactive` | エラーを発生させる必要がある |
| 9-2 | エラーの発生理由と具体的回復手段が書かれているか | `interactive` | 同上 |
| 9-3 | エラー修正時に入力済みデータが保持されているか | `interactive` | 同上 |

**この原則は 3 項目すべてが `interactive` である。** 静的巡回だけの評価では原則 9 は丸ごと未判定になる。レポートで「エラー処理に問題なし」と書いてはならない。

## 10. ヘルプとドキュメント（3 項目）

| ID | 評価観点 | 検出手段 | 判定の手がかり |
| --- | --- | --- | --- |
| 10-1 | アイコンの意味を補足するヘルプ機能が存在するか | `static` | `title` 属性・tooltip 要素の有無（`findings.svgUnlabelled` と併読） |
| 10-2 | タスク実行中の文脈に沿ったヘルプ（インコンテキスト）か | `static` | フォーム脇の「?」等インライン導線の有無 |
| 10-3 | ヘルプ内の検索性が高く解決策へ直接導かれているか | `interactive` | ヘルプ内の検索操作が要る |

## Phase 4 での使い方

1. `auto` の 10 項目は `measure-ux.js` の出力から機械的に埋める。うち 4-1 のみページ横断の突合が要る。
2. `static` の 16 項目はスクリーンショットと DOM から人が判定し、**根拠の種類を「推定」**とする。
3. `auto-todo` は現在 0 件。将来この区分へ項目が入ったら「自動計測できるが未実装のため未判定」と書く。**「問題なし」と書かない。**
4. `interactive` の 21 項目と `user-test` の 1 項目は、レポート §2 の「計測から除外した範囲」へ列挙する。
5. 検出した課題は `ux-report-template.md` の課題 ID（`UX-NN`）を振り、本書の項目 ID（`8-8` 等）を根拠として併記する。

## 評価手法の位置づけ

本スキルが行うのは**ヒューリスティック評価（エキスパートレビュー）**であり、開発早期に低コストで UI 欠陥を洗い出す手法である。次の限界が原理的に伴う。

| 手法 | 実施主体 | 本スキルとの関係 |
| --- | --- | --- |
| ヒューリスティック評価 | UI/UX 専門家 | **本スキルが行うもの。** 実際のユーザー挙動と乖離しうる |
| ユーザビリティテスト | 想定ユーザー | 行わない。SUS・タスク完了率・所要時間はここでしか得られない |
| 認知的ウォークスルー | 開発チーム・専門家 | 行わない。タスク単位の認知阻害要因は取れない |
| アイトラッキング | 想定ユーザー | 行わない |
| アンケート（SUS / NPS） | 一般ユーザー | 行わない |
| A/B テスト | 実運用トラフィック | 行わない |

ヒューリスティック評価の妥当性を上げるには本来 3〜5 名の独立した評価者が要る。**単一エージェントによる評価はこの条件を満たさない**ため、その旨をレポート §7 に書く。
