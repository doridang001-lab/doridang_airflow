// ===================================================================================
// ================= 00_config.js : 전역 변수 선언 =================
// ===================================================================================

// ===================================================================================
// ================= COMMON UTILITIES =================
// ===================================================================================

let DOWNLOAD_PATH = '';

// 다운로드 경로 불러오기
chrome.storage.sync.get(['downloadPath'], (result) => {
  DOWNLOAD_PATH = result.downloadPath || '';
  console.log(`[Collector] 다운로드 경로: ${DOWNLOAD_PATH || '기본'}`);
});

// 공유 전역 객체 선언 (subsequent scripts에서 사용)
var Utils = {};
var Sites = {};
