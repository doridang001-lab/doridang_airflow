# 배민 크롬확장 NOW 수집 에러 수정 (NOW 수집 + 매장선택 정렬)

## Task
크롬확장 배치(`runner_baemin.html`)의 배민 우리가게NOW 수집이 잘못된 셀렉터로 빈 데이터를 "성공" 처리하는 버그를 고친다. 핵심 증거는 NOW 로그의 `rows=88`(실제 지표는 6항목뿐). 셀렉터/준비판정/매장선택 로직을 정답 레퍼런스인 Selenium DAG(`modules/extract/croling_beamin.py`)와 동일한 방식으로 정렬한다.

## Project Conventions (이 태스크 = 크롬확장 JS, Python 아님)
- 대상은 브라우저 확장 JS(content script + runner). Python 규칙 아님.
- 수정 파일은 **레포 밖 OneDrive 폴더**에 있음. 경로 그대로 사용.
- 셀렉터/파싱 기준은 항상 DAG(`croling_beamin.py`)의 검증된 구현을 따른다.
- 기존 CSV 헤더 컬럼 구조는 절대 변경하지 않는다(다운스트림 적재 DAG 호환).
- 기존 코드 스타일(들여쓰기 2칸, camelCase, `Sites['baemin']` 객체 메서드) 유지.

## Files to Create / Modify
수정(2개):
- `C:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용\content\02_baemin.js`
  - 함수 `_collectMetrics` (약 L1602~)
- `C:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용\runner_baemin.js`
  - 함수 `waitForBaeminDashboardReady` (L557~585)
  - 함수 `selectTargetStore` (L392~555)
  - 함수 `captureBaeminDebug` (L331~375)

레퍼런스(읽기 전용, 수정 금지):
- `C:\airflow\modules\extract\croling_beamin.py`
  - `_COLLECT_METRICS_JS` (L533~569), `wait_for_metrics_data` (L996~1023), `select_store_by_id` (L895~993), `get_store_options` (L862~875)

## Implementation Steps

### 1. `content/02_baemin.js` — `_collectMetrics` 를 Item 기반 + 값 검증으로 교체
현재(버그):
```js
let cards = document.querySelectorAll('.WooriShopNowCard-module__rcFf');           // 0개 매칭(클래스 해시 변경)
if (!cards.length) cards = document.querySelectorAll('div[data-atelier-component="Container"]'); // 88개 범용 컨테이너 폴백
// cards.forEach(...) → 88 junk row
```
교체 명세:
- 위 두 셀렉터(`WooriShopNowCard-module__rcFf`, `div[data-atelier-component="Container"]` 폴백) **완전 제거**.
- DAG `_COLLECT_METRICS_JS`(아래 Reference Code) 와 동일하게 `.WooriShopNowItem-module__TKcC` 를 순회한다.
  - 각 item에서 `item.querySelectorAll('span')` 로 spans 획득. `spans.length < 2` 면 skip.
  - `spans[0].textContent.trim()` = label. `LABELS`(조리소요시간/주문접수시간/최근재주문율/조리시간준수율/주문접수율/최근별점)에 없으면 skip.
  - 값 = `spans[1].textContent.trim()` 에서 `[\d.]+` 추출. 비율 항목(조리시간준수율/주문접수율/최근재주문율)은 `/100`.
  - 순위 = `spans[2]` (있으면) `^(상위|하위)\s*([\d.]+)%$` 파싱 → `_순위구분`, `_순위비율`(=값/100).
- **출력은 매장당 1행**: 기존 CSV `headers`(L1659~1667) 컬럼 구조 그대로 유지하고, label별 컬럼에 위 값 채움.
  즉 `rows` 는 길이 1 배열(`[data]`)이 되어야 한다(88행 아님).
- 값이 채워진 item이 0개면 기존대로 `{ success:false, error:'메트릭 데이터 없음' }` 반환.
- `collected_at`, `store_id`, `store_name`, `url` 등 메타 컬럼은 기존과 동일하게 채운다. `cardIndex` 컬럼은 유지하되 단일행이므로 `0`.

### 2. `runner_baemin.js` — `waitForBaeminDashboardReady` 준비판정을 실제 값 검증으로 교체
현재(버그): probe가 `div[data-atelier-component="Container"]` cardCount + 본문 텍스트 라벨 존재 + `h3`(항상 존재) 로 판정 → 라벨만 바뀌어도 조기 ready.
교체 명세:
- in-page `func` 의 probe를 DAG `wait_for_metrics_data`(아래 Reference Code) 와 동일하게:
  ```js
  const items = document.querySelectorAll('.WooriShopNowItem-module__TKcC');
  let hasData = false;
  if (items.length) {
    const spans = items[0].querySelectorAll('span');
    const LABELS = ['조리소요시간','주문접수시간','최근재주문율','조리시간준수율','주문접수율','최근별점'];
    hasData = spans.length >= 2 && LABELS.includes(spans[0].textContent.trim()) && spans[1].textContent.trim() !== '';
  }
  return { hasData, url: location.href };
  ```
- 조기 종료 폴백 `(lastInfo.store_name && value.hasStoreSelector)` **제거**.
- ready 조건 = `value.hasData === true` **그리고** (`!expectedStore || isSameStoreName(lastInfo.store_name, expectedStore)`).
- 타임아웃 직전 1회 `await chrome.tabs.reload(tabId)` 후 재폴링(woori 카테고리의 reload 패턴 재사용). 이미 반복 루프이므로, deadline 절반 경과 시점에 1회만 reload 수행하도록 플래그 처리.

### 3. `runner_baemin.js` — `selectTargetStore` 를 ShopSelect value 설정 + 검증으로 단일화
- in-page `selectStore()` 의 select 쿼리에서 **끝의 bare `, select` 폴백 제거** →
  `'select.Select-module__a623, select.ShopSelect-module___pC1, .ShopSelect-module__JWCr select'` 만 사용(서비스 selector self/ceo/store 매칭 방지).
- 선택 후 검증을 DAG `select_store_by_id._verify` 와 동일하게:
  `select.value === option.value` **또는** `.ShopSelect-module__j4Qm` 텍스트에 storeId 포함 → true.
- click>click 폴백(`findTarget`, `openers` 루프, `candidateOpeners` 루프) **제거**. ShopSelect value 설정이 표준 경로다.
  (단일 계정 다매장도 동일 `<select>` value 전환으로 처리됨 — 로그상 selectCount=2 중 두 번째가 ShopSelect.)
- 선택 직후 `waitForBaeminDashboardReady`(변경 2) 호출해 실제 값 로드 확인 후 `selected` 반환(기존 흐름 유지).

### 4. `runner_baemin.js` — `captureBaeminDebug` 셀렉터 정정 (저비용)
- in-page `func` 의 `select` 쿼리에서 끝의 bare `, select` 제거 → ShopSelect 한정.
- `cardCount` 계산을 `.WooriShopNowItem-module__TKcC` 기준으로 변경(정상 ~6, 비정상 0). `div[data-atelier-component="Container"]` 제거.

## Reference Code

### croling_beamin.py — `_COLLECT_METRICS_JS` (지표 추출 정답)
```python
_COLLECT_METRICS_JS = r"""
return (function() {
    const LABELS = ['조리소요시간','주문접수시간','최근재주문율','조리시간준수율','주문접수율','최근별점'];
    const RATIO  = new Set(['조리시간준수율','주문접수율','최근재주문율']);
    const result = {};
    const items = document.querySelectorAll('.WooriShopNowItem-module__TKcC');
    for (const item of items) {
        const spans = item.querySelectorAll('span');
        if (spans.length < 2) continue;
        const label = spans[0].textContent.trim();
        if (!LABELS.includes(label)) continue;
        const rawVal  = spans[1].textContent.trim();
        const rawRank = spans.length > 2 ? spans[2].textContent.trim() : '';
        const numMatch = rawVal.match(/[\d.]+/);
        let numStr = numMatch ? numMatch[0] : '';
        if (numStr && RATIO.has(label)) numStr = String(parseFloat(numStr) / 100);
        const rankMatch = rawRank.match(/^(상위|하위)\s*([\d.]+)%$/);
        result[label]               = numStr;
        result[label + '_순위구분'] = rankMatch ? rankMatch[1] : '';
        result[label + '_순위비율'] = rankMatch ? String(parseFloat(rankMatch[2]) / 100) : '';
    }
    return result;
})();
"""
```

### croling_beamin.py — `wait_for_metrics_data` (준비판정 정답)
```python
def wait_for_metrics_data(driver, timeout: int = 45) -> bool:
    def _has_data(d):
        return d.execute_script(r"""
            const LABELS = ['조리소요시간','주문접수시간','최근재주문율',
                            '조리시간준수율','주문접수율','최근별점'];
            const items = document.querySelectorAll('.WooriShopNowItem-module__TKcC');
            if (!items.length) return false;
            const spans = items[0].querySelectorAll('span');
            return spans.length >= 2
                && LABELS.includes(spans[0].textContent.trim())
                && spans[1].textContent.trim() !== '';
        """)
    for attempt in range(2):
        try:
            WebDriverWait(driver, timeout).until(_has_data)
            return True
        except TimeoutException:
            if attempt == 0:
                driver.refresh()
                time.sleep(random.uniform(3.0, 5.0))
    return False
```

### croling_beamin.py — `select_store_by_id._verify` (매장전환 검증 정답)
```python
def _verify(wait_sec=5):
    sel_el = WebDriverWait(driver, wait_sec).until(
        EC.presence_of_element_located((By.CSS_SELECTOR, "select[class*='ShopSelect']")))
    actual = driver.execute_script("return arguments[0].value;", sel_el)
    if str(actual) == str(store_id):
        return True
    info = driver.find_element(By.CSS_SELECTOR, "[class*='ShopSelect-module__j4Qm']")
    if str(store_id) in info.text:
        return True
    return False
```

## Test Cases
JS는 정적 검증 + 확장 reload 후 실제 수집 검증으로 확인한다.

1. [구문 검증] `node --check "C:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용\runner_baemin.js"` → 기대: 에러 없음
2. [구문 검증] `node --check "C:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용\content\02_baemin.js"` → 기대: 에러 없음
3. [잔존 셀렉터 0건] `02_baemin.js` 와 `runner_baemin.js` 에서 `WooriShopNowCard-module__rcFf` 및 `data-atelier-component="Container"` 가 NOW 수집/준비판정 경로에 더 이상 없는지 grep → 기대: 0건
4. [bare select 폴백 제거] `selectTargetStore`/`captureBaeminDebug` 의 select 쿼리 문자열이 `', select'` 로 끝나지 않는지 확인 → 기대: ShopSelect 한정만 존재
5. [실측] 확장 reload 후 `runner_baemin.html`에서 도리당 강원영월점(skmo1004 / 14853280) 단일 수집 → 기대: NOW 로그 `rows=1`(6개 지표 컬럼 채워짐), `rows=88` 아님
6. [실측] 매장 전환 후 `[DBG dashboard-ready]` 의 cards가 전환 전과 다르고 값이 채워진 뒤 통과 → 기대: 곱도리탕(14853281) vs 도리당(14853280) 저장 데이터가 서로 다름
7. [실측] 2단계 상세(woori/change/orders/ad)까지 실패 배지 없이 완료 → 기대: 전체 완료

## Verification Loop
```
LOOP until all PASS:
  1. Test Cases 1~4 (정적) 먼저 실행
  2. FAIL 항목 → 원인 분석 → 코드 수정 → 1로
  3. 정적 PASS 후 사용자에게 확장 reload + Test 5~7 실측 요청
종료 조건: 정적 1~4 전부 PASS + 실측 5~7 PASS + Constraints 위반 없음
```

## Constraints
- CSV 헤더 컬럼(`headers`, 02_baemin.js L1659~1667) 변경 금지 — 행 수만 88→1로 정상화.
- 매장명 정규화(`storeKey`/`_storeKey` 의 `닭도리탕전문` 치환) 건드리지 말 것 — 현재 매칭 정상 동작.
- woori/change/orders/ad 수집 함수는 이번 범위 아님 — 건드리지 말 것.
- LABELS 배열 6항목 순서/표현 정확히 유지(DAG와 동일).
- 레퍼런스 `croling_beamin.py` 는 읽기 전용 — 수정 금지.

## Do Not Ask — Decide Yourself
- 파일이 이미 존재함(수정 대상) → 해당 함수만 교체.
- 셀렉터/파싱이 모호하면 → Reference Code의 DAG 구현을 그대로 따른다.
- 타입/주석 스타일 → 기존 파일과 동일(주석 최소화, WHY만).
- 변수명 스타일 → 기존 파일과 동일(camelCase).
- reload 1회 플래그 구현 방식 → 가장 단순한 로컬 변수로 처리.
