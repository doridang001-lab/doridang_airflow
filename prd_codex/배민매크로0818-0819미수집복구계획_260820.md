# 배민매크로0818-0819미수집복구계획

## Task

배민 매크로 수집이 2026-08-18(41/76매장), 2026-08-19(정기수집분 0행)에서 대량 누락됐다.
원인은 (1) 날짜 필터 specific 모드 DOM 셀렉터 파손으로 어제 이전 날짜 수집 전면 불가,
(2) 미커밋 정산 게이트가 합계검증을 통과한 27개 매장 주문을 전량 폐기, (3) 백필 43건이
`max_active_runs=1` 슬롯을 점유해 정기 DagRun 생성 봉쇄, (4) 스케줄 가드가 그 상태를 탐지 못함.
이 4가지를 코드 수정으로 해결한다.

## Project Conventions

- logging: `logger = logging.getLogger(__name__)` — print 절대 금지
- import 경로: `from modules.transform.utility.XXX import ...` (절대경로)
- 파이프라인 파일 위치: `modules/transform/pipelines/{domain}/`
- DAG 파일 위치: `dags/{domain}/`
- 스케줄 상수: `from modules.transform.utility.schedule import ...` (하드코딩 금지)
- 경로 상수: `from modules.transform.utility.paths import ...` (하드코딩 금지)

## Files to Create / Modify

수정:
- `modules/transform/pipelines/db/DB_Beamin_04_orders.py` (정산 게이트, 달력 월 파싱, 정산 섹션 대기)
- `modules/transform/utility/dag_schedule_guard.py` (`OverdueSchedule`, `collect_overdue_schedules`, `format_overdue_schedule_alert`)
- `dags/strategy/Strategy_ScheduleGuard_01_Overdue_Dags.py` (`trigger_recovery_runs`)

생성: 없음

## Implementation Steps

### 1. 정산 게이트 완화 — 데이터 유실 중단

`DB_Beamin_04_orders.py`

1-1. `_MIN_SETTLE_RATE = 0.9` (`:73`) 을 환경변수로 분리한다.

```python
_MIN_SETTLE_RATE = float(os.getenv("BAEMIN_MIN_SETTLE_RATE", "0.9"))
```

`os`가 미import이면 파일 상단 import 블록에 추가한다.

1-2. `_block_low_settle_rate()` (`:374`) 에서 `matched` 강등을 제거한다.
현재는 `vr["matched"] = False`로 만들어 저장 자체를 차단하는데, 이 매장들은 금액·건수 합계검증을
**통과한** 정상 데이터다. 플래그만 남기고 저장은 진행시킨다.

```python
def _block_low_settle_rate(vr: dict) -> dict:
    """정산정보 수집률이 낮아도 합계검증을 통과했으면 저장한다.

    settle_rate는 배민 UI 렌더 실패에 좌우되므로 저장 차단 기준으로 쓰지 않는다.
    """
    if not _has_low_settle_rate(vr):
        return vr
    vr["settlement_suspect"] = True
    vr["reason"] = "low_settle_rate"
    vr["save_partial"] = True
    return vr
```

`matched`가 `True`로 유지되므로 `collect_orders_for_driver()` (`:848`)의
`if vr_normal.get("matched") is False:` 분기를 타지 않고 `elif rows:`로 내려가 정상 저장된다.

1-3. `_collect_with_retry_on_mismatch()` (`:684~760`) 에서 실패 사유가 `low_settle_rate` 뿐일 때
재수집 횟수를 1회로 제한한다. 현재는 합계가 맞는데도 매장당 최대 3회 전체 재수집을 돌아
`retry_failed`의 120분 `execution_timeout`을 소진시킨다.

`:734`의 `if attempt >= max_retry: break` 앞에 다음을 추가한다.

```python
        if low_settle_rate and vr.get("matched") is True and attempt >= 1:
            logger.warning(
                "정산정보 수집률 낮음 재시도 중단(합계검증 통과): %s / %s rate=%s",
                store,
                status_label,
                None if settle_rate is None else f"{settle_rate:.1%}",
            )
            break
```

### 2. 날짜 필터 specific 모드 복구 — 최우선

`DB_Beamin_04_orders.py`

`_get_calendar_ym()` (`:1539`) 이 `[class*="DatePicker"][class*="Header"]` 등 클래스 substring으로
달력 헤더를 찾는데, 현재 배민 DOM 클래스는 전부 해시(`c_qx9u_13c33de7`, `Typography_b_r4ax_1bisyd49`)라
매칭되지 않는다. `None` 반환 → `:1565`의 `if not ym: break`로 월 이동을 포기 → `final_ym=None` → 실패.

2-1. `_get_calendar_ym()` 을 클래스 비의존 + 다단 폴백으로 교체한다.

```python
        def _get_calendar_ym(d):
            """달력에서 (year, month) 반환. 실패 시 None.

            배민 DOM 클래스가 빌드마다 해시로 바뀌므로 클래스 substring에 의존하지 않는다.
            """
            text = d.execute_script(
                """
                const anchors = [
                    '[data-atelier-component*="DatePicker"]',
                    '[data-atelier-component*="Calendar"]',
                    '[role="dialog"]',
                    '[class*="DatePicker"][class*="Header"]',
                    '[class*="CalendarHeader"]',
                ];
                for (const sel of anchors) {
                    for (const el of document.querySelectorAll(sel)) {
                        const t = (el.textContent || '').replace(/\\s+/g, ' ');
                        if (/(\\d{4})\\s*[.년]\\s*(\\d{1,2})\\s*[.월]?/.test(t)) return t;
                    }
                }
                const prev = [...document.querySelectorAll('button')]
                    .find(b => /이전\\s*달/.test(b.getAttribute('aria-label') || ''));
                if (prev) {
                    let node = prev.parentElement;
                    for (let i = 0; i < 4 && node; i++, node = node.parentElement) {
                        const t = (node.textContent || '').replace(/\\s+/g, ' ');
                        if (/(\\d{4})\\s*[.년]\\s*(\\d{1,2})\\s*[.월]?/.test(t)) return t;
                    }
                }
                return null;
                """
            )
            if not text:
                return None
            m = _re.search(r"(\d{4})\s*[.년]\s*(\d{1,2})", text)
            return (int(m.group(1)), int(m.group(2))) if m else None
```

2-2. 월 이동 루프 (`:1561~1596`)에서 `ym`이 `None`일 때 즉시 `break`하지 않고,
`_date_popup_debug_state(driver)`를 남긴 뒤 한 번만 재시도하고 그래도 실패하면 사유를 구분해 로깅한다.

```python
        ym_unreadable = 0
        for _ in range(24):
            ym = _get_calendar_ym(driver)
            if ym and (ym[0], ym[1]) == (target_year, target_month):
                break
            if not ym:
                ym_unreadable += 1
                if ym_unreadable >= 2:
                    logger.warning(
                        "달력 헤더 월 파싱 실패(셀렉터 회귀 의심): target=%s popup=%s",
                        target_date,
                        _date_popup_debug_state(driver),
                    )
                    break
                time.sleep(0.4)
                continue
            direction = "prev" if (ym[0] * 12 + ym[1]) > target_month_index else "next"
            ...  # 기존 월 이동 JS 그대로
```

2-3. **중요 — 달력이 두 달치 그리드를 동시에 렌더한다.** 디버그 덤프의 `ariaButtons`에
`1일`~`31일` 세트가 두 번 등장한다. `_get_calendar_ym`이 어느 쪽 월을 반환하는지 불확정이므로,
시작일 클릭 (`:1607` 이후 `clicked_start`) 로직이 두 그리드 중 잘못된 쪽을 클릭할 수 있다.
시작일/종료일 클릭 후 반드시 `_confirm_visible_order_date(driver, target_date, target_date)` (`:1674`)로
실측 검증하는 현재 흐름을 유지하고, 검증 실패 시 반대쪽 그리드를 클릭하는 재시도를 1회 넣는다.

### 3. 정산 섹션 렌더 대기 안정화

`DB_Beamin_04_orders.py`

3-1. `_wait_settle_section()` (`:2078`) timeout `0.8` → `2.5`.

3-2. `_SETTLE_READY_JS` (`:2054`) 에서 해시 클래스 의존을 제거한다.
`.DetailInfo-module__pZYe` / `.DetailInfo-module__Sopx` / `.DetailInfo-module__bKQt` /
`.FieldItem-module__gYJs` 대신 `td[colspan="9"]` 하위 `section` 중 헤더 텍스트에 `정산정보`가
포함되고 자식 항목이 1개 이상인 것을 찾는다. `tr.Table_b_r4ax_1dwbr4on[data-index]`(`_TABLE_ROW_CSS`)는
현재도 정상 동작 중이므로 그대로 둔다.

```python
_SETTLE_READY_JS = r"""
const row = document.querySelector('tr.Table_b_r4ax_1dwbr4on[data-index="' + arguments[0] + '"]');
if (!row) return false;

let detail = null;
let sibling = row.nextElementSibling;
while (sibling && sibling.tagName === 'TR') {
    const cell = sibling.querySelector('td[colspan="9"]');
    if (cell) { detail = cell; break; }
    sibling = sibling.nextElementSibling;
}
if (!detail) return false;

for (const sec of detail.querySelectorAll('section, div')) {
    const head = (sec.firstElementChild?.textContent || '').replace(/\s+/g, '');
    if (head.includes('정산정보') && sec.children.length > 1) return true;
}
return false;
"""
```

3-3. `정산정보 미렌더` 행별 WARNING (`:1781`) 을 매장 단위 집계로 축약한다.
현재 run당 1,000~2,000줄이 쌓여 로그가 사실상 못 읽는 상태다.
`_collect_all_pages()` 안에서 카운터를 누적하고 함수 종료 직전 한 줄만 남긴다.

```python
    if settle_miss_count:
        logger.warning("정산정보 미렌더: %s %d/%d행", store, settle_miss_count, len(all_rows))
```

### 4. 스케줄 가드 사각지대 제거

`modules/transform/utility/dag_schedule_guard.py`

Airflow는 `max_active_runs` 한도에 걸린 DAG의 `next_dagrun_create_after`를 **NULL**로 만든다.
현재 `collect_overdue_schedules()` (`:125`)가 `.filter(dag_model.next_dagrun_create_after.isnot(None))`로
정확히 그 상태를 걸러내 탐지 불가다.

4-1. `OverdueSchedule` (`:23`)에 `blocked` 필드 추가.

```python
@dataclass(frozen=True)
class OverdueSchedule:
    dag_id: str
    next_dagrun: Any
    next_dagrun_create_after: Any
    lag_minutes: int
    blocked: bool = False
```

4-2. `collect_overdue_schedules()` 의 `create_after` NOT NULL 필터를 제거하고,
NULL인 경우 `next_dagrun` 기준으로 lag를 계산해 `blocked=True`로 잡는다.

```python
    query = (
        session.query(dag_model)
        .filter(dag_model.is_active == True)  # noqa: E712
        .filter(dag_model.is_paused == False)  # noqa: E712
        .filter(dag_model.next_dagrun.isnot(None))
        .order_by(dag_model.next_dagrun)
    )
    ...
    for model in query.all():
        ...
        create_after = _to_pendulum_datetime(model.next_dagrun_create_after)
        blocked = create_after is None
        anchor = _to_pendulum_datetime(model.next_dagrun) if blocked else create_after
        if anchor is None:
            continue
        anchor = anchor.in_timezone("UTC")
        if anchor > threshold:
            continue
        lag_minutes = max(0, int((current - anchor).total_seconds() // 60))
        overdue.append(
            OverdueSchedule(
                dag_id=str(model.dag_id),
                next_dagrun=model.next_dagrun,
                next_dagrun_create_after=model.next_dagrun_create_after,
                lag_minutes=lag_minutes,
                blocked=blocked,
            )
        )
```

`.limit(limit)`은 DB 단 필터가 느슨해진 만큼 파이썬 루프에서 `len(overdue) >= limit` 체크로 옮긴다.

4-3. `format_overdue_schedule_alert()` (`:180`) 에서 `blocked` 항목을
`max_active_runs 봉쇄 의심`으로 구분 표기한다.

`dags/strategy/Strategy_ScheduleGuard_01_Overdue_Dags.py`

4-4. `trigger_recovery_runs()` 에서 `blocked=True` 항목은 **자동 트리거하지 않고 알림만** 한다.
슬롯이 이미 꽉 찬 상태라 트리거해봐야 큐만 늘어난다.

```python
    for item in overdue:
        if getattr(item, "blocked", False):
            logger.warning(
                "%s 스케줄 봉쇄(max_active_runs 의심), 자동 트리거 생략: next_dagrun=%s lag=%d분",
                item.dag_id,
                item.next_dagrun,
                item.lag_minutes,
            )
            skipped.append(f"{item.dag_id}:blocked")
            continue
        ...  # 기존 트리거 로직
```

## Reference Code

### modules/transform/pipelines/db/DB_Beamin_04_orders.py — 정산 게이트 현재 코드

```python
_TABLE_ROW_CSS = "tr.Table_b_r4ax_1dwbr4on[data-index]"
_MAX_PAGES = 50
_PAGE_TRANSITION_TIMEOUT = 25
_MAX_VALIDATION_RETRY = 2
_MIN_SETTLE_RATE = 0.9


def _has_low_settle_rate(vr: dict) -> bool:
    settle_rate = vr.get("settle_rate")
    return (
        vr.get("matched") is True
        and settle_rate is not None
        and float(settle_rate) < _MIN_SETTLE_RATE
    )


def _block_low_settle_rate(vr: dict) -> dict:
    if not _has_low_settle_rate(vr):
        return vr
    vr["matched"] = False
    vr["settlement_suspect"] = True
    vr["reason"] = "low_settle_rate"
    vr["save_partial"] = False
    return vr


# collect_orders_for_driver() 저장 분기 (:846~862)
        if vr_normal.get("matched") is False:
            if vr_normal.get("save_partial") and rows:
                saved = _save_orders_csv(rows, brand, store, target_date)
                logger.warning("날짜 필터 혼입 부분 저장 후 실패 유지: ...")
            logger.warning("검증 불일치로 정상 주문 저장 생략: %s / %s", store, target_date)
            ok = False
            failure_reason = str(vr_normal.get("reason") or "validation_mismatch")
        elif rows:
            saved = _save_orders_csv(rows, brand, store, target_date)
            logger.info("저장 완료(정상): brand=%s store=%s → %s (%d행)", brand, store, saved, len(rows))
```

### modules/transform/pipelines/db/DB_Beamin_04_orders.py — 파손된 달력 파싱

```python
def _set_date(driver, target_date: str) -> bool:
    """target_date에 맞는 날짜 필터 함수를 선택해 호출한다."""
    today = pendulum.now(KST).format("YYYY-MM-DD")
    yesterday = pendulum.yesterday(KST).format("YYYY-MM-DD")
    if target_date == today:
        return _set_date_relative_for_target(driver, target_date, "오늘") or _set_date_specific(driver, target_date)
    if target_date == yesterday:
        return _set_date_relative_for_target(driver, target_date, "어제") or _set_date_specific(driver, target_date)
    return _set_date_specific(driver, target_date)


# _set_date_specific() 내부 (:1539~1567) — 여기가 파손 지점
        def _get_calendar_ym(d):
            """달력 헤더에서 (year, month) 반환. 실패 시 None."""
            text = d.execute_script(
                """
                const h = document.querySelector('[class*="DatePicker"][class*="Header"], [class*="CalendarHeader"], [class*="Calendar"][class*="Header"]');
                return h ? h.textContent : null;
                """
            )
            if not text:
                return None
            m = _re.search(r"(\d{4})[^\d]+(\d{1,2})", text)
            return (int(m.group(1)), int(m.group(2))) if m else None

        for _ in range(24):
            ym = _get_calendar_ym(driver)
            if ym and (ym[0], ym[1]) == (target_year, target_month):
                break
            if not ym:
                break          # ← None이면 즉시 포기. 현재 100% 이 경로
```

### modules/transform/utility/dag_schedule_guard.py — 가드 현재 코드

```python
@dataclass(frozen=True)
class OverdueSchedule:
    dag_id: str
    next_dagrun: Any
    next_dagrun_create_after: Any
    lag_minutes: int


def collect_overdue_schedules(
    session: Any,
    dag_model: Any,
    *,
    dag_run_model: Any | None = None,
    now: Any | None = None,
    grace_minutes: int = 10,
    exclude_dag_ids: Iterable[str] | None = None,
    skip_existing_logical_runs: bool = False,
    limit: int = 50,
) -> list[OverdueSchedule]:
    """Return unpaused DAGs whose scheduled DagRun creation is late."""
    current = pendulum.instance(now or pendulum.now("UTC")).in_timezone("UTC")
    threshold = current.subtract(minutes=grace_minutes)
    query = (
        session.query(dag_model)
        .filter(dag_model.is_active == True)  # noqa: E712
        .filter(dag_model.is_paused == False)  # noqa: E712
        .filter(dag_model.next_dagrun.isnot(None))
        .filter(dag_model.next_dagrun_create_after.isnot(None))   # ← 사각지대
        .filter(dag_model.next_dagrun_create_after <= threshold)
        .order_by(dag_model.next_dagrun_create_after)
    )
```

## Test Cases

1. **모듈 import** `docker exec airflow-airflow-worker-1 python -c "import modules.transform.pipelines.db.DB_Beamin_04_orders as m; print(m._MIN_SETTLE_RATE)"` → 기대: `0.9` 출력, ImportError 없음
2. **정산 게이트 비차단** `docker exec airflow-airflow-worker-1 python -c "
from modules.transform.pipelines.db.DB_Beamin_04_orders import _block_low_settle_rate
vr={'matched':True,'settle_rate':0.0,'settle_count':0,'settle_denominator':14}
out=_block_low_settle_rate(dict(vr))
assert out['matched'] is True, out
assert out['settlement_suspect'] is True, out
print('OK', out['reason'])"` → 기대: `OK low_settle_rate`, AssertionError 없음
3. **정산 rate None 무영향** 같은 함수에 `{'matched':True,'settle_rate':None}` 입력 → 기대: `settlement_suspect` 키 없음, `matched is True`
4. **가드 blocked 탐지** `docker exec airflow-airflow-worker-1 python -c "
from modules.transform.utility.dag_schedule_guard import OverdueSchedule
o=OverdueSchedule(dag_id='X',next_dagrun=None,next_dagrun_create_after=None,lag_minutes=5)
print(o.blocked)"` → 기대: `False` (기본값 존재 확인)
5. **가드 DAG import** `docker exec airflow-airflow-worker-1 python -c "from dags.strategy.Strategy_ScheduleGuard_01_Overdue_Dags import dag; print(dag.dag_id)"` → 기대: `Strategy_ScheduleGuard_01_Overdue_Dags`
6. **배민 DAG import** `docker exec airflow-airflow-worker-1 python -c "from dags.db.DB_Beamin_Macro_Dags import dag; print(len(dag.tasks))"` → 기대: ImportError 없음
7. **실제 봉쇄 상태 탐지 (통합)** 현재 `DB_Beamin_Macro_Dags`는 `next_dagrun_create_after IS NULL` 상태다.
   `docker exec airflow-airflow-worker-1 airflow dags test Strategy_ScheduleGuard_01_Overdue_Dags 2026-08-20` →
   기대: 로그에 `DB_Beamin_Macro_Dags` + `봉쇄` 포함, `trigger_dag` 호출 없음
8. **날짜 필터 (실측 필요)** 로그인 세션에서 임의 과거 날짜 1매장 수집 →
   기대: `날짜 필터 설정 완료: 2026-08-18` 출력, `달력 목표 월 이동 실패` 0건
9. **회귀 확인** 재수집 로그에서 `검증 불일치로 정상 주문 저장 생략` 0건, `저장 완료(정상)` 다수

## Verification Loop

구현 완료 후 아래 루프를 **모든 Test Cases PASS까지** 반복한다.

```
LOOP until all PASS:
  1. Test Cases 순서대로 실행
  2. FAIL 항목 → 원인 분석
  3. 코드 수정
  4. 전체 Test Cases 재실행
종료 조건: 전체 PASS + Constraints 위반 없음
```

Test Case 8은 실제 배민 로그인 세션이 필요하므로 자동 루프에서 제외하고, 1~7과 9만 루프에 넣는다.

## Constraints

- **Test Case 8(날짜 필터)은 DOM 실측 없이 셀렉터를 추측해 확정하지 말 것.**
  Step 2의 코드는 다단 폴백이라 클래스 변경에 강하지만, 두 달치 그리드 구조상
  실측 없이는 시작일 클릭이 올바른 월을 짚는지 보장할 수 없다.
  실측이 불가능하면 Step 2-1, 2-2까지만 적용하고 2-3은 TODO로 남긴 뒤 명시적으로 보고할 것.
- `_TABLE_ROW_CSS = "tr.Table_b_r4ax_1dwbr4on[data-index]"` 는 현재 정상 동작 중이다. 건드리지 말 것.
- `_has_low_settle_rate()` 자체는 그대로 둔다. 재시도 트리거·알림 플래그로 계속 쓰인다.
- `dags/db/DB_Beamin_Macro_Dags.py:938`의 `settle_suspects` 집계와 알림 본문
  "정산정보 수집 의심" 섹션은 이미 `settlement_suspect`를 소비한다. 신규 배선 금지.
- `dags/db/DB_Beamin_Macro_Dags.py:302`의 `on_failure_callback_no_telegram`은
  의도적으로 텔레그램을 끈 설정이다. 임의로 바꾸지 말 것.
- 워킹트리에 미커밋 변경이 많다. `git checkout`/`git stash`/`git reset` 등 되돌리는 명령 절대 금지.
- Airflow DagRun 삭제·취소는 이 태스크 범위 밖이다. 운영자가 별도 수행한다.
- `dags/db/DB_Beamin_Macro_Pc2_Dags.py` 삭제 등 다른 미커밋 변경은 건드리지 말 것.

## Do Not Ask — Decide Yourself

- 파일이 이미 존재하면: 덮어쓰기
- import가 모호하면: Reference Code의 패턴 따라가기
- 타입 힌트 여부: 기존 파일과 동일하게
- 주석 여부: 최소화 (WHY만, WHAT 설명 금지)
- 변수명·함수명 스타일: snake_case, 기존 파일과 동일하게
