# 배민 매크로 수집 실패 + 재시도 무력화 수정

## Task

2026-07-09 대상 `DB_Beamin_Macro_Dags` run에서 12계정 중 4계정만 수집 성공했고, 이어진 `retry_failed`가 85분을 쓰고도 실패 8계정 중 1개만 복구했다. 원인은 (1) 죽은 Chrome 프로세스가 고아로 남아 뒤쪽 계정에서 `Connection refused`가 폭증하는 좀비 누적, (2) 재시도 경로가 `stability_profile`을 전달받지 못해 본수집(`safe_memory`)보다 약한 기본 프로필(`safe`)로 도는 구조적 결함이다. 여기에 (3) DAG 파일의 중복 함수 정의로 검증 로그/메일 발송이 조용히 유실되고 있다.

## Project Conventions

- logging: `logger = logging.getLogger(__name__)` — print 절대 금지
- import 경로: `from modules.transform.utility.XXX import ...` (절대경로)
- 파이프라인 파일 위치: `modules/transform/pipelines/{domain}/`
- DAG 파일 위치: `dags/{domain}/`
- 스케줄 상수: `from modules.transform.utility.schedule import ...` (하드코딩 금지)
- 경로 상수: `from modules.transform.utility.paths import ...` (하드코딩 금지)
- DAG에는 오케스트레이션만, 비즈니스 로직은 `modules/transform/pipelines/`

## Files to Create / Modify

**Create**: 없음

**Modify**:
- `modules/transform/pipelines/db/DB_Beamin_combined.py` — 좀비 Chrome 강제 종료, `retry_once_failed`에 `stability_profile` 파라미터 추가, 세션 재생성 카운터 off-by-one 로그 수정
- `modules/transform/pipelines/db/DB_Beamin_retry.py` — `build_retry_conf` / `build_next_retry_conf` conf에 `stability_profile` 전파
- `dags/db/DB_Beamin_Macro_Dags.py` — `retry_failed`에서 프로필 결정·전달, 중복 함수 정의 제거 + `_save_validate_log`/`_send_alert` 복구

**범위 밖 (건드리지 말 것)**:
- `DB_Beamin_combined.py:245`, `:500` 의 죽은 `collect_now_and_woori` 정의 2개 — 그대로 둔다. 살아있는 `:999` 정의만 수정한다.

---

## 근거 (로그 ↔ 코드 대조)

원인 2의 직접 증거. 같은 계정 `muchunby`에 대해 두 task의 세션 재생성 카운터 분모가 다르다:

```
collect_all   : 대시보드 매장목록 로드 실패, 세션 재생성 시도: muchunby (1/4)   → max_session_recovery=4 = safe_memory
retry_failed  : 대시보드 매장목록 로드 실패, 세션 재생성 시도: muchunby (3/2)   → max_session_recovery=2 = safe
retry_failed  : 로그인 중 드라이버 충돌 (1/2): muchunby                          → login_attempts=2       = safe
```

`retry_once_failed()`에 `stability_profile` 파라미터가 없어 내부 `collect_now_and_woori(...)` 호출이 `None`을 넘기고, `resolve_stability_profile`이 `DEFAULT_PROFILE="safe"`로 폴백한다.

---

## Implementation Steps

### 1. 좀비 Chrome 강제 종료 (`DB_Beamin_combined.py`)

`croling_beamin.quit_driver_safely` 만이 PID 트리를 강제 종료한다. 형제 모듈 `DB_Beamin_03_shop_change.py`는 이미 이걸 import해서 쓴다. **새 함수를 만들지 말고 그대로 재사용한다.**

1-1. `from modules.extract.croling_beamin import (...)` 블록에 `quit_driver_safely` 를 알파벳 순 위치(`logout_baemin` 다음)에 추가.

1-2. `_close_driver_safely` (현재 `:165`) — 맨 `driver.quit()` 제거:

```python
def _close_driver_safely(driver: Any, account_id: str) -> None:
    try:
        if driver:
            logout_baemin(driver, account_id)
    except Exception:
        pass
    quit_driver_safely(driver, account_id)
```

1-3. `_quit_driver_safely` (현재 `:178`) — 시그니처는 유지(호출부 `:1419`, `:1453`, `:1501`), 본문만 위임:

```python
def _quit_driver_safely(driver: Any) -> None:
    quit_driver_safely(driver)
```

1-4. `_build_account_session` (현재 `:132-135`)의 로그인 실패 후 맨 `driver.quit()` try/except 블록을 `quit_driver_safely(driver, account_id)` 한 줄로 교체.

`quit_driver_safely`는 `driver=None`이어도 좀비 회수를 수행하고, 해당 드라이버의 `browser_pid` / `service.process.pid` 만 정확히 죽이므로 동시 실행 세션에 영향이 없다.

### 2. `retry_once_failed`에 안정성 프로필 전파 (`DB_Beamin_combined.py`)

2-1. 시그니처 변경 (현재 `:767`):

```python
def retry_once_failed(
    failed: dict,
    target_date: str | None = None,
    stability_profile: str | None = None,
) -> dict:
```

2-2. 내부 계정 레벨 재수집 호출 (현재 `:790`):

```python
    # 1. 계정 레벨 실패 → 해당 계정 전체 재수집
    if failed.get("accounts"):
        result = collect_now_and_woori(
            failed["accounts"],
            target_date=target_date,
            stability_profile=stability_profile,
        )
        if isinstance(result, dict):
            extend_residual(result.get("failed"))
```

2-3. 함수 진입부 `logger.info("재시도 시작: ...")` 에 `profile=%s` 를 추가해 실제 적용 프로필을 로그로 남긴다.

### 3. 세션 재생성 카운터 off-by-one 로그 수정 (`DB_Beamin_combined.py`)

`_ensure_dashboard_store_select` (현재 `:225-234`)는 `range(max_recovery + 1)` 를 돌면서 분모를 `max_recovery` 로 찍어 `(3/2)` 같은 로그를 낸다. **분모만** `max_recovery + 1` 로 고친다. 루프 동작은 바꾸지 않는다.

```python
        logger.warning(
            "대시보드 매장목록 로드 실패, 세션 재생성 시도: %s (%d/%d)",
            account_id,
            attempt + 1,
            max_recovery + 1,
        )
```

### 4. Retry DAG conf에 프로필 전파 (`DB_Beamin_retry.py`)

4-1. `build_retry_conf` (현재 `:79-118`) 에 `stability_profile: str | None = None` 파라미터를 추가하고, 반환 dict에 `"stability_profile": stability_profile` 키를 넣는다.

4-2. `build_next_retry_conf` (현재 `:206-276`) 반환 dict에도 동일 키를 넣되 값은 `previous_conf.get("stability_profile")` 로 승계한다.

`retry_collect_from_conf` (`:430`)는 이미 `stability_profile=conf.get("stability_profile")` 로 읽고 있으므로 추가 수정 불필요.

### 5. DAG `retry_failed`에서 프로필 결정·전달 (`dags/db/DB_Beamin_Macro_Dags.py`)

`collect_all` (`:982`)과 동일한 방식으로 프로필을 결정한다:

```python
def retry_failed(**context) -> str:
    dag_run = context.get("dag_run")
    conf = (getattr(dag_run, "conf", None) or {}) if dag_run else {}
    target_date = conf.get("target_date")
    profile = resolve_stability_profile(conf.get("stability_profile") or "safe_memory")
    logger.info("재시도 안정성 프로필: %s", profile["name"])
    ...
    result = pipeline_retry_failed(
        immediate_failed,
        target_date=target_date,
        stability_profile=profile["name"],
    )
```

`trigger_retry_if_needed` (`:1187`)의 `build_retry_conf(...)` 호출에도 `stability_profile=conf.get("stability_profile") or "safe_memory"` 를 전달한다.

### 6. DAG 중복 함수 정의 정리 (`dags/db/DB_Beamin_Macro_Dags.py`)

같은 함수가 두 번 정의돼 있고 Python은 **뒤쪽 정의만 남긴다**. `_save_validate_log` 호출은 전부 앞쪽 죽은 정의에만 있어서 검증 로그 md가 기록되지 않고 orders 불일치 메일도 발송되지 않는다.

| 함수 | 죽은 정의(삭제) | 살아있는 정의(보강) | 복구할 동작 |
|---|---|---|---|
| `_send_alert` | `:115` (2인자) | `:1015` (3인자) | — |
| `validate_orders` | `:380` | `:1029` | `_save_validate_log`, 불일치 시 `_send_alert` |
| `validate_ad_funnel` | `:424` | `:1057` | `_save_validate_log` |
| `validate_toorder` | `:458` | `:1090` | `_save_validate_log` |

**순서를 바꾸지 말고, 앞쪽 정의를 지우면서 그 안에만 있던 동작을 뒤쪽 정의로 병합한다.**

6-1. `_send_alert`(`:115`) 삭제. `_on_failure_callback`은 2인자로 호출하지만 `:1015` 버전의 `html_content`가 기본값 `None`이라 그대로 동작한다.

6-2. `validate_orders`(`:380`) 삭제. 살아있는 `:1029` 버전에 `target_date`를 conf에서 읽는 코드를 넣고, 말미에 아래를 추가:

```python
    if mismatches:
        _send_alert(subject=f"[배민 orders 불일치] {len(mismatches)}건", body=summary)
        send_telegram(summary)
    _save_validate_log(target_date, "orders 검증", summary)
    return summary
```

죽은 정의 `:405-413` 의 `missing_brand_stores` 참조 3줄은 **해당 함수에 그 변수가 정의된 적이 없다(NameError 지뢰)**. 복원하지 말고 폐기한다.

6-3. `validate_ad_funnel`(`:424`) 삭제. `:1057` 버전 말미에 `_save_validate_log(target_date, "ad_funnel 빈값 점검", summary)` 추가.

6-4. `validate_toorder`(`:458`) 삭제. `:1090` 버전 말미에 `_save_validate_log(target_date, "ToOrder 교차검증", summary)` 추가. `:1090`은 이미 `missing_brand_stores`를 `:1119`에서 올바르게 정의해 쓰고 있으므로 그대로 둔다.

6-5. 죽은 함수 삭제: `_build_collection_notification_legacy_v1`(`:679`), `_build_collection_notification_legacy_v4`(`:1225`), `notify_collection_result_legacy_v4`(`:1379`).

6-6. 미사용 상수 `ORDERS_BACKFILL_DAYS` 제거.

---

## Reference Code

### modules/transform/pipelines/db/DB_Beamin_03_shop_change.py (import 패턴 — 이걸 따라간다)

```python
from selenium.common.exceptions import WebDriverException
from modules.extract.croling_beamin import (
    TIMING,
    clean_chrome_profile,
    human_click,
    is_driver_crash_error,
    launch_browser,
    login_baemin,
    logout_baemin,
    quit_driver_safely,
    wait_for_page,
)
from modules.transform.pipelines.db.beamin_stability import resolve_stability_profile
```

### modules/extract/croling_beamin.py — `quit_driver_safely` (재사용 대상, 수정 금지)

```python
def quit_driver_safely(driver, account_id: str = "SYSTEM") -> None:
    """driver.quit() + 해당 드라이버의 chrome/chromedriver 프로세스 트리 강제 종료 + 좀비 회수.

    driver의 browser_pid(chrome)와 service.process.pid(chromedriver)만 정확히 죽이므로
    다른 계정의 동시 실행 세션에 영향이 없다. driver가 None이어도 좀비 회수는 수행.
    """
    if driver is None:
        _reap_zombie_children()
        return

    browser_pid = getattr(driver, "browser_pid", None)
    service = getattr(driver, "service", None)
    service_pid = getattr(getattr(service, "process", None), "pid", None)

    try:
        driver.quit()
    except Exception:
        pass

    _kill_pid_tree(browser_pid)
    _kill_pid_tree(service_pid)

    n = _reap_zombie_children()
    if n:
        log(f"좀비 프로세스 {n}개 회수", account_id)
```

### modules/transform/pipelines/db/beamin_stability.py — 프로필 값 (수정 금지)

```python
DEFAULT_PROFILE = "safe"

STABILITY_PROFILES: dict[str, dict[str, Any]] = {
    "safe": {
        "login_attempts": 2,
        "max_session_recovery_per_account": 2,
        ...
    },
    "safe_memory": {
        "driver_restart_every_stores": 1,  # 매장마다 신선한 세션
        "login_attempts": 3,
        "max_session_recovery_per_account": 4,  # 렌더러 타임아웃 복구 여유
        ...
    },
}


def resolve_stability_profile(name: str | None) -> dict[str, Any]:
    profile_name = (name or DEFAULT_PROFILE).strip() or DEFAULT_PROFILE
    profile = deepcopy(STABILITY_PROFILES.get(profile_name, STABILITY_PROFILES[DEFAULT_PROFILE]))
    profile["name"] = profile_name if profile_name in STABILITY_PROFILES else DEFAULT_PROFILE
    return profile
```

### modules/transform/pipelines/db/DB_Beamin_combined.py — 살아있는 `collect_now_and_woori` 시그니처 (`:999`)

```python
def collect_now_and_woori(
    account_list: list[dict],
    target_date: str | None = None,
    stability_profile: str | None = None,
) -> dict:
    profile = resolve_stability_profile(stability_profile)
    metrics = _new_runtime_metrics(profile["name"], account_list)
```

---

## Test Cases

1. **구문 파싱** — `python -c "import ast;[ast.parse(open(p,encoding='utf-8').read()) for p in ['dags/db/DB_Beamin_Macro_Dags.py','modules/transform/pipelines/db/DB_Beamin_combined.py','modules/transform/pipelines/db/DB_Beamin_retry.py']]"` → 기대: 출력 없음, exit 0

2. **중복 정의 제거** — `grep -c "^def validate_orders" dags/db/DB_Beamin_Macro_Dags.py` → 기대: `1`
   `validate_ad_funnel`, `validate_toorder`, `_send_alert` 도 각각 `1`

3. **죽은 함수 제거** — `grep -c "legacy_v1\|legacy_v4" dags/db/DB_Beamin_Macro_Dags.py` → 기대: `0`

4. **검증 로그 복구** — `grep -c "_save_validate_log(" dags/db/DB_Beamin_Macro_Dags.py` → 기대: `4` (정의 1 + 호출 3)

5. **`quit_driver_safely` 도입** — `grep -c "driver.quit()" modules/transform/pipelines/db/DB_Beamin_combined.py` → 기대: `0`
   `grep -c "quit_driver_safely" modules/transform/pipelines/db/DB_Beamin_combined.py` → 기대: `5` 이상 (import 1 + 정의 1 + 호출 3)

6. **`collect_now_and_woori` 죽은 정의 보존 확인** — `grep -c "^def collect_now_and_woori" modules/transform/pipelines/db/DB_Beamin_combined.py` → 기대: `3` (범위 밖, 줄이면 안 됨)

7. **프로필 전파 (정적)** — `python -c "import inspect,modules.transform.pipelines.db.DB_Beamin_combined as m; assert 'stability_profile' in inspect.signature(m.retry_once_failed).parameters"` → 기대: AssertionError 없음

8. **Retry conf 키** — `python -c "from modules.transform.pipelines.db.DB_Beamin_retry import build_retry_conf; c=build_retry_conf(failed={'accounts':[],'stores':[],'orders':[],'ads':[]}, target_date='2026-07-09', stability_profile='safe_memory'); assert c['stability_profile']=='safe_memory'"` → 기대: AssertionError 없음

9. **DAG import** — `python -c "from dags.db.DB_Beamin_Macro_Dags import dag"` → 기대: ImportError / NameError 없음

10. **DAG 파싱 (컨테이너)** — `docker compose exec airflow-scheduler airflow dags list-import-errors` → 기대: `DB_Beamin_Macro_Dags` / `DB_Beamin_Macro_Dags_Retry` 미포함

11. **런타임 프로필 전파 (핵심 증거)** — 단일 계정으로 재시도 경로를 태운다:
    ```
    airflow dags trigger DB_Beamin_Macro_Dags -c '{"stores":["도리당 법흥리점"],"target_date":"2026-07-09"}'
    ```
    → 기대: `retry_failed` task 로그에 `재시도 안정성 프로필: safe_memory` 가 찍히고, 세션 재생성 카운터가 `(n/5)` 로 표시된다 (safe_memory `max_recovery=4` → 분모 `4+1=5`). **수정 전에는 `(n/2)` 였다.**

12. **좀비 프로세스 회수** — run 종료 후:
    ```
    docker compose exec airflow-scheduler bash -c "ps -eo pid,stat,comm | grep -c chrome"
    ```
    → 기대: `0` 에 수렴. `좀비 프로세스 N개 회수` 로그가 남을 수 있음(정상).

13. **검증 md 생성** — run 후 `ANALYTICS_DB/baemin_validate_log/validate_20260709.md` 가 존재하고 `orders 검증` / `ad_funnel 빈값 점검` / `ToOrder 교차검증` 3개 섹션이 append 되어 있어야 한다.

## Verification Loop

구현 완료 후 아래 루프를 **모든 Test Cases PASS까지** 반복한다.

```
LOOP until all PASS:
  1. Test Cases 1~9 (정적) 순서대로 실행
  2. FAIL 항목 → 원인 분석
  3. 코드 수정
  4. 전체 재실행
  5. 정적 전부 PASS → 10~13 (런타임) 실행
종료 조건: 전체 PASS + Constraints 위반 없음
```

Test Case 11이 `(n/2)` 로 나오면 원인 2가 안 고쳐진 것이다 — `retry_failed` → `pipeline_retry_failed` → `collect_now_and_woori` 전 구간의 `stability_profile` 인자 전달을 다시 추적하라.

## Constraints

- `DB_Beamin_combined.py:245`, `:500` 의 죽은 `collect_now_and_woori` 정의 2개는 **삭제하지 않는다**. 살아있는 `:999` 정의만 수정한다. (Test Case 6이 이를 검사)
- `croling_beamin.quit_driver_safely` / `beamin_stability.resolve_stability_profile` 은 **수정하지 않는다**. 재사용만 한다.
- `_quit_driver_safely(driver)` 의 1인자 시그니처를 유지하라. 호출부가 `:1419`, `:1453`, `:1501` 세 곳이다.
- `_ensure_dashboard_store_select` 의 **루프 범위 `range(max_recovery + 1)` 를 바꾸지 마라.** 로그 분모만 고친다.
- 중복 함수 정리 시 **함수 정의 순서를 재배치하지 마라.** 앞쪽을 지우고 뒤쪽에 병합한다.
- `_on_failure_callback` 은 `_send_alert(subject=..., body=...)` 2인자로 호출한다. `html_content` 기본값 `None` 유지 필수.
- `selenium_pool` 은 레포 어디에도 생성 코드가 없다(메타DB 수동 생성 추정). 이번 작업에서 건드리지 마라.

## Do Not Ask — Decide Yourself

- 파일이 이미 존재하면: 덮어쓰기
- import가 모호하면: Reference Code의 `DB_Beamin_03_shop_change.py` 패턴 따라가기
- 타입 힌트 여부: 기존 파일과 동일하게 (`str | None = None`)
- 주석 여부: 최소화 (WHY만, WHAT 설명 금지)
- 변수명·함수명 스타일: snake_case, 기존 파일과 동일하게
