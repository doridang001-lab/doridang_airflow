# 육계시세이메일버그2종수정

## Task
이메일에 이동평균이 `nan`으로 표시되고, chicken.or.kr 카드에 전월/전년 가격이 없는 두 가지 버그를 수정한다. 수집 실패 rows가 CSV에 남아 rolling mean을 오염시키고, chicken.or.kr 사이트는 전월/전년을 제공하지 않아 CSV 누적 데이터 기반 lookup이 필요하다.

## Project Conventions
- logging: `logger = logging.getLogger(__name__)` — print 절대 금지
- import 경로: `from modules.transform.utility.XXX import ...` (절대경로)
- 파이프라인 파일 위치: `modules/transform/pipelines/strategy/`
- 경로 상수: `from modules.transform.utility.paths import CHICKEN_PRICE_CSV_PATH`

## Files to Create / Modify
- **수정**: `modules/transform/pipelines/strategy/SMP_chicken_price_collect.py`
  - `send_notification` 함수만 변경 (다른 함수 건드리지 않음)

## Implementation Steps

### 1. 이동평균 NaN 수정 (send_notification 내부, line ~764)

현재 문제 코드:
```python
site_df = df_csv[df_csv["source_site"] == site].sort_values("base_date")
ma20 = site_df["price_today"].rolling(20).mean().iloc[-1] if len(site_df) >= 20 else None
```

**Fix**: `dropna` + `drop_duplicates` 추가 후 같은 방식 유지:
```python
for site in df_csv["source_site"].unique():
    site_df = (
        df_csv[df_csv["source_site"] == site]
        .dropna(subset=["price_today"])
        .drop_duplicates(subset=["base_date"], keep="last")
        .sort_values("base_date")
    )
    ma20  = site_df["price_today"].rolling(20).mean().iloc[-1]  if len(site_df) >= 20  else None
    ma60  = site_df["price_today"].rolling(60).mean().iloc[-1]  if len(site_df) >= 60  else None
    ma120 = site_df["price_today"].rolling(120).mean().iloc[-1] if len(site_df) >= 120 else None
    ma_info[site] = {"ma20": ma20, "ma60": ma60, "ma120": ma120}
```

### 2. chicken.or.kr 전월/전년 CSV lookup (send_notification 내부)

**위치**: 이동평균 `ma_info` 구성 블록 직후, `_build_email_html` 호출 직전에 삽입.

`df_csv`는 이미 위 블록에서 `pd.read_csv(CHICKEN_PRICE_CSV_PATH)`로 로드되어 있음 — 재사용.

```python
# chicken.or.kr 전월/전년: CSV 누적 데이터 lookup
if CHICKEN_PRICE_CSV_PATH.exists():
    ch_csv = (
        df_csv[df_csv["source_site"] == "chicken.or.kr"]
        .dropna(subset=["price_today"])
        .drop_duplicates(subset=["base_date"], keep="last")
        .copy()
    )
    ch_csv["base_date"] = pd.to_datetime(ch_csv["base_date"], errors="coerce")
    ch_csv = ch_csv.dropna(subset=["base_date"])

    def _nearest_price(target_date, days_tol):
        if ch_csv.empty:
            return None
        diff = (ch_csv["base_date"] - target_date).abs()
        mask = diff <= pd.Timedelta(days=days_tol)
        if not mask.any():
            return None
        idx = diff[mask].idxmin()
        return int(ch_csv.loc[idx, "price_today"])

    for r in results:
        if r.get("source_site") == "chicken.or.kr" and r.get("success"):
            base = pd.to_datetime(r.get("base_date"), errors="coerce")
            if pd.isna(base):
                continue
            r["price_prev_month"] = _nearest_price(base - pd.DateOffset(months=1), 7)
            r["price_prev_year"]  = _nearest_price(base - pd.DateOffset(years=1), 14)
```

## Reference Code

### send_notification (기존 코드, lines 744–799)
```python
def send_notification(**context) -> None:
    ti = context["ti"]
    json_str = ti.xcom_pull(task_ids="merge_results")
    if json_str:
        results = pd.read_json(StringIO(json_str), orient="records").to_dict(orient="records")
    else:
        results = []

    # 이동평균 계산
    ma_info: dict = {}
    if CHICKEN_PRICE_CSV_PATH.exists():
        df_csv = pd.read_csv(CHICKEN_PRICE_CSV_PATH, encoding="utf-8-sig")
        df_csv["price_today"] = pd.to_numeric(df_csv["price_today"], errors="coerce")
        df_csv["base_date"] = pd.to_datetime(df_csv["base_date"], errors="coerce")

        for site in df_csv["source_site"].unique():
            site_df = df_csv[df_csv["source_site"] == site].sort_values("base_date")
            ma20  = site_df["price_today"].rolling(20).mean().iloc[-1]  if len(site_df) >= 20  else None
            ma60  = site_df["price_today"].rolling(60).mean().iloc[-1]  if len(site_df) >= 60  else None
            ma120 = site_df["price_today"].rolling(120).mean().iloc[-1] if len(site_df) >= 120 else None
            ma_info[site] = {"ma20": ma20, "ma60": ma60, "ma120": ma120}

    base_date = next(
        (r.get("base_date") for r in results if r.get("success")),
        datetime.now().strftime("%Y-%m-%d"),
    )
    any_failure = (not results) or any(not r.get("success", False) for r in results)

    html_content = _build_email_html(results, ma_info, base_date, any_failure)
    ...
```

## Test Cases
1. **이동평균 NaN 해소** — CSV에 `price_today=NaN` rows가 있어도 이동평균이 숫자로 출력되는지
   ```python
   python -c "
   import pandas as pd
   from io import StringIO
   csv = '''source_site,base_date,price_today
   poultry.or.kr,2026-01-01,
   poultry.or.kr,2026-01-02,1800
   '''
   df = pd.read_csv(StringIO(csv))
   df['price_today'] = pd.to_numeric(df['price_today'], errors='coerce')
   site_df = df.dropna(subset=['price_today']).drop_duplicates(subset=['base_date'], keep='last').sort_values('base_date')
   print(site_df['price_today'].rolling(1).mean().iloc[-1])  # 기대: 1800.0
   "
   ```
2. **DAG import 오류 없음**
   ```bash
   python -c "from modules.transform.pipelines.strategy.SMP_chicken_price_collect import send_notification; print('OK')"
   ```
3. **chicken.or.kr 전월/전년 lookup** — CSV에 30일 전 데이터가 있으면 `price_prev_month` 값이 채워지는지 실제 DAG 실행 또는 `send_notification` 수동 호출 후 이메일 확인

## Verification Loop
```
LOOP until all PASS:
  1. Test Cases 순서대로 실행
  2. FAIL 항목 → 원인 분석
  3. 코드 수정
  4. 전체 Test Cases 재실행
종료 조건: 전체 PASS + Constraints 위반 없음
```

## Constraints
- `send_notification` 함수 시그니처 변경 금지 (`**context` 유지)
- `_build_email_html` 함수 변경 금지 — `results` dict에 `price_prev_month`/`price_prev_year` 직접 주입하는 방식 사용
- CSV 없거나 데이터 부족 시 `None` 유지 (조용히 생략) — 예외 발생 금지
- `df_csv` 변수는 이미 이동평균 블록에서 로드됨 — 재로드하지 말고 재사용
- `_nearest_price` 함수는 `send_notification` 내부 로컬 함수로 정의 (모듈 레벨 금지)

## Do Not Ask — Decide Yourself
- 파일이 이미 존재하면: 덮어쓰기
- import가 모호하면: Reference Code의 패턴 따라가기
- 타입 힌트 여부: 기존 파일과 동일하게
- 주석 여부: 최소화 (WHY만, WHAT 설명 금지)
- 변수명·함수명 스타일: snake_case, 기존 파일과 동일하게
