# 배민매크로DAG보완(최근3일로그기반)

## Task
최근 3일(2026-07-19~21) `DB_Beamin_Macro_Dags` 로그에서 세션 끊김/렌더러 타임아웃이 반복 발생 후 자체 복구되는 패턴이 확인됐다. 두 가지를 개선한다: (1) 메인 스케줄 DAG가 쓰는 `safe_daily` 안정성 프로필을 튜닝해 렌더러 타임아웃/세션 끊김 재발 빈도를 낮춘다. (2) PC2 DAG의 상위/하위 매장 분할 우선순위 로직에 unified 테스트매장(교차검증용)을 먼저 수집하도록 반영한다.

## Project Conventions
- logging: `logger = logging.getLogger(__name__)` — print 절대 금지
- import 경로: `from modules.transform.utility.XXX import ...` (절대경로)
- 파이프라인 파일 위치: `modules/transform/pipelines/{domain}/`
- DAG 파일 위치: `dags/{domain}/`
- 스케줄 상수: `from modules.transform.utility.schedule import ...` (하드코딩 금지)
- 경로 상수: `from modules.transform.utility.paths import ...` (하드코딩 금지)
- DAG는 orchestration만, 비즈니스 로직은 `modules/transform/pipelines/`

## Files to Create / Modify
- `modules/transform/pipelines/db/beamin_stability.py` (수정 — `safe_daily` 프로필 값 조정)
- `dags/db/DB_Beamin_Macro_Pc2_Dags.py` (수정 — `_split_accounts_by_range` 우선순위 로직에 테스트매장 반영)

## Implementation Steps

### Step 1 — `beamin_stability.py`의 `safe_daily` 프로필 튜닝
`STABILITY_PROFILES["safe_daily"]`를 다음과 같이 변경:
```python
"safe_daily": {
    "initial_stagger_range": (10.0, 40.0),
    "account_wait_range": (25.0, 60.0),
    "orders_validation_retry": 2,
    "max_parallel_accounts": 1,
    "driver_restart_every_stores": 2,        # 3 → 2 (렌더러 행 누적 완화)
    "login_attempts": 3,
    "login_retry_wait_range": (5.0, 20.0),   # (5,15) → (5,20)
    "max_session_recovery_per_account": 4,   # 3 → 4
},
```
다른 프로필(`safe`, `safe_memory`, `today_orders`, `bulk_70`)은 건드리지 않는다. `account_wait_range`, `max_parallel_accounts`, `initial_stagger_range`, `orders_validation_retry`, `login_attempts`는 그대로 유지 (실행시간 300분 예산 보호).

### Step 2 — PC2 DAG 우선순위 로직에 unified 테스트매장 반영
`dags/db/DB_Beamin_Macro_Pc2_Dags.py` 상단 import에 추가:
```python
from modules.transform.pipelines.db.DB_UnifiedSales_common import DELIVERY_MANUAL_TEST_STORES
```

`_split_accounts_by_range` 함수(현재 line 90~112)를 다음 동작으로 재작성한다:
1. `collect_range`가 `"상위"` 또는 `"하위"`인 경우: 기존 분할 로직(정렬 후 절반 슬라이스)은 그대로 유지하되, **분할된 결과 리스트 내에서** `store_name`이 `DELIVERY_MANUAL_TEST_STORES`에 속하는 계정을 리스트 맨 앞으로 재정렬한다 (분할 대상 자체는 바뀌지 않음, 순서만 테스트매장 우선).
2. `collect_range`가 그 외 값(`None` 포함, 인식 불가 값도 기존처럼 warning 후 처리)인 경우: 상/하위 분할 없이 전체 계정을 반환하되, 테스트매장 계정을 맨 앞에 배치하고 나머지는 기존 `store_name` 이름순 정렬을 유지한다.
3. 매칭은 `account.get("store_name")` 문자열이 `DELIVERY_MANUAL_TEST_STORES` 리스트에 있는지로 판단한다 (대소문자/공백 트리밍 없이 정확히 일치, 기존 리스트 값 형식 그대로).

`load_accounts` 함수(line 115~129)는 시그니처/호출부 변경 없음 — `_split_accounts_by_range` 내부 로직만 바뀐다.

## Reference Code

### modules/transform/pipelines/db/beamin_stability.py (현재 STABILITY_PROFILES)
```python
from __future__ import annotations

import json
from copy import deepcopy
from datetime import UTC, datetime
from pathlib import Path
from typing import Any

from modules.transform.utility.paths import DASHBOARD_DB

DEFAULT_PROFILE = "safe"

STABILITY_PROFILES: dict[str, dict[str, Any]] = {
    "safe_daily": {
        "initial_stagger_range": (10.0, 40.0),
        "account_wait_range": (25.0, 60.0),
        "orders_validation_retry": 2,
        "max_parallel_accounts": 1,
        "driver_restart_every_stores": 3,
        "login_attempts": 3,
        "login_retry_wait_range": (5.0, 15.0),
        "max_session_recovery_per_account": 3,
    },
    "safe_memory": {
        "driver_restart_every_stores": 1,  # 매장마다 신선한 세션 (렌더러 행 누적 차단)
        "max_session_recovery_per_account": 4,  # 렌더러 타임아웃 복구 여유
        # ... 나머지 키 동일 패턴
    },
}


def resolve_stability_profile(name: str | None) -> dict[str, Any]:
    profile_name = (name or DEFAULT_PROFILE).strip() or DEFAULT_PROFILE
    profile = deepcopy(STABILITY_PROFILES.get(profile_name, STABILITY_PROFILES[DEFAULT_PROFILE]))
    profile["name"] = profile_name if profile_name in STABILITY_PROFILES else DEFAULT_PROFILE
    return profile
```

### dags/db/DB_Beamin_Macro_Pc2_Dags.py (현재 관련 함수)
```python
COLLECT_RANGE: str | None = "하위"

def _split_accounts_by_range(accounts: list[dict], collect_range: str | None) -> list[dict]:
    if not collect_range or not accounts:
        return accounts

    ordered = sorted(accounts, key=lambda a: str(a.get("store_name", "")))
    mid = len(ordered) // 2
    if collect_range == "상위":
        selected = ordered[:mid]
    elif collect_range == "하위":
        selected = ordered[mid:]
    else:
        logger.warning("알 수 없는 COLLECT_RANGE=%r", collect_range)
        return accounts

    logger.info(
        "PC2 매장 분할 [%s]: 전체 %d개 -> %d개 (%s ~ %s)",
        collect_range, len(ordered), len(selected),
        selected[0]["store_name"] if selected else "-",
        selected[-1]["store_name"] if selected else "-",
    )
    return selected


def load_accounts(**context) -> str:
    dag_run = context.get("dag_run")
    conf = (getattr(dag_run, "conf", None) or {}) if dag_run else {}
    stores_override = conf.get("stores")
    target = stores_override if stores_override else TARGET_STORES
    accounts = pipeline_load_accounts(target_stores=target, exact=True)

    if not stores_override:
        collect_range = conf.get("collect_range", COLLECT_RANGE)
        accounts = _split_accounts_by_range(accounts, collect_range)

    context["ti"].xcom_push(key="account_list", value=accounts)
    stores = [a["store_name"] for a in accounts]
    logger.info("PC2 계정 로드 완료: %d개 -> %s", len(accounts), stores)
    return f"PC2 계정 {len(accounts)}개, {stores}"
```

### modules/transform/pipelines/db/DB_UnifiedSales_common.py (테스트매장 목록)
```python
# 테스트매장
_BASE_DELIVERY_MANUAL_TEST_STORES = [
    "해운대중동점", "법흥리점", "송파삼전점", "동탄영천점",
    # ... (총 12개, 파일 참조)
    "구로디지털점", "부천옥길점",
]

ADD_TEST_STORES = ["삼송점"]
EXCLUDE_TEST_STORES = []

_EXCLUDED_TEST_STORE_SET = {str(store).strip() for store in EXCLUDE_TEST_STORES if str(store).strip()}

DELIVERY_MANUAL_TEST_STORES = [
    store
    for store in dict.fromkeys(_BASE_DELIVERY_MANUAL_TEST_STORES + ADD_TEST_STORES)
    if str(store).strip() and str(store).strip() not in _EXCLUDED_TEST_STORE_SET
]
```

## Test Cases
1. [프로필 값 확인] `python -c "from modules.transform.pipelines.db.beamin_stability import resolve_stability_profile as r; p=r('safe_daily'); assert p['driver_restart_every_stores']==2 and p['max_session_recovery_per_account']==4 and p['login_retry_wait_range']==(5.0,20.0); print('OK', p)"` → 기대: `OK {...}` 출력, AssertionError 없음
2. [다른 프로필 무변경] `python -c "from modules.transform.pipelines.db.beamin_stability import STABILITY_PROFILES as S; assert S['safe']['driver_restart_every_stores']==4 or True; print('safe_memory', S['safe_memory']['driver_restart_every_stores'])"` → 기대: `safe_memory 1` (변경 없음 확인)
3. [PC2 분할 - 상위/하위 + 테스트매장 우선순위] 임시 스크립트로 mock 계정 리스트(테스트매장명 섞어서) 만들어 `_split_accounts_by_range(mock, "상위")`, `_split_accounts_by_range(mock, "하위")` 호출 → 기대: 반환 리스트의 앞부분에 테스트매장 계정이 위치, 분할된 개수(원래 로직의 mid 슬라이스 크기)는 변하지 않음
4. [PC2 분할 - None] `_split_accounts_by_range(mock, None)` → 기대: 전체 계정 반환, 테스트매장 계정이 리스트 맨 앞에 오고 나머지는 기존 이름순
5. [DAG 파싱] `python -m py_compile dags/db/DB_Beamin_Macro_Pc2_Dags.py` → 기대: 에러 없음 (Airflow 컨텍스트 없이 문법/재귀 import 확인)
6. [순환 import 확인] `python -c "import ast; ast.parse(open('dags/db/DB_Beamin_Macro_Pc2_Dags.py', encoding='utf-8').read())"` 및 실제 가능하면 `python -c "from dags.db.DB_Beamin_Macro_Pc2_Dags import dag"` → 기대: ImportError/circular import 없음

## Verification Loop
```
LOOP until all PASS:
  1. Test Cases 순서대로 실행
  2. FAIL 항목 → 원인 분석
  3. 코드 수정
  4. 전체 Test Cases 재실행
종료 조건: 전체 PASS + Constraints 위반 없음
```

## Constraints
- `beamin_stability.py`의 `safe`, `safe_memory`, `today_orders`, `bulk_70` 프로필은 절대 변경하지 않는다 (다른 DAG/모드가 참조 중).
- `_split_accounts_by_range`의 기존 시그니처 `(accounts: list[dict], collect_range: str | None) -> list[dict]`를 유지한다 (호출부 `load_accounts`에서 인자 변경 없이 호출).
- 상위/하위 분할의 **분할 크기(mid 슬라이스)** 자체는 바꾸지 않는다 — 오직 순서(정렬)만 테스트매장 우선으로 바꾼다.
- `DELIVERY_MANUAL_TEST_STORES` import로 인한 순환 import 여부를 반드시 확인한다 (`DB_UnifiedSales_common.py` → PC2 DAG 방향은 문제없어야 하나, 반대방향 import가 있는지 grep으로 확인 후 진행).
- 폴더 구조/파일명 변경 금지, 하드코딩 금지(기존 상수/모듈 재사용).

## Do Not Ask — Decide Yourself
- 파일이 이미 존재하면: 덮어쓰기
- import가 모호하면: Reference Code의 패턴 따라가기
- 타입 힌트 여부: 기존 파일과 동일하게 (`str | None` 스타일)
- 주석 여부: 최소화 (WHY만, WHAT 설명 금지)
- 변수명·함수명 스타일: snake_case, 기존 파일과 동일하게
