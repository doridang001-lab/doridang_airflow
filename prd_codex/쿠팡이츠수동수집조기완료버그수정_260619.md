# 쿠팡이츠 수동 수집 조기 완료 버그 수정

## Task

수동 수집(`opts.source === 'manual'`) 시 페이지가 로딩 중인데도 수집이 완료로 처리되는 버그를 수정한다.
검색 버튼 클릭 후 로딩 스피너가 뜨는 동안 이전 결과의 DOM 값이 남아있어 `_waitForOrdersDataReady`가 즉시 `true`를 반환하는 것이 핵심 원인이다.

## Files to Modify

- `C:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용\content\03_coupangeats.js`
  - `_waitForOrdersDataReady` (line 2352)
  - 수동 수집 경로 2곳 (line ~1911, ~1920)

## Implementation Steps

### Step 1 — `_waitForOrdersDataReady` 교체 (line 2352~2362)

기존 함수 전체를 아래로 교체한다:

```javascript
async _waitForOrdersDataReady(timeoutMs = 8000) {
  const start = Date.now();
  let stableCount = 0;
  let lastSeen = -1;
  while (Date.now() - start < timeoutMs) {
    const count = this._getExpectedOrderCount();
    const sales = this._getExpectedSalesTotal();
    if (count > 0 || sales > 0) {
      if (count === lastSeen) {
        stableCount++;
        if (stableCount >= 3) return true;   // 300ms×3 = 0.9s 안정 확인
      } else {
        stableCount = 1;
        lastSeen = count;
      }
      await new Promise(r => setTimeout(r, 300));
    } else {
      stableCount = 0;
      lastSeen = -1;
      await new Promise(r => setTimeout(r, 100));
    }
  }
  return false;
},
```

**WHY:** 로딩 중에는 이전 DOM값이 남아 `count > 0`이 즉시 충족됨. count가 3회 연속 동일한 값이어야 진짜 로드 완료로 판정.

---

### Step 2 — manual branch에 `_waitForStableState` 추가 (line ~1910)

**위치 1** (manual 분기, `opts.source === 'manual'`):

찾아야 할 코드:
```javascript
await this._waitForOrdersCountChange(prevCount, 8000);
dataReady = await this._waitForOrdersDataReady(8000);
```
→ 아래로 교체:
```javascript
await this._waitForOrdersCountChange(prevCount, 8000);
dataReady = await this._waitForOrdersDataReady(8000);
if (dataReady) await this._waitForStableState(5000);
```

**위치 2** (dateSet 분기, `if (dateSet)` 블록 안):

찾아야 할 코드:
```javascript
await this._waitForOrdersCountChange(prevCount, 8000);
dataReady = await this._waitForOrdersDataReady(8000);
```
→ 아래로 교체 (동일):
```javascript
await this._waitForOrdersCountChange(prevCount, 8000);
dataReady = await this._waitForOrdersDataReady(8000);
if (dataReady) await this._waitForStableState(5000);
```

두 위치 모두 `_waitForOrdersDataReady` 직후에 `if (dataReady) await this._waitForStableState(5000);` 한 줄만 추가하면 된다.

## Reference Code

### `_waitForStableState` (현재 구현 — 그대로 유지)
```javascript
async _waitForStableState(maxWait = 5000) {
  const startTime = Date.now();
  let lastCount = 0;
  let stableCount = 0;
  while (Date.now() - startTime < maxWait) {
    await this._closeErrorPopups();
    const currentItems = document.querySelectorAll('.order-search-result-content > li.col-12');
    const currentCount = currentItems.length;
    if (currentCount === lastCount && currentCount > 0) {
      stableCount++;
      if (stableCount >= 3) {
        Utils.updateProgressModal({ debug: `✅ 페이지 안정화 (주문 ${currentCount}개)` });
        return true;
      }
    } else {
      stableCount = 0;
      lastCount = currentCount;
    }
    await this._randomDelay(300, 500);
  }
  Utils.updateProgressModal({ debug: `⚠️ 안정화 타임아웃 (주문 ${lastCount}개)` });
  return false;
},
```

### 기존 `_waitForOrdersDataReady` (교체 대상)
```javascript
async _waitForOrdersDataReady(timeoutMs = 8000) {
  const start = Date.now();
  while (Date.now() - start < timeoutMs) {
    const count = this._getExpectedOrderCount();
    const sales = this._getExpectedSalesTotal();
    // 건수 또는 매출액이 읽히면 데이터 로드 완료
    if (count > 0 || sales > 0) return true;
    await new Promise(r => setTimeout(r, 100));
  }
  return false; // 타임아웃
},
```

## Test Cases

1. **[수동 수집 정상 완료]** `https://store.coupangeats.com/merchant/management/orders/1007558` 접속 → 확장 수동 수집 실행 → 로딩 스피너가 사라진 후 완료 메시지 출력되는지 확인
2. **[건수 일치]** 수집 완료 후 progress modal의 수집 건수 = 화면 표시 건수 일치 확인
3. **[타임아웃 회귀 없음]** 조회 결과가 0건인 매장에서도 무한 대기하지 않고 8초 내 리턴 확인

## Verification Loop

```
LOOP until all PASS:
  1. Test Cases 순서대로 실행
  2. FAIL 항목 → 원인 분석
  3. 코드 수정
  4. 전체 Test Cases 재실행
종료 조건: 전체 PASS + 조기 완료 버그 미재현
```

## Constraints

- `_waitForStableState` 함수 자체는 수정하지 않는다 (검증된 구현)
- 총 수정 줄 수는 최소화: `_waitForOrdersDataReady` 교체 + 1줄 추가 × 2곳
- 다른 `_waitForOrdersDataReady` 호출부(recovery 경로, multistore 경로)는 건드리지 않는다 — 해당 경로는 이미 `_waitForStableState`를 호출하고 있음

## Do Not Ask — Decide Yourself

- 파일이 이미 존재하면: 덮어쓰기
- `_waitForStableState` 호출 위치 헷갈리면: `_waitForOrdersDataReady` 바로 다음 줄에 추가
- 두 위치 중 한 곳만 적용 여부 고민 시: 둘 다 적용
- 변수명·코드 스타일: 기존 파일과 동일하게 (async/await, 화살표 없는 `function` 아닌 메서드 리터럴)
