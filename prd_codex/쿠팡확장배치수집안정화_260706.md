# 쿠팡 확장 배치 수집 안정화

## Task
쿠팡이츠 크롬 확장(MV3) 배치 수집이 (1) 콜드 세션 때문에 매장당 20분씩 스톨하고 수동 재클릭해야 성공하며, (2) `10057` 밴 매장을 계속 재시도·백필하는 문제를 고친다. 세 가지 수정: **10057 밴 즉시 스킵 / 수집 전 실제 UI 준비 대기(warm-load) / 세션 초기화 시 에셋 캐시 보존**.

## Project Conventions (이 프로젝트 = 크롬 확장, JS)
- **순수 브라우저 JS (ES2020)**, 빌드 스텝 없음. `node --check`로만 문법 검증.
- 로깅은 확장 자체 방식 사용: 러너 대시보드는 `log(msg, level)`(runner.js 내부 함수), 콘텐츠 스크립트 진행 표시는 `Utils.updateProgressModal({ debug: '...' })`. `console.log` 신규 추가 금지.
- 런너↔콘텐츠 통신은 `chrome.runtime.sendMessage` / `chrome.tabs.sendMessage`. 완료 신호는 반드시 `MULTISTORE_COMPLETE` payload로 top-frame(`window.top === window.self`)에서만 발신.
- 기존 함수/플래그 네이밍(camelCase, `_`prefix = 인스턴스 내부) 그대로 따른다.
- **파일 직접 수정 위치(사용자 승인):** `C:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용\`
  - `-DESKTOP-HG136JL` 붙은 파일은 OneDrive 동기화 충돌본 → **절대 건드리지 말 것**.

## Files to Create / Modify
- **수정** `content\03_coupangeats.js` — 10057 하드스로틀 터미널 처리, blocked payload 발신 (변경 1)
- **수정** `runner.js` — `waitForOrdersUiReady` 추가 + COLLECT 직전 호출 (변경 2), `clearSession`에서 cacheStorage 삭제 제거 (변경 3)

## Implementation Steps

### 변경 1 — 10057 밴 매장 즉시 스킵 (`content\03_coupangeats.js`)
현재 10057 감지는 55~210초 in-page 쿨다운만 유발하고 계속 진행 → 러너의 스킵 분기(`runner.js:849-854`)가 발동 못 함(死코드). 감지를 **터미널(밴) 조건**으로 승격한다.

1. 인스턴스 플래그 `this._hardThrottled = false` 추가. 초기화는 `collect()` 진입부(`03_coupangeats.js:11-13`, `this._collectSource = opts.source || 'manual';` 근처).
2. `_waitBeforeOrdersSearch()`(`115-141`) — 이 함수는 `_clickOrdersSearchButton()`(`2445`)이 매 조회 전에 호출하는 choke point. 여기서 `this._isOrdersThrottleVisible()`가 true면:
   - escalating 쿨다운(`_ordersThrottleDelayMs`, `104-113`)을 **돌지 말고** `this._hardThrottled = true` 세팅 후 **즉시 반환**. (10057은 요청 과다 밴 → 더 기다리면 악화. 사용자 명시 요구.)
   - 트리거는 기존 `_isOrdersThrottleVisible()` 시그니처(10056/10057/과도한 요청/ACCESS_DENIED, `85-94`)만 사용 — 일반 타임아웃과 구분.
3. 배치 multistore 루프 `_collectOrdersMultiStore`의 `for` 루프(`2235`)에서 각 반복 시작 시(또는 조회 직후) `if (this._hardThrottled) break;`. 루프 종료 후 남은 매장 storeResults 행에 `{ storeName, completed:false, blocked:true, status:'warn', note:'10057: 과도한 요청으로 인해 서비스 이용이 일시적으로 제한되었습니다.' }` 부여.
4. 최종 `MULTISTORE_COMPLETE` payload(`2349-2360`)에 `this._hardThrottled`면 `hardThrottle:true`, `blocked:true` 추가.
5. 단일매장 블록(`2101-2138`, `2140-2211`)에서도 동일하게 `_hardThrottled` 확인 후 blocked payload 발신.
6. **핵심:** 콘텐츠 스크립트가 스스로 멈추고 `MULTISTORE_COMPLETE`를 **적극 발신**해야 함. hang 시 러너는 10분 타임아웃으로 `{timeout:true}`만 받고 `hardThrottle`을 못 봄.

→ 러너의 기존 스킵 분기(`runner.js:849-854`, `isHardThrottlePayload`가 `payload.hardThrottle` 검사 `219-222`)가 발동 → `markAccountStoresBlocked`(계정 모든 매장 blocked, `258-269`) → CMG·메뉴 스킵 + `incompleteStores()`(`283-287`)에서 제외 → **자동 재시도·백필 모두 제외**.

### 변경 2 — 수집 전 실제 UI 준비 대기 (`runner.js`)
현재 orders 이동 후 `readyState==='complete'`만 확인(`waitForPageLoad` `511-525`)하고 600ms 뒤 바로 COLLECT(`838-843`) → SPA 미하이드레이션 상태의 빈 DOM에 대고 콘텐츠 스크립트가 내부 재시도 루프를 돌다 타임아웃.

1. 신규 함수 `waitForOrdersUiReady(tabId, ms = 60000)` 추가 (`waitForWujieApp` `612-635` 바로 옆에 같은 패턴으로):
   - `chrome.scripting.executeScript`로 orders 조회 UI 존재 폴링. 판정 셀렉터: 매장 드롭다운 `.dropdown-btn.highlight` **또는** 조회 버튼(현행 `_clickOrdersSearchButton`이 쓰는 셀렉터와 동일한 것). 1000ms 간격, deadline까지, `stopRequested`면 조기 반환.
   - 준비되면 `true`, 미준비면 `false` 반환.
2. 호출 지점(준비 안 돼도 로그만 남기고 **현행처럼 진행** — 안전 폴백):
   - 초기 orders 수집: `838-841`의 `waitForPageLoadWithRetry` 직후 `await sleep(600)` 자리.
   - 즉시 재시도 경로: `984-986`.
3. CMG는 이미 `waitForWujieApp`로 shadowRoot 준비 확인하므로 **손대지 않음**.

### 변경 3 — 세션 초기화 시 에셋 캐시 보존 (`runner.js`)
`clearSession()`(`637-646`)에서 `cacheStorage:true`를 **제거**. `cookies:true`·`localStorage:true`만 유지(계정 전환 인증 초기화용). 정적 에셋 캐시 유지 → React/WUJIE 콜드 마운트 가속 → `WUJIE-APP 미준비`·안정화 타임아웃 감소. 캐시는 인증정보 아님 → 계정 전환 안전.

## Reference Code

### runner.js — clearSession (637-646) [변경 3 대상]
```js
async function clearSession() {
  return new Promise(resolve => {
    try {
      chrome.browsingData.remove(
        {origins:['https://store.coupangeats.com','https://advertising.coupangeats.com']},
        {cookies:true, localStorage:true, cacheStorage:true},   // ← cacheStorage:true 제거
        () => resolve(true)
      );
    } catch(e) { log(`세션 초기화 오류(무시): ${e.message}`); resolve(false); }
  });
}
```

### runner.js — waitForWujieApp (612-635) [변경 2: 같은 패턴으로 waitForOrdersUiReady 신규 작성]
```js
async function waitForWujieApp(tabId, ms = 30000) {
  const deadline = Date.now() + ms;
  while (Date.now() < deadline) {
    if (stopRequested) return false;
    try {
      const res = await chrome.scripting.executeScript({
        target: { tabId },
        func: () => {
          const wa = document.querySelector('WUJIE-APP');
          if (!wa || !wa.shadowRoot) return false;
          return !!(wa.shadowRoot.querySelector('form.cmgeats-store-dropdown') &&
                    wa.shadowRoot.querySelector('.calendar-dropdown-input'));
        }
      });
      if (res && res[0] && res[0].result) return true;
    } catch(_) {}
    await sleep(1200);
  }
  return false;
}
```

### runner.js — 초기 orders COLLECT 발신 (838-843) [변경 2 삽입 지점]
```js
      await waitForPageLoadWithRetry(workTabId, 'orders 페이지');
      setRow(i, {orders:'run', note:'orders 수집중', current:true});
      await injectContent(workTabId);
      await sleep(600);   // ← 이 자리에서 waitForOrdersUiReady(workTabId) 먼저 대기
      try { await chrome.tabs.sendMessage(workTabId, {type:'COLLECT', source:'batch', targetStores}); }
      catch(e) { log(`[${acc.id}] COLLECT 전송 실패: ${e.message}`, 'err'); }
```

### runner.js — 하드스로틀 스킵 분기 (219-222, 849-854) [변경 1이 살려낼 기존 코드 — 수정 불필요]
```js
function isHardThrottlePayload(payload) {
  const rows = Array.isArray(payload?.storeResults) ? payload.storeResults : [];
  return !!(payload?.blocked || payload?.hardThrottle || rows.some(r => r.blocked || r.hardThrottle || r.status === 'blocked' || String(r.note || '').includes('10057')));
}
// ...
if (isHardThrottlePayload(op)) {
  const note = '10057: 과도한 요청으로 인해 서비스 이용이 일시적으로 제한되었습니다.';
  log(`[${acc.id}] ${note} — 자동/수동 재시도 제외`, 'err');
  markAccountStoresBlocked(acc, targetStores, note);
  setRow(i, {orders:'warn', cmg:'skip', menu:'skip', note, current:false, blocked:true, promote:true});
  return {status:'warn', note, ordersOk:true, cmgOk:true, menuOk:true, blocked:true, hardThrottle:true, dur:fmtMs(Date.now() - t0)};
}
```

### 03_coupangeats.js — 10057 감지 & in-page 쿨다운 (85-94, 115-141) [변경 1 대상]
```js
_isOrdersThrottleVisible() {
  const bodyText = document.body?.innerText || '';
  return bodyText.includes('10056') || bodyText.includes('10057')
    || bodyText.includes('ACCESS_DENIDE') || bodyText.includes('ACCESS_DENIED')
    || bodyText.includes('ACCESS_DENY')
    || bodyText.includes('단시간 내 반복 요청') || bodyText.includes('과도한 요청');
},
async _waitBeforeOrdersSearch() {
  const MIN_SEARCH_INTERVAL_MS = this._isSafeOrdersPacingEnabled() ? 15000 + Math.floor(Math.random()*10001) : 10000;
  if (this._isOrdersThrottleVisible()) {
    // ← 여기서 this._hardThrottled = true 세팅 후 즉시 return (escalating 쿨다운 스킵)
    this._ordersThrottleCount++;
    this._lastOrdersThrottleAt = Date.now();
    this._markOrdersTransientError();
    const cooldownMs = this._ordersThrottleDelayMs();
    this._ordersSearchCooldownUntil = Math.max(this._ordersSearchCooldownUntil, Date.now() + cooldownMs);
    // ...
  }
  // ...
},
```

### 03_coupangeats.js — 배치 orders 완료 payload (2349-2360) [변경 1: hardThrottle/blocked 추가]
```js
chrome.runtime.sendMessage({
  type: 'MULTISTORE_COMPLETE',
  payload: {
    category: 'orders',
    storeCount: matchingStores.length,
    completedCount: storeResults.filter(r => r.completed).length,
    stopped: !!this._stopFlag,
    zeroOrders: storeResults.length > 0 && storeResults.every(r => r.zeroOrders),
    partialThrottle: storeResults.some(r => r.partialThrottle),
    storeResults
    // ← this._hardThrottled 이면 hardThrottle:true, blocked:true 추가
  }
}).catch(() => {});
```

## Test Cases
1. **문법** `node --check "content/03_coupangeats.js"` → 기대: 출력 없음(exit 0)
2. **문법** `node --check "runner.js"` → 기대: 출력 없음(exit 0)
3. **10057 스킵 (변경 1):** 확장 리로드 → 배치 실행. 10057 발생 계정에서:
   - 대시보드 로그에 `10057 ... — 자동/수동 재시도 제외` 출력
   - 해당 계정 매장이 blocked(skip 표시), CMG·메뉴로 넘어가지 않음
   - 배치 종료 시 `📋 백필 큐 저장` 목록에 **미포함**
   - 다음 배치에서 재시도 **안 됨**
4. **warm-load (변경 2·3):** 정상 매장 1계정 배치 1회 →
   - 로그에서 `orders 타임아웃 — 0/0개`, `부분 저장 후 백필`, `WUJIE-APP 미준비`가 이전 대비 사라짐
   - 매장당 소요가 8분 예산에 붙지 않고 수 분 내 `완료`
   - 이전에 수동 재클릭해야 성공하던 매장이 자동으로 `완료`
5. **회귀:** 다른 계정으로 전환 로그인 정상 동작(캐시 보존이 인증에 영향 없음) 확인

## Verification Loop
```
LOOP until all PASS:
  1. Test Cases 1~2 (node --check) 먼저 실행
  2. FAIL → 문법 오류 원인 분석 후 수정
  3. Test Cases 3~5는 실제 확장 리로드 후 사용자 배치 실행으로 관찰
종료 조건: 1~2 PASS + Constraints 위반 없음 (3~5는 런타임 관찰 항목)
```

## Constraints
- `-DESKTOP-HG136JL` 파일 절대 수정 금지 (OneDrive 충돌본).
- `MULTISTORE_COMPLETE`는 top-frame(`window.top === window.self`)에서만 발신 — 기존 가드 유지.
- 변경 2의 `waitForOrdersUiReady`는 실패해도 **현행처럼 진행**(예외/스톨 방지). 새 하드 실패 경로 만들지 말 것.
- 변경 1의 트리거는 `_isOrdersThrottleVisible()`만 사용 — 일반 타임아웃을 밴으로 오인 금지.
- 10057은 **계정/IP 단위 밴**으로 간주 → 계정 전체 스킵이 정상 동작(개별 매장만 스킵 아님).
- CMG 경로(`waitForWujieApp`)는 이번에 손대지 않음.

## Do Not Ask — Decide Yourself
- 파일 이미 존재: 덮어쓰기(직접 편집)
- 셀렉터 모호 시: 기존 `_clickOrdersSearchButton`/`_getOrdersDropdownStores`가 쓰는 셀렉터 그대로 재사용
- 네이밍: 인스턴스 내부 플래그는 `_camelCase`, 기존 파일과 동일
- 주석: 최소화 (WHY만)
- 새 상수 필요 시: 파일 상단 인스턴스 상수 블록(`ORDERS_STORE_BUDGET_MS` 근처) 컨벤션 따르기
