# 부트스트랩 중복 네비게이션 제거 + 렌더러 마커 회귀 제거

## Task
`DB_Beamin_Macro_Dags`의 collect_all에서 로그인 직후 계정이 통째로 실패한다. 원인은 `login_baemin`이 이미 대시보드로 이동시켰는데 `_build_dashboard_session`이 직후 **무조건 한 번 더** `driver.get("https://self.baemin.com/")`로 무거운 React 대시보드를 재로드해 `Timed out receiving message from renderer`가 나고, 직전에 추가한 마커 때문에 이게 "죽은 세션"으로 오분류돼 재생성 한도 소진 → 계정 드롭되기 때문이다. 부트스트랩을 조건부 이동으로 바꾸고(주 수정) 렌더러 마커를 제거한다(보강).

## Project Conventions
- logging: `logger = logging.getLogger(__name__)` — print 절대 금지
- import 경로: `from modules.transform.pipelines.db.XXX import ...` (절대경로)
- 파이프라인 파일 위치: `modules/transform/pipelines/db/`
- 경로 상수: `from modules.transform.utility.paths import ...` (하드코딩 금지)

## Files to Create / Modify
- **수정**: `modules/transform/pipelines/db/DB_Beamin_combined.py` (단일 파일, 2곳)
- **참조(수정 없음)**: `modules/extract/croling_beamin.py` — `is_on_main_dashboard` / `login_baemin` 제공

## Implementation Steps

### 1. (주 수정) `_build_dashboard_session` 조건부 이동 — combined.py 현재 line 162-188
`driver.get`을 **무조건 호출 → `is_on_main_dashboard` 확인 후에만** 호출로 변경. 로그인 후 이미 대시보드면 redundant reload를 스킵 → 부트스트랩 렌더러 타임아웃 제거. 기존 try/except 세션복구 분기는 그대로 유지(실제 이동이 필요한 드문 경우 대비).

변경 전:
```python
        try:
            driver.set_page_load_timeout(45)
            driver.get("https://self.baemin.com/")
            return driver
        except Exception as exc:
            if _is_recoverable_session_issue(exc):
                ...
```
변경 후:
```python
        try:
            if not is_on_main_dashboard(driver.current_url):
                driver.set_page_load_timeout(45)
                driver.get("https://self.baemin.com/")
            return driver
        except Exception as exc:
            if _is_recoverable_session_issue(exc):
                ...   # 이하 기존 분기 그대로
```
`is_on_main_dashboard`는 **이미 import되어 있다**(combined.py line 26, croling_beamin에서) → import 추가 불필요. line 994에서도 동일 패턴으로 사용 중.

### 2. (보강) 렌더러 마커 제거 — `_SESSION_ISSUE_MARKERS` 현재 line 124-139
마지막 항목 한 줄만 삭제:
```python
    "session not created",
-   "timed out receiving message from renderer",   # ← 이 줄만 제거
)
```
수집 단계(`_run_stage`)에서 렌더러 타임아웃이 나도 "죽은 세션"으로 오분류해 매장을 재생성/드롭하지 않도록.

### 변경 금지
NOW 복원(line 1093 `_run_stage("NOW 수집", ...)`), store_id 정규화·브랜드 정렬, 세션복구 인프라(`_close_driver_safely`/`_is_recoverable_session_issue`) 등 나머지 working tree 변경은 **전부 보존**. 위 2곳만 수정.

## Reference Code

### DB_Beamin_combined.py (현재 — 수정 대상)
```python
from modules.extract.croling_beamin import (
    ..., is_on_main_dashboard, launch_browser, login_baemin, logout_baemin,
    ..., wait_for_page,
)   # line 26: is_on_main_dashboard 이미 import됨

_SESSION_ISSUE_MARKERS = (
    "remote end closed", "connection aborted", "remotedisconnected",
    "connection refused", "max retries exceeded", "newconnectionerror",
    "invalid session id", "chrome not reachable", "disconnected",
    "tab crashed", "session deleted", "cannot connect to chrome",
    "session not created",
    "timed out receiving message from renderer",   # ← 제거 대상
)

def _build_dashboard_session(account: dict, metrics: dict, profile: dict) -> Any | None:
    account_id = account["account_id"]
    max_recovery = int(profile["max_session_recovery_per_account"])
    for attempt in range(max_recovery + 1):
        driver = _build_account_session(account, metrics)
        if driver is None:
            return None
        try:
            driver.set_page_load_timeout(45)        # ← is_on_main_dashboard 가드로 감쌀 대상
            driver.get("https://self.baemin.com/")
            return driver
        except Exception as exc:
            if _is_recoverable_session_issue(exc):
                _close_driver_safely(driver, account_id)
                if attempt < max_recovery:
                    metrics["session_recovery_count"] += 1
                    logger.warning("초기 대시보드 세션 문제 감지, 재생성 시도: %s / %s", account_id, exc)
                    continue
                logger.warning("초기 대시보드 세션 문제 재생성 한도 초과: %s / %s", account_id, exc)
                return None
            logger.warning("대시보드 이동 타임아웃 (계속 진행): %s", exc)
            return driver
    return None
```

### croling_beamin.py (참조 — login_baemin이 이미 대시보드로 이동시킴)
```python
def is_on_main_dashboard(url: str) -> bool:
    """우리가게NOW ShopSelect가 있는 메인 대시보드 URL인지 확인한다."""
    from urllib.parse import urlparse
    parsed = urlparse(url)
    return parsed.hostname == "self.baemin.com" and parsed.path in ("", "/")

def ensure_main_dashboard(driver, account_id=None) -> None:
    if is_on_main_dashboard(driver.current_url):
        return
    driver.set_page_load_timeout(45)
    driver.get(MAIN_URL)

def login_baemin(driver, account_id, password) -> bool:
    ...
    if is_on_success_page(current_url):
        ensure_main_dashboard(driver, account_id)   # ← 로그인 성공 시 이미 대시보드 진입
        log("  로그인 성공 (URL 매칭)", account_id)
        return True
```

## Test Cases
1. [구문] `python -c "import ast; ast.parse(open('modules/transform/pipelines/db/DB_Beamin_combined.py',encoding='utf-8').read())"` → 기대: 출력 없음
2. [마커 제거] `python -c "from modules.transform.pipelines.db.DB_Beamin_combined import _SESSION_ISSUE_MARKERS as m; assert 'timed out receiving message from renderer' not in m; print('OK', len(m))"` → 기대: `OK 13`
3. [조건부 이동 반영] `python -c "import inspect, modules.transform.pipelines.db.DB_Beamin_combined as M; s=inspect.getsource(M._build_dashboard_session); assert 'is_on_main_dashboard(driver.current_url)' in s; print('OK')"` → 기대: `OK`
4. [diff 검증] `git diff modules/transform/pipelines/db/DB_Beamin_combined.py` → 기대: `_build_dashboard_session`이 조건부 이동으로 바뀌고 마커 1줄 제거, 나머지 working tree 변경(NOW 복원·store_id 정규화 등) 보존
5. [실행] `DB_Beamin_Macro_Dags` 수동 트리거 → collect_all 로그에서 로그인 직후 "초기 대시보드 세션 문제 감지/재생성" 로그가 사라지고 곧장 매장 목록 조회 → NOW 수집 진입, `failed_accounts` 감소

## Verification Loop
```
LOOP until all PASS:
  1. Test Cases 1~4 순서대로 실행
  2. FAIL 항목 → 원인 분석
  3. 코드 수정
  4. 전체 Test Cases 재실행
종료 조건: 1~4 전체 PASS + Constraints 위반 없음 (5번은 Airflow 실행 환경에서 수동 확인)
```

## Constraints
- 위 2곳(`_build_dashboard_session` 조건부 이동, 마커 1줄 제거)만 수정. NOW 복원·세션 인프라·store_id 정규화 등 다른 working tree 변경은 절대 손대지 말 것. `git checkout`/파일 통째 되돌리기 금지.
- `_build_dashboard_session`의 try/except 세션복구 분기는 제거하지 말고 유지 (조건부 가드만 추가).
- `is_on_main_dashboard` 신규 import 추가 금지 (이미 import됨).
- 범위 밖: 렌더러 타임아웃 자체 완화(재시도·대기 상향), `max_session_recovery_per_account` 상향, page load timeout 튜닝, 커밋/푸시.

## Do Not Ask — Decide Yourself
- 마커 위치 헷갈리면: `"session not created",` 바로 다음 줄이 제거 대상
- 조건부 가드 위치: 기존 `driver.set_page_load_timeout(45); driver.get(...)` 두 줄을 `if not is_on_main_dashboard(driver.current_url):` 블록으로 감싸기
- 들여쓰기/스타일: 기존 함수와 동일하게
- 주석: 최소화 (WHY만)
