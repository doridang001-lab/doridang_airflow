# 안 쓰는 스킬 · 에이전트 삭제

## Task
`.claude` 하위에 실제 호출 이력이 0회인 스킬/에이전트가 쌓여 카탈로그가 지저분하다. 244개 세션 트랜스크립트 분석으로 확정한 미사용 항목을 삭제해 정리한다. 실사용 중인 `to-codex`, `gitpush`, `crawl`과 Plan Mode를 구현하는 `plan-workflow` 플러그인은 절대 삭제하지 않는다.

## Files to Delete

### 에이전트 (`C:\airflow\.claude\agents\`)
- `dag-team-planner.md`
- `dag-team-coder.md`
- `dag-team-tester.md`
- `web-scraper-auto-scroll.md`

### 프로젝트 스킬 (`C:\airflow\.claude\skills\`)
- `data-asset.md`  ← 파일 (깨진 스킬, 디렉터리+SKILL.md 구조 아님)
- `claude-md-improver\`  ← 디렉터리
- `data-report\`  ← 디렉터리
- `flow-upload\`  ← 디렉터리
- `output-style\`  ← 디렉터리
- `ppt\`  ← 디렉터리
- `to-compact\`  ← 디렉터리

### 유저 스킬 (`C:\Users\민준\.claude\skills\`)
- `ollama-connect\`  ← 디렉터리
- `partition-migrate\`  ← 디렉터리

## Do NOT Delete (실사용 중)
- `plan-workflow\` (유저 스킬) — 훅 기반 플러그인. **Plan Mode 자체를 구현**. 삭제 시 Plan Mode 고장.
- `to-codex\` (유저 스킬) — 176회 사용
- `gitpush\` (프로젝트 스킬) — 85회 사용
- `crawl\` (유저 스킬) — MEMORY.md 크롤링 규칙에서 필수 워크플로우로 명시

## Implementation Steps
1. 삭제 전 상태 확인 (git 추적 여부 포함):
   - `git -C C:/airflow status --short C:/airflow/.claude/agents C:/airflow/.claude/skills`
2. 에이전트 4개 파일 삭제:
   ```powershell
   Remove-Item -Force "C:\airflow\.claude\agents\dag-team-planner.md","C:\airflow\.claude\agents\dag-team-coder.md","C:\airflow\.claude\agents\dag-team-tester.md","C:\airflow\.claude\agents\web-scraper-auto-scroll.md"
   ```
3. 프로젝트 스킬 삭제 (파일 1개 + 디렉터리 6개):
   ```powershell
   Remove-Item -Force "C:\airflow\.claude\skills\data-asset.md"
   Remove-Item -Recurse -Force "C:\airflow\.claude\skills\claude-md-improver","C:\airflow\.claude\skills\data-report","C:\airflow\.claude\skills\flow-upload","C:\airflow\.claude\skills\output-style","C:\airflow\.claude\skills\ppt","C:\airflow\.claude\skills\to-compact"
   ```
4. 유저 스킬 디렉터리 2개 삭제:
   ```powershell
   Remove-Item -Recurse -Force "C:\Users\민준\.claude\skills\ollama-connect","C:\Users\민준\.claude\skills\partition-migrate"
   ```
5. (선택) `C:\airflow\.claude\settings.local.json`의 죽은 허용목록 정리 — 삭제된 스킬의 permission 항목(예: `"Skill(ppt)"`, `"Skill(claude-md-improver)"`). 기능 영향 없음. 원치 않으면 건너뛴다.

## Test Cases
1. [에이전트 삭제 확인] `Get-ChildItem C:\airflow\.claude\agents` → 기대: dag-team-*, web-scraper-auto-scroll 없음 (폴더가 비었거나 다른 파일만 남음)
2. [프로젝트 스킬 확인] `Get-ChildItem C:\airflow\.claude\skills -Name` → 기대: `gitpush`만 남음 (data-asset.md 및 삭제 대상 6개 폴더 없음)
3. [유저 스킬 확인] `Get-ChildItem C:\Users\민준\.claude\skills -Name` → 기대: `crawl`, `to-codex`, `plan-workflow`만 남음 (ollama-connect, partition-migrate 없음)
4. [git 반영 확인] `git -C C:/airflow status --short` → 기대: 삭제된 추적 파일이 `D`로 표시(추적 대상이었을 경우)

## Verification Loop
```
LOOP until all PASS:
  1. Test Cases 1~4 순서대로 실행
  2. FAIL 항목 → 삭제 누락/오타 경로 확인
  3. 재삭제 또는 경로 수정
  4. 전체 Test Cases 재실행
종료 조건: 전체 PASS + Do NOT Delete 항목이 모두 그대로 존재
```

## Constraints
- **Do NOT Delete 목록(plan-workflow, to-codex, gitpush, crawl)은 절대 건드리지 말 것.**
- `plan-workflow` 삭제 시 Plan Mode가 고장나므로 특히 주의.
- 디렉터리는 `-Recurse`, 단일 파일(`data-asset.md`)은 `-Recurse` 없이 삭제.
- 삭제 전 `git status`로 추적 여부만 확인하고, 커밋/푸시는 사용자 지시 없으면 하지 않는다.

## Do Not Ask — Decide Yourself
- 경로가 이미 없으면(이전에 삭제됨): 성공으로 간주하고 다음 항목 진행
- git 추적 파일이면: `Remove-Item`으로 삭제 후 `git status`에 `D`로 뜨는 것 정상
- settings.local.json 정리(Step 5): 애매하면 건너뛴다 (기능 영향 없음)
