# 미지원페이지버그수정

## Task
개발용 크롬 확장(doridang_collector_개발용)으로 교체 후 배치 수집 중 "⚠️ 미지원 페이지" 팝업이 반복 발생함. 로그인 후 쿠팡이츠가 `/home/STORE_ID`에 착지할 때 runner가 이를 orders 도달로 오판하고 COLLECT를 보내는 버그(Bug 1)와, 06_batch.js 래퍼가 opts를 원본 collect()에 전달하지 않는 버그(Bug 2) 2개를 수정한다.

## Status
- 구현 완료: 2026-06-23
- 최종 보완 완료: 2026-06-23
- 적용 파일:
  - `c:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용\runner.js`
  - `c:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용\content\06_batch.js`
  - `c:\airflow\coupang_extension_build\runner.js`
  - `c:\airflow\coupang_extension_build\content\06_batch.js`
  - `c:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용\content\05_main.js`
  - `c:\airflow\coupang_extension_build\content\05_main.js`
  - `c:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용\content\03_coupangeats.js`
  - `c:\airflow\coupang_extension_build\content\03_coupangeats.js`
- 보완 반영: `/orders` 이동 실패 시 `COLLECT`를 보내지 않고 orders 실패로 종료한다.
- 자동 실행 확인: 작업 스케줄러 `CoupangAutoCollect`는 Windows 로그온 후 30초 지연으로 `c:\airflow\scripts\coupang_boot_autostart.ps1`를 실행하며, 해당 스크립트는 `c:\airflow\coupang_extension_build` 확장을 로드한다.
- 추가 보완: 대시보드 배치 수집의 `ce_current_target_stores`가 orders 주소 수동 수집과 충돌하지 않도록, 수동 단축키 시작 시 배치/재시작 상태를 제거하고 `COLLECT` 메시지가 `source:'batch'`일 때만 대상 매장 상태를 저장한다.
- 추가 보완: 쿠팡 계정/드롭다운/수동 현재 매장에서 `도리당`, `나홀로`가 아닌 브랜드는 배치 큐와 실제 수집 대상에서 제외한다.
- 추가 보완: 배치 수집에서 `opts`가 `03_coupangeats.js`까지 전달되도록 유지해 orders 주문일 필터가 배치 기준인 어제~어제로 적용되게 했다.
- 자동 실행 반영: OneDrive 개발용 확장뿐 아니라 작업 스케줄러가 실제 로드하는 `c:\airflow\coupang_extension_build`에도 동일 수정본을 반영했다.
- 재발 방지 방향: OneDrive 확장 폴더에는 테스트 폴더를 만들지 않고, 이 md에서 원본 확장 경로를 참조만 한다. 실제 사전검사/런타임 검증 하네스는 `c:\airflow\scripts` 아래에 둔다.

## Project Conventions
- 파일 경로: 아래 Files to Modify 기준, 절대경로 사용
- 언어: JavaScript (Chrome Extension MV3)
- 최초 계획은 `runner.js`, `content/06_batch.js` 2개 파일 수정이었으나, 실제 장애 원인 추가 확인 후 `content/05_main.js`, `content/03_coupangeats.js`, 자동 실행용 빌드 폴더까지 범위를 확장했다.
- 기존 변수명·함수명 스타일 유지 (camelCase)
- 주석 최소화 (WHY만, WHAT 설명 금지)

## Files to Modify

- `c:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용\runner.js`
- `c:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용\content\06_batch.js`
- `c:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용\content\05_main.js`
- `c:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용\content\03_coupangeats.js`
- `c:\airflow\coupang_extension_build\runner.js`
- `c:\airflow\coupang_extension_build\content\06_batch.js`
- `c:\airflow\coupang_extension_build\content\05_main.js`
- `c:\airflow\coupang_extension_build\content\03_coupangeats.js`

## Implementation Steps

### Fix 1 — runner.js: `/home` 착지 후 `/orders`로 명시 이동

**파일**: `runner.js`  
**위치**: line ~708 (주석 `// 3) 주문서(orders) 수집` 블록)

**현재 코드** (line 708–715):
```javascript
if (!skip.orders) {
  setRow(i, {orders:'run', note:'orders 로딩 대기', current:true});
  await waitForPageLoadWithRetry(workTabId, 'orders 페이지');
  setRow(i, {orders:'run', note:'orders 수집중', current:true});
  await injectContent(workTabId);
        await sleep(600);
  try { await chrome.tabs.sendMessage(workTabId, {type:'COLLECT', source:'batch', targetStores}); }
  catch(e) { log(`[${acc.id}] COLLECT 전송 실패: ${e.message}`, 'err'); }
```

**수정 후**:
```javascript
if (!skip.orders) {
  setRow(i, {orders:'run', note:'orders 페이지로 이동', current:true});
  // home 착지 케이스 대비: /orders 경로가 아니면 명시 이동 후 대기
  const curUrl = await getUrl(workTabId);
  if (!curUrl.includes('/merchant/management/orders')) {
    await chrome.tabs.update(workTabId, {url: 'https://store.coupangeats.com/merchant/management/orders'});
    const ordersNav = await waitUrl(workTabId, {orders: /merchant\/management\/orders/}, 20000);
    if (!ordersNav || stopRequested) {
      const note = stopRequested ? '중단됨' : 'orders 페이지 이동 실패';
      log(`[${acc.id}] ${note}`, 'err');
      setRow(i, {orders:'fail', note});
      recordCategoryResults('orders', acc, targetStores, null, false, note);
      return {status:'fail', note, ordersOk:false, cmgOk:false};
    }
  }
  await waitForPageLoadWithRetry(workTabId, 'orders 페이지');
  setRow(i, {orders:'run', note:'orders 수집중', current:true});
  await injectContent(workTabId);
  await sleep(600);
  try { await chrome.tabs.sendMessage(workTabId, {type:'COLLECT', source:'batch', targetStores}); }
  catch(e) { log(`[${acc.id}] COLLECT 전송 실패: ${e.message}`, 'err'); }
```

**주의**: `getUrl`, `waitUrl`, `waitForPageLoadWithRetry`, `injectContent`, `sleep` 함수는 이미 runner.js에 존재함 — 새로 정의하지 않는다.

---

### Fix 2 — 06_batch.js: opts 전달

**파일**: `content/06_batch.js`  
**위치**: line ~31 (IIFE 내부 `site.collect = async function` 부분)

**현재 코드**:
```javascript
site.collect = async function () {
  let collectError = null;
  try {
    await origCollect();
  } catch (e) {
```

**수정 후**:
```javascript
site.collect = async function (opts = {}) {
  let collectError = null;
  try {
    await origCollect(opts);
  } catch (e) {
```

`opts`를 받아서 `origCollect(opts)`로 그대로 전달하는 것이 전부다. 나머지 코드는 변경 없음.

---

### Fix 3 — 05_main.js: 배치 상태와 수동 수집 충돌 방지

**파일**: `content/05_main.js`

**문제**:
- 대시보드 배치 수집에서 저장한 `ce_current_target_stores`가 남아 있는 상태에서 사용자가 orders 주소로 직접 들어가 수동 수집하면, 이전 배치 대상 매장 상태와 수동 수집이 섞일 수 있다.

**수정 내용**:
- 단축키 수동 시작 시 `__collector_auto_run`, `ce_current_target_stores`, `ce_orders_restart`, `ce_orders_resume` 상태를 제거한다.
- `COLLECT` 메시지는 `source:'batch'`이고 `targetStores`가 있을 때만 `ce_current_target_stores`를 저장한다.
- 수동 또는 대상 없는 `COLLECT`는 기존 배치 대상/재시작 상태를 제거한 뒤 `runCollector({ source, targetStores })`를 호출한다.

---

### Fix 4 — 03_coupangeats.js / runner.js: 대상 브랜드 제한

**파일**: `runner.js`, `content/03_coupangeats.js`

**문제**:
- 쿠팡 계정에 `도리당`, `나홀로` 외 브랜드가 함께 있으면 배치 큐 또는 실제 드롭다운 수집 대상에 포함될 수 있다.
- orders 주소로 직접 접근해 수동 수집할 때도 현재 매장이 대상 브랜드인지 확인하지 않았다.

**수정 내용**:
- `runner.js`의 계정 큐 생성과 계정별 대상 매장 계산에서 `도리당`, `나홀로`가 아닌 매장은 제외한다.
- `03_coupangeats.js`의 대상 매장 필터링에서도 `도리당`, `나홀로`가 아닌 매장은 제외한다.
- 수동 orders 수집 시작 시 현재 매장명이 대상 브랜드가 아니면 `MULTISTORE_COMPLETE`를 `skipped:true`, `reason:'not_target_brand'`로 보내고 수집을 중단한다.

## Reference Code

### runner.js — getUrl / waitUrl (참고용, 수정 불필요)
```javascript
async function getUrl(tabId) {
  try { const t = await chrome.tabs.get(tabId); return t.url||''; } catch(_) { return ''; }
}
async function waitUrl(tabId, regexMap, ms) {
  const deadline = Date.now() + ms;
  while (Date.now() < deadline) {
    // ...
  }
}
// orders 즉시 재시도 패턴 (line ~830) — Fix 1의 참고 패턴:
await chrome.tabs.update(workTabId, {url: 'https://store.coupangeats.com/merchant/management/orders'});
const ordersNav = await waitUrl(workTabId, {orders: RE_ORDERS}, 25000);
```

### 06_batch.js — 현재 래퍼 전체 (수정 범위 확인용)
```javascript
if (!site.__batchWrapped) {
  site.__batchWrapped = true;
  const origCollect = site.collect.bind(site);

  site.collect = async function () {       // ← 여기만 수정
    let collectError = null;
    try {
      await origCollect();                 // ← 여기만 수정
    } catch (e) {
      collectError = e;
      console.error('[Batch] collect() 오류:', e);
    }
    // ... COLLECTION_COMPLETE 발신 로직 (변경 없음)
    if (collectError) throw collectError;
  };
}
```

## Test Cases

1. **Fix 2 확인** — 수정 후 배치 수집 1개 계정 실행:
   - 기대: `targetStores` 필터가 적용되고 주문일이 배치 모드 기준(어제~어제)으로 설정됨
   - 주의: `[Collector] 쿠팡이츠 수집 시작 (source: batch)` 로그는 `05_main.js`에서 `site.collect(opts)` 호출 전에 찍히므로 단독 검증 기준으로 쓰지 않는다.

2. **Fix 1 확인** — 배치 수집 중 로그인 후 `/home/` 착지 시:
   - 기대: `/merchant/management/orders`로 명시 이동 후 수집 시작
   - 기대: "미지원 페이지" 팝업 **미발생**
   - runner 대시보드에서 orders 컬럼이 ✅ 표시됨
   - `/orders` 이동 실패 시 `orders 페이지 이동 실패`로 종료되고 `COLLECT`는 전송되지 않음

3. **전체 수집 완료** — 2~3개 계정 배치 수집:
   - 기대: CSV 파일 정상 다운로드, COLLECTION_COMPLETE 신호 수신

4. **수동/배치 충돌 확인**:
   - 대시보드 배치 수집 후 orders 주소로 직접 들어가 수동 수집을 실행한다.
   - 기대: 수동 수집은 이전 배치의 `ce_current_target_stores`를 재사용하지 않는다.
   - 기대: 배치 수집은 `source:'batch'`일 때만 대상 매장 상태를 저장한다.

5. **대상 브랜드 제한 확인**:
   - 쿠팡 계정/매장 목록에 `도리당`, `나홀로` 외 브랜드가 섞여 있는 상태에서 실행한다.
   - 기대: 배치 큐와 실제 드롭다운 수집 대상에는 `도리당`, `나홀로` 매장만 포함된다.
   - 기대: orders 주소 직접 접근 수동 수집에서 현재 매장이 대상 브랜드가 아니면 수집하지 않고 skipped 완료 신호만 보낸다.

## Verification Loop

```
LOOP until all PASS:
  1. chrome://extensions → 개발용 확장 새로고침 버튼 클릭
  2. runner.html 열기 → 1~2개 계정 선택 → 수집 시작
  3. Test Cases 1~3 순서대로 확인
  4. FAIL 항목 → 원인 분석 후 코드 수정
  5. 전체 Test Cases 재실행
종료 조건: 전체 PASS + "미지원 페이지" 미발생
```

## Regression Harness Plan

하네스는 OneDrive 확장 폴더에 새 폴더를 만들지 않는다. 이 문서는 원본 확장 경로만 참조하고, 실행 스크립트는 저장소 운영 경로인 `c:\airflow\scripts`에 둔다.

- 원본 확장 참조:
  - `c:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용`
- 자동 실행 빌드 참조:
  - `c:\airflow\coupang_extension_build`
- 권장 사전검사 스크립트:
  - `c:\airflow\scripts\coupang_extension_preflight.ps1`
- 권장 런타임 확인 스크립트:
  - `c:\airflow\scripts\coupang_extension_runtime_check.ps1`

사전검사에서 확인할 항목:
- `manifest.json`이 참조하는 content script 파일 존재 여부
- OneDrive 원본과 `c:\airflow\coupang_extension_build`의 핵심 파일 해시 일치 여부
- `node --check` 문법 검사
- `source:'batch'`일 때만 배치 대상 매장 상태를 저장하는지
- `source:'manual'` 수집이 이전 배치 상태를 제거하는지
- `도리당`, `나홀로` 외 브랜드가 큐/수집 대상에서 제외되는지
- batch 모드 orders 날짜가 어제~어제로 적용되는지

런타임 확인에서 확인할 항목:
- 작업 스케줄러 `CoupangAutoCollect`가 로그온 트리거로 등록되어 있는지
- 자동 실행 스크립트가 `c:\airflow\coupang_extension_build`를 로드하는지
- Chrome 디버그 포트 9222가 열리는지
- runner 탭 또는 확장 service worker가 로드되는지
- 실제 수집 클릭 전 단계까지 자동 실행 경로가 정상인지

## Constraints

- `runner.js` Fix 1에서 `getUrl`, `waitUrl` 등 **기존 함수를 재정의하지 않는다**
- `06_batch.js`에서 `COLLECTION_COMPLETE` 발신 로직, `LOGOUT_DOM` 핸들러 등 나머지 코드는 **변경하지 않는다**
- `content/03_coupangeats.js`, `content/05_main.js`는 추가 장애 원인 대응 범위에 한해 최소 수정한다.
- Fix 1에서 `waitUrl`의 두 번째 인자는 `{orders: /merchant\/management\/orders/}` (regex 객체) — 기존 `waitUrl` 함수 시그니처와 동일하게
- OneDrive 개발용 확장 수정본은 작업 스케줄러가 사용하는 `c:\airflow\coupang_extension_build`에도 동기화한다.

## Do Not Ask — Decide Yourself

- 파일이 이미 존재하면: 해당 줄만 수정 (전체 덮어쓰기 금지)
- `await sleep(600)` 앞 불규칙한 들여쓰기(기존 코드): 수정하지 않음
- runner.js에서 `waitUrl` 호출 시 timeout ms: 20000 사용
- 변수명·함수명: 기존 파일 스타일(camelCase) 유지
