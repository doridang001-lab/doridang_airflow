# 배민매크로DAG 기본 안정성 프로필 되돌리기

## Task
`DB_Beamin_Macro_Dags` 정기 run이 한 회차에 10시간+ 소요되어 다음날 run을 막고 있음. 원인은 워킹카피가 기본 안정성 프로필을 `safe`→`safe_memory`로 강제(`... or "safe_memory"`)한 것. safe_memory는 매장마다 드라이버 재시작 + account_wait 60~140s로 런타임을 2~3배 늘리지만 렌더러 타임아웃 실패는 그대로 발생함. 리팩터 개선분은 유지하고 **기본 프로필만 `safe`로 되돌린다**(파일 전체 revert 아님).

로그 근거: `collect_all` 07-10 03:10 KST 시작 → 08:27 종료(5시간 17분), 9/12 계정만 성공. 첫 로그 라인 `수집 안정성 프로필: name=safe_memory driver_restart_every_stores=1 max_session_recovery_per_account=4`.

## Project Conventions
- logging: `logger = logging.getLogger(__name__)` — print 절대 금지
- import 경로: `from modules.transform.utility.XXX import ...` (절대경로)
- 파이프라인 파일 위치: `modules/transform/pipelines/{domain}/`
- DAG 파일 위치: `dags/{domain}/`
- 스케줄 상수: `from modules.transform.utility.schedule import ...` (하드코딩 금지)
- 경로 상수: `from modules.transform.utility.paths import ...` (하드코딩 금지)

## Files to Create / Modify
- 수정: `dags/db/DB_Beamin_Macro_Dags.py` — 3곳에서 `or "safe_memory"` 제거
- 생성 파일 없음

## Implementation Steps
1. **L327 `retry_failed`**: `profile = resolve_stability_profile(conf.get("stability_profile") or "safe_memory")` → `profile = resolve_stability_profile(conf.get("stability_profile"))`
2. **L730 `collect_all`**: `profile = resolve_stability_profile(conf.get("stability_profile") or "safe_memory")` → `profile = resolve_stability_profile(conf.get("stability_profile"))`
3. **L951 `trigger_retry_if_needed`** (Retry DAG conf 전달): `stability_profile=conf.get("stability_profile") or "safe_memory",` → `stability_profile=conf.get("stability_profile"),`

세 곳 모두 값이 None이면 `resolve_stability_profile`의 `DEFAULT_PROFILE="safe"`로 귀결됨(`modules/transform/pipelines/db/beamin_stability.py`). `DB_Beamin_Macro_Dags_Retry.py`는 conf를 그대로 전달받으므로 별도 수정 불필요 — 트리거 conf에서 safe_memory가 빠지면 Retry 체인 전체가 safe로 동작. 수동 실행 시 `conf={"stability_profile":"safe_memory"}`를 넘기면 여전히 느린 프로필 선택 가능(선택지 유지).

## Reference Code
### modules/transform/pipelines/db/beamin_stability.py
```python
DEFAULT_PROFILE = "safe"

STABILITY_PROFILES: dict[str, dict[str, Any]] = {
    "safe": {
        "initial_stagger_range": (0.0, 20.0),
        "account_wait_range": (20.0, 45.0),
        "orders_validation_retry": 2,
        "max_parallel_accounts": 2,
        "driver_restart_every_stores": 4,
        "login_attempts": 2,
        "login_retry_wait_range": (1.0, 3.0),
        "max_session_recovery_per_account": 2,
    },
    "safe_memory": {
        "initial_stagger_range": (30.0, 80.0),
        "account_wait_range": (60.0, 140.0),
        "driver_restart_every_stores": 1,   # 매장마다 신선한 세션
        "max_session_recovery_per_account": 4,
        # ...
    },
    # today_orders, bulk_70 ...
}

def resolve_stability_profile(name: str | None) -> dict[str, Any]:
    profile_name = (name or DEFAULT_PROFILE).strip() or DEFAULT_PROFILE
    profile = deepcopy(STABILITY_PROFILES.get(profile_name, STABILITY_PROFILES[DEFAULT_PROFILE]))
    profile["name"] = profile_name if profile_name in STABILITY_PROFILES else DEFAULT_PROFILE
    return profile
```

## Test Cases
1. [문법] `python -c "import ast; ast.parse(open('dags/db/DB_Beamin_Macro_Dags.py',encoding='utf-8').read())"` → 기대: 에러 없음
2. [기본 프로필] `python -c "from modules.transform.pipelines.db.beamin_stability import resolve_stability_profile as r; print(r(None)['name'])"` → 기대: `safe`
3. [잔존 확인] `python -c "print('safe_memory' in open('dags/db/DB_Beamin_Macro_Dags.py',encoding='utf-8').read().split('conf.get(\"stability_profile\")')[0])" ` 대신 `grep -n 'or \"safe_memory\"' dags/db/DB_Beamin_Macro_Dags.py` → 기대: 매칭 0건
4. [다음 run 로그] 수동 trigger(conf 없이) 후 collect_all 첫 로그 라인이 `수집 안정성 프로필: name=safe ...` 인지 확인

## Verification Loop
```
LOOP until all PASS:
  1. Test Cases 순서대로 실행
  2. FAIL 항목 → 원인 분석
  3. 코드 수정
  4. 전체 Test Cases 재실행
종료 조건: 전체 PASS + Constraints 위반 없음
```

## Constraints
- 3곳 외 다른 로직(residual_failed, blind 검증, 알림 포맷, pool/max_active_tasks 등 리팩터분)은 건드리지 않는다.
- `beamin_stability.py`의 프로필 값 자체는 변경하지 않는다(기본 선택만 바꿈).
- 수동 실행용 safe_memory 선택 경로는 그대로 유지한다.

## Do Not Ask — Decide Yourself
- import가 모호하면: Reference Code 패턴 따라가기
- 타입 힌트·주석 스타일: 기존 파일과 동일하게
- 변수명·함수명: 기존 그대로

## 별도 조치 (코드 밖, 사람이 실행)
코드 수정은 다음 run부터 적용. 현재 걸려있는 `scheduled__2026-07-09T18:10:00+00:00` run과 연쇄된 `DB_Beamin_Macro_Dags_Retry` run은 Airflow UI/CLI에서 mark failed 또는 clear 하여 백로그(다음날 03:10 run 막힘)를 해소해야 함.
