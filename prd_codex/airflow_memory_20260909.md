# 2026-09-09 Airflow 메모리 고갈 완화 및 승인 작업 복구 결과

- 원인: WSL 커널 로그에서 가용 메모리 약 95MB, 스왑 4GB 전량 소진 확인. 여러 DAG의 파일 읽기·브라우저 준비에서 `Errno 12: Cannot allocate memory` 발생.
- 감시기가 08:22경 워커와 트리거러를 재시작했고, 08:29경 일부 실행이 실패 처리됨. 개별 실패 10건은 `.tmp/memory_incident_20260909.json`에 정확한 run_id·task_id·map_index·로그 경로로 기록.
- 조치: Celery 동시 실행을 16개에서 6개로 축소. 실행 중인 작업을 종료하지 않고 유휴 프로세스 10개만 축소. 현재 워커의 airflow.cfg와 저장소 docker-compose.yaml 양쪽에 6개를 지정해 restart 및 recreate 이후 유지.
- 검증: Compose 설정 검증 통과, 현재 설정 6 및 실제 워커 자식 프로세스 6개 확인. Celery 통계의 max-concurrency는 시작 당시 값 16을 표시하므로 실제 processes 개수로 검증. DAG import error 0건. 08:23 이후 성공 태스크 61건 확인.

## 승인 후 재처리 범위

사용자가 2026-09-09 후속 메시지에서 OneDrive 갱신을 포함한 재처리를 승인했다. 기존 스케줄러가 진행하던 실행은 유지하며 중복 복구를 피한다.

| 대상 | 확인된 상태 | 승인 후 처리 |
|---|---|---|
| DB_OKPOS_Sales_Today_Dags | 09-08T12:00Z 실행의 download_receipt_batch 인덱스 0, 1, 2 메모리 오류 | 날짜 계산과 후속 성공 산출물 확인 후 필요한 실패 배치와 차단된 저장 태스크만 재실행 |
| DB_MenuHierarchy_Test_Dags | 09-08T04:00Z build_orders 메모리 오류, 대상 2026-07·08 | 후속 성공 실행이 없을 때 해당 주문서 재생성 |
| DB_DeliveryCommission_Dags | 09-08T12:10Z monitor_baemin_settlement_missing 실행 전 executor 실패 | 누락 점검 및 차단된 수수료·매출·비용 마트 재처리 |
| DB_UnifiedSales | 09-07T22:40Z sync_unionpos_products 메모리 오류 | 후속 schedule_guard 실행에서 같은 태스크 성공 확인. 후속 전체 실행 결과와 대상 기간 대조 후 미복구 범위만 처리 |
| 배민 업로드 검증·룩백·재수집 | 실패 태스크 4건이 있어도 종료 태스크 때문에 DAG 자체는 success | 기존 후속 재수집 큐·검증 결과와 대조해 중복 없이 누락 범위만 재처리 |

- 예상 갱신 경로: OneDrive의 Repository, data/analytics, data/mart, 필요 시 Collect_Data. 정확한 날짜·매장 범위는 저장한 실행 식별자와 기존 산출물을 대조해 한정한다.
- 새 외부 메시지는 수동 전송하지 않는다. 알림 태스크 재실행이 필요하면 별도 전송 권한을 확인한다.
- 남은 위험: 동시 실행 축소는 메모리 피크를 낮추지만 개별 작업의 과도한 사용까지 보장하지 않는다. 처리 지연이 늘 수 있으며, 실패 이력과 데이터 복구 완료는 별개이다. C: 여유 공간 약 19GB도 운영 감시가 필요하다.

## 승인 후 실행 기록

- 08:59 메뉴계층 `build_orders` 1개 재개. 기존 날짜 XCom의 2026-07·08 유지.
- 08:59 OKPOS 실패 배치 3개와 차단된 merge·save 태스크 2개 재개. 기존 `resolve_today` XCom의 날짜는 2026-09-09로 확인.
- 09:00 수수료 DAG 실패·차단 태스크 5개 재개. 배민 업로드 `validate_toorder`와 룩백 `trigger_missing_baemin_orders`만 각각 재개해 성공했던 알림·정리 작업을 중복 실행하지 않음.
- 통합매출은 기존 후속 schedule_guard 실행이 정상 저장 중이므로 중복 실행하지 않음. 7월 19일 배민 실패 배치는 기존 attempt_2가 큐에 있고 잔여 대상 7개를 승계했음을 XCom으로 확인.
- 실행별 상태는 `.tmp/approved_recovery_20260909.json`에 읽기 전용으로 갱신. 재개 자체를 산출물 복구 완료로 간주하지 않음.

## 산출물 확인

- OKPOS: 09:08 전체 12개 태스크 성공. 동두천지행점 291행, 송파삼전점 173행, 평택비전점 136행의 `analytics/okpos_sales_raw/brand=도리당/store=*/ym=2026-09/okpos_order.csv` 저장 및 직접 읽기 확인. 요청일 XCom은 09-09지만 실제 CSV 영업일은 09-01~09-08이며, 날짜는 기존 OKPOS 화면·원천 기준을 유지.
- 메뉴계층: 09:10 전체 성공. `data/mart/new_classification_orders/02_최종주문.csv` 16,140행·86개 컬럼 읽기 확인. 2026-07 8,642행, 2026-08 7,498행.
- 배민 업로드 검증: 전체 8개 태스크 성공. 09-07 비교 52건에서 강동점 금액 차이 53,900원과 브랜드 매핑 누락이 남음. 태스크 실행 복구와 데이터 일치 여부를 구분하며, 기존 재수집 큐를 중복 생성하지 않음.
- 배민 룩백: 전체 3개 태스크 성공. 누락이 실측된 5개 날짜만 기존 중복 방지 규칙으로 큐에 추가. 수수료 재수집 트리거는 기존 19건을 재사용하고 신규 1건만 생성.
- 09:11 운영 상태: 실행 중 DAG 12개에서 6개, 실제 실행 태스크 5개, 과거 날짜 수집·재시도 대기 36건. 가용 메모리 약 8.6GB. 통합매출은 송파점 전체기간 재계산을 계속 저장 중이며 중복 실행하지 않음.
- 수수료 DAG: 09:15 전체 5개 태스크 성공. 수수료 parquet 31,322행(09:14:14 저장), 배달매출 parquet 41,070행(09:14:44), 매장비용 parquet 2,907행(09:15:06)을 직접 열어 확인.

## 이번 재처리 완료와 계속 진행 중인 범위

- 이번에 재개한 5개 DAG(OKPOS, 메뉴계층, 수수료, 배민 업로드 검증, 배민 룩백)는 모두 성공. 재개한 13개 태스크 완료. 성공했던 원천·알림 작업은 중복 실행하지 않음.
- 통합매출의 후속 schedule_guard 실행과 기존 배민 07-19 attempt_2, 09-07 attempt_1은 계속 실행·대기 중이다. 과거 실패 이력을 성공으로 바꾸지 않았으며, 해당 날짜의 모든 데이터가 복구됐다고 판정하지 않음.
- 배민 09-07 강동점의 금액 차이·브랜드 매핑 누락은 검증 결과에 남긴다. 메모리 오류 복구와 별개인 원천 데이터 차이이다.
- 09:14 조회 기준 승인 이후 새 최종 실패 0건. 실제 장기 계산과 과거 재수집이 끝날 때까지 대기 건수는 0이 되지 않을 수 있다.
