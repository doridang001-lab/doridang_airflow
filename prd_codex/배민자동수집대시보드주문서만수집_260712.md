# 배민 자동수집 대시보드 — 주문서만 수집

## Task
`doridang_collector_개발용` 크롬 확장의 **배민 전체 자동수집 대시보드**(`runner_baemin.html` / `runner_baemin.js`)는 현재 매장별로 5개 항목(`now`, `woori`=우가클, `change`=변경이력, `orders`=주문, `ad`=광고)을 수집한다. 이를 **주문서(orders)만** 수집하도록 축소하고 나머지 항목은 UI 컬럼까지 전부 제거한다.

## Project Conventions
- 대상 폴더: `C:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용` (크롬 MV3 확장, OneDrive)
- 언어/설명: 한글
- content script는 **현재 페이지 URL로 카테고리를 판별**한다 (`content/02_baemin.js` 24–27줄). 러너는 `COLLECT` 메시지에 카테고리를 싣지 않고, 이동한 URL로 수집 대상이 정해진다 → 러너에서 orders URL로만 이동하면 orders만 수집됨.
- 수동수집(popup/단축키/확장아이콘)과 대시보드 배치수집 분리 규칙을 깨지 않는다.
- 중복 백업 파일(`*-DESKTOP-*`, `runner_baemin.js.bak_*`)은 건드리지 않는다.
- 최소 범위 수정. 로딩 순서·content script 경계 유지.

## Files to Create / Modify
- 수정: `runner_baemin.js` (카테고리 정의, `processAccount`, `runBatch`)
- 수정: `runner_baemin.html` (테이블 `<thead>` 컬럼)
- 생성 파일 없음

## Implementation Steps

### 1. `runner_baemin.js` — 카테고리 축소 (현재 50–51줄)
```js
const categories = ['orders'];
const categoryLabels = { orders: '주문' };
```
`buildRows()`가 `categories` 배열로 배지 셀을 생성하므로 표에는 자동으로 `주문` 배지 1개만 남는다.

### 2. `runner_baemin.js` — `processAccount()` 단순화 (현재 716–802줄)
- `phase` 파라미터 및 `phaseLabel` / `activeCategories` 분기를 제거하고 단일 흐름으로 만든다. `activeCategories`는 `['orders']` 고정.
- 로그인 → 홈 이동 → `waitForBaeminShopSelectReady` → `selectTargetStore`까지는 **그대로 유지** (매장 선택 확정은 orders 수집에 필수).
- now 수집 블록(현재 763–775줄 `if (phase !== 'details')` 전체) **삭제**하고, `storeId`는 `selectTargetStore`가 반환한 `shopInfo.store_id`를 직접 사용.
- 상세 수집 블록(현재 778–788줄 `if (phase !== 'now')`)에서 woori/woori/change/ad 호출을 **삭제**하고 orders만 남긴다:
```js
if (!storeId) throw new Error('store_id 확보 실패');
const now = new Date();
const yesterday = ymd(new Date(now.getFullYear(), now.getMonth(), now.getDate() - 1));
await collectCategory(tabId, i, 'orders', urlFor('orders', storeId, yesterday));

const elapsed = fmt(Date.now() - t0);
setRow(i, { status: 'ok', note: '완료', current: false, elapsed });
return { status: 'ok' };
```
- 시그니처는 `async function processAccount(i, tabId)` 로 변경(`phase` 인자 삭제). 로그의 `phaseLabel` 부분은 제거하거나 고정 문자열로.
- catch 블록의 배지 실패 처리(현재 797줄)는 `activeCategories`(=['orders']) 기준 그대로 동작하도록 유지.

### 3. `runner_baemin.js` — `runBatch()` 단순화 (현재 812–884줄)
- 비-retry 모드의 "1단계 now / 2단계 details" 이중 루프(현재 844–863줄)를 제거하고, 모든 모드(all/top50/bottom50/retry)를 단일 루프로 통일:
```js
for (const i of indices) {
  if (stopRequested) break;
  await processAccount(i, tabId);
  updateMeta();
  await sleepWithStop(1000);
}
```
- now 실패 시 상세 건너뛰기 로직(현재 853–862줄) 삭제. `processAccount(i, tabId, 'all')` 등 phase 인자 호출부는 모두 `processAccount(i, tabId)`로 정리.

### 4. `runner_baemin.html` — 테이블 헤더 컬럼 축소 (현재 126–138줄 `<thead>`)
`now`, `우가클`, `변경이력`, `광고` `<th>` 를 제거하고 아래만 유지:
```html
<tr>
  <th style="width:36px">#</th>
  <th>매장 / 계정</th>
  <th style="width:64px">주문</th>
  <th style="width:54px">소요</th>
  <th>비고</th>
</tr>
```
제목/버튼(선택 수집·상위50%·하위50%·재시도·중단·배민 열기)은 유지.

### 5. (선택) 미사용 코드
`monthStr`, `previousMonth`, `collectCategory`의 `now`/`woori` 전용 분기는 미사용이 되지만 동작에 무해하므로 그대로 둔다. 무리한 정리로 다른 흐름을 깨지 않는다.

## Reference Code

### runner_baemin.js — 카테고리 정의 (현재 50–51줄)
```js
const categories = ['now', 'woori', 'change', 'orders', 'ad'];
const categoryLabels = { now:'now', woori:'우가클', change:'변경이력', orders:'주문', ad:'광고' };
```

### runner_baemin.js — buildRows() 배지 생성부 (현재 154–160줄, 수정 불필요·자동 반영)
```js
rowState = queue.map(() => ({ status:'wait', note:'', elapsed:'', cats:Object.fromEntries(categories.map(c => [c, 'wait'])) }));
queue.forEach((acc, i) => {
  const tr = document.createElement('tr');
  tr.id = `row-${i}`;
  tr.innerHTML = `<td>${i + 1}</td><td>${escapeHtml(acc.store)}<br><span style="color:#6a7488">${escapeHtml(acc.id)}</span></td>
    ${categories.map(c => `<td><span id="r-${c}-${i}" class="badge b-wait">대기</span></td>`).join('')}
    <td class="elapsed"></td><td class="note"></td>`;
```

### runner_baemin.js — urlFor() orders 분기 (현재 639–648줄, 그대로 사용)
```js
function urlFor(category, storeId, date) {
  const today = new Date();
  const yesterday = new Date(today.getFullYear(), today.getMonth(), today.getDate() - 1);
  const targetDate = date || ymd(yesterday);
  // ...
  if (category === 'orders') return `https://self.baemin.com/orders/history?startDate=${targetDate}&endDate=${targetDate}`;
  return HOME_URL;
}
```

### runner_baemin.js — 상세 수집 블록 (현재 778–788줄, 여기서 orders만 남김)
```js
if (phase !== 'now') {
  const now = new Date();
  const thisMonth = monthStr(now);
  const lastMonth = monthStr(previousMonth(now));
  await collectCategory(tabId, i, 'woori', urlFor('woori', storeId, `${thisMonth}-01`));
  await collectCategory(tabId, i, 'woori', urlFor('woori', storeId, `${lastMonth}-01`));
  await collectCategory(tabId, i, 'change', urlFor('change', storeId));
  const yesterday = ymd(new Date(now.getFullYear(), now.getMonth(), now.getDate() - 1));
  await collectCategory(tabId, i, 'orders', urlFor('orders', storeId, yesterday));
  await collectCategory(tabId, i, 'ad', urlFor('ad', storeId, yesterday));
}
```

### content/02_baemin.js — URL 기반 카테고리 판별 (현재 24–27줄, 참고·수정 불필요)
```js
if (location.href.includes('/orders/history')) category = 'orders';
else if (location.href.includes('/stat/marketing/woori-shop-click')) category = 'woori';
else if (location.href.includes('/stat/advertisement')) category = 'ad';
else if (location.href.includes('/history/change/shop')) category = 'change';
```

## Test Cases
1. [JS 문법] `node --check "C:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용\runner_baemin.js"` → 기대: 출력 없음(에러 없음)
2. [배지 정의] `runner_baemin.js`에서 `categories` 가 `['orders']` 단일 요소인지 확인 → 기대: 다른 카테고리 문자열 부재
3. [phase 잔재 없음] `runner_baemin.js`에서 `processAccount(` 호출부에 `'now'`/`'details'`/`'all'` 인자가 남아있지 않은지 확인 → 기대: `processAccount(i, tabId)` 형태만 존재
4. [HTML 컬럼] `runner_baemin.html` `<thead>`에 `now`/`우가클`/`변경이력`/`광고` 텍스트가 없는지 확인 → 기대: `#`, `매장 / 계정`, `주문`, `소요`, `비고` 5개 `<th>`만 존재
5. [브라우저 동작] `chrome://extensions/`에서 확장 리로드 후 `runner_baemin.html`을 열고 1~2개 매장만 선택해 `▶ 선택 수집` 실행 → 기대:
   - 표에 `주문` 배지 컬럼만 표시
   - 로그가 로그인 → 매장 선택 → `[주문 수집 요청] .../orders/history?...` → `주문 완료` 순으로 흐름
   - now/우가클/변경이력/광고 페이지로 이동 로그 없음
   - 주문 CSV 다운로드됨
6. [재시도] 실패 매장 발생 시 `↩ 재시도`가 주문만 재수집하는지 확인

## Verification Loop
```
LOOP until all PASS:
  1. Test Cases 1~4 (정적 검증) 실행
  2. FAIL 항목 → 원인 분석 → 코드 수정 → 재실행
  3. 정적 PASS 후 Test Case 5~6 (브라우저) 안내
종료 조건: 정적 Test Cases 전체 PASS + Constraints 위반 없음
```

## Constraints
- OneDrive 폴더 수정이므로 사용자 승인 후 진행(이미 승인됨).
- `selectTargetStore` → `waitForBaeminShopSelectReady` / `waitForBaeminDashboardReady` 는 매장 선택 확정용이므로 **삭제하지 않는다** (NOW 카드 렌더 확인은 매장 검증이지 수집이 아님).
- `store_id`는 반드시 `selectTargetStore` 반환값에서 확보. now 수집 제거로 인해 storeId 확보가 누락되지 않게 한다.
- 중복 백업 파일(`*-DESKTOP-*`, `runner_baemin.js.bak_*`) 수정 금지.
- 수동수집/대시보드 배치수집 분리, content script 로딩 순서(`CONTENT_FILES`)를 깨지 않는다.
- 최소 diff. woori/change/ad 관련 헬퍼를 무리하게 삭제하다 다른 흐름을 깨지 않는다(미사용 방치 허용).

## Do Not Ask — Decide Yourself
- 파일이 이미 존재하면: 수정(덮어쓰기).
- 코드 스타일: 기존 `runner_baemin.js`와 동일(2-space, 세미콜론, 화살표 함수).
- 미사용 헬퍼(`monthStr`/`previousMonth`): 삭제 여부 애매하면 그대로 둔다.
- 주석: 최소화(WHY만).
