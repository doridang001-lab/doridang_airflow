# 쿠팡이츠 "다음 페이지 없음/이동 실패" 경고 원인 수정

## Final Plan
- `_clickNextOrderPage()`는 boolean 대신 `{ moved, stalled }` 객체만 반환한다. 버튼 없음/숨김/비활성은 `stalled: false`로 진짜 마지막 페이지로 보고, 버튼 클릭 후 목표 페이지 미도달은 `stalled: true`로 이동 정체로 본다.
- `_collectOrdersPages` 호출부는 이동 정체(`!moved && stalled`)일 때 팝업 정리와 800~1500ms 대기 후 1회만 재시도한다.
- 재시도 후에도 이동 정체면 `reachedEnd`를 세우지 않고 `_markOrdersTransientError()`, `hasError = true`, `failureNote = '페이지 이동 정체 - 재조회 없이 백필 이월'`로 종료해 기존 잔여 구간 재수집 조건을 살린다.
- 진짜 마지막 페이지(`!moved && !stalled`)에서만 기존처럼 `다음 페이지 없음/이동 실패` 로그와 `reachedEnd = true`를 유지한다.
- 작업 기록은 `C:\airflow\prd_codex\update_log.md`에 누적하고, `AGENTS.md`에 이 규칙을 추가한다.

## Risk Review
- 반환 타입 변경 위험: `_clickNextOrderPage()` 호출부가 객체 분기를 사용하도록 바뀌었는지 검색으로 확인한다.
- UI 판정 위험: 쿠팡 UI가 일시적으로 next 버튼을 비활성화하는 경우는 여전히 마지막 페이지로 볼 수 있으므로 수동 수집 로그로 관찰한다.
- 무한 재시도 위험: 재시도는 호출부에서 1회만 수행한다.
- 백필 차단 위험: 정체 케이스에서는 `reachedEnd = true`를 세우지 않아 `remaining` 계산 경로를 막지 않는다.
- OneDrive 위험: 개발용 OneDrive 파일은 승인 전 수정하지 않으며, 승인 후 저장소 파일과 동일 내용으로만 반영한다.
- 검증 한계: `node --check`와 정적 검색은 문법/분기만 보장하므로 Chrome 확장 재로드 후 실사용 수집 1회가 필요하다.

## Task
쿠팡이츠 자동수집(크롬 확장) 배치 로그(`(1).txt`, `(2).txt`, 2026-07-16)에서 `다음 페이지 없음/이동 실패` 경고가 다른 경고 유형보다 압도적으로 자주 반복된다. 원인 조사 결과, `content/03_coupangeats.js`의 `_clickNextOrderPage()`가 (A) 진짜 마지막 페이지와 (B) 이동 정체(next 버튼 클릭했지만 SPA가 ~4.5초 폴링 동안 페이지 전환을 안 함)를 구분하지 못하고 둘 다 `false`를 반환하며, 호출부는 무조건 `reachedEnd = true`로 세팅한다. 이 플래그가 서있으면 이후 검증 불일치(주문수/매출 mismatch) 시 잔여 구간 재수집 경로(`remaining = (!reachedEnd && bounds) ? ... : null`)가 강제로 차단되어, (B) 정체 상황에서도 재시도 없이 부분 수집인 채로 `warning` 상태 종료된다. 이 두 상황을 구분해 (B)는 짧은 재시도 여지를 주고, 그래도 실패하면 `reachedEnd`를 세우지 않아 기존 검증-불일치 재수집 경로가 정상 작동하도록 고친다.

## Project Conventions
- logging: 콘솔/모달 로그는 `Utils.updateProgressModal({ debug: ... })` 패턴 사용 (기존 파일 전체가 이 패턴)
- 크롬 확장 코드이므로 Python 컨벤션(logger, paths.py 등) 미적용 — 순수 vanilla JS, 기존 함수 스타일(화살표 아님, `async`/`await`, `this.` 메서드) 그대로 따를 것
- 파일 위치: `C:\airflow\coupang_extension_build\content\03_coupangeats.js` 와 `C:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용\content\03_coupangeats.js` — 두 경로는 동일 파일(동일 크기/mtime, 하드링크 추정)로 동기화되어 있음. **수정 전에 두 파일이 여전히 동일한지(diff) 먼저 확인**하고, 다르면 양쪽 다 반영.
- 기존 에러 처리 관례: "미완료/정체"는 `hasError = true; failureNote = '...'; break;` 로 표시 (3478~3484행 패턴). `reachedEnd`는 오직 "진짜 마지막 페이지"에만 세팅되어야 한다는 것이 이번 수정의 핵심 불변조건.
- 재시도는 무한루프 금지 — 기존 코드도 폴링 15회(180~300ms) 제한, 딜레이 뒤 1회만 추가 재시도.

## Files to Create / Modify
- `C:\airflow\coupang_extension_build\content\03_coupangeats.js` (수정)
- `C:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용\content\03_coupangeats.js` (동기화 확인 대상. OneDrive 수정 승인을 받은 뒤에만 동일 수정)
- `C:\airflow\prd_codex\update_log.md` (작업 업데이트 기록 누적)
- `C:\airflow\AGENTS.md` (`update_log.md` 누적 규칙 추가)

## Implementation Steps

### 1. `_clickNextOrderPage()` 반환값을 boolean → 상태 객체로 변경
파일: `content/03_coupangeats.js`, 현재 1565~1602행 (아래 Reference Code 참고)

- 버튼 없음/hidden/disabled (현재 1581~1584행, `return false`) → `return { moved: false, stalled: false };` (진짜 끝)
- 폴링 타임아웃 (현재 1599~1601행, `return moved;`) → `return { moved, stalled: !moved };`
- 페이지 번호 직접 클릭 성공(1569~1572행) / 폴링 중 이동 확인(1590~1597행) → `return { moved: true, stalled: false };`
- 기존 `Utils.updateProgressModal({ debug: ... })` 로그 문구는 그대로 유지, 반환 타입만 변경.

### 2. 호출부(`_collectOrdersPages` 내부)에서 stall 시 1회 재시도 추가
파일: `content/03_coupangeats.js`, 현재 3488~3496행 부근 (아래 Reference Code 참고)

- `const hasNext = await this._clickNextOrderPage();` 를 `let nextResult = await this._clickNextOrderPage();` 로 변경
- `!nextResult.moved && nextResult.stalled` 인 경우:
  1. `await this._closeErrorPopups();`
  2. `await this._randomDelay(800, 1500);`
  3. `nextResult = await this._clickNextOrderPage();` (재시도, 총 2회 시도로 제한 — 추가 루프 금지)
- 최종 `!nextResult.moved` 분기:
  - `nextResult.stalled === false` (진짜 끝): 기존과 동일하게
    ```js
    Utils.updateProgressModal({ debug: `다음 페이지 없음/이동 실패 - ${this._ordersDiag({ currentPage, totalRows: allRows.length, ids: collectedOrderIds.size })}` });
    console.log('[쿠팡이츠] 마지막 페이지 도달');
    reachedEnd = true;
    break;
    ```
  - `nextResult.stalled === true` (재시도까지 실패한 정체): `reachedEnd`를 **세우지 않고**, 기존 3478~3484행 패턴과 동일하게 처리
    ```js
    this._markOrdersTransientError();
    hasError = true;
    failureNote = '페이지 이동 정체 - 재조회 없이 백필 이월';
    Utils.updateProgressModal({ debug: `다음 페이지 이동 정체(재시도 실패) - ${this._ordersDiag({ currentPage, totalRows: allRows.length, ids: collectedOrderIds.size })}` });
    break;
    ```
    (`_markOrdersTransientError()` / `_hadRecentOrdersTransientError()` 는 이미 존재하는 함수 — 3479, 4052행에서 이미 사용 중)
- `nextResult.moved === true`인 경우는 기존 로직(3498행 이후, `_waitForPageLoad` 등) 그대로 이어서 실행.

### 3. 4104행 `remaining` 계산 조건은 수정 불필요
`const remaining = (!reachedEnd && bounds) ? this._calcRemainingRange(bounds, originalRange) : null;` — `reachedEnd`가 (B) 케이스에서 더 이상 세팅되지 않으므로 이 줄은 그대로 두면 정체 케이스에 대해 자동으로 잔여 구간 재수집을 허용하게 됨. **이 줄은 절대 건드리지 말 것.**

## Reference Code
### content/03_coupangeats.js — `_clickNextOrderPage()` (현재, 1565~1602행)
```js
  async _clickNextOrderPage() {
    const currentPage = this._getCurrentPage();
    const targetPage = currentPage + 1;
    Utils.updateProgressModal({ debug: `페이지 이동 시작 - ${this._ordersDiag({ targetPage })}` });
    if (this._clickPageNumber(targetPage)) {
      Utils.updateProgressModal({ debug: `페이지 번호 직접 클릭 - ${this._ordersDiag({ targetPage })}` });
      return true;
    }

    const nextBtn = document.querySelector('.merchant-pagination button[data-at="next-btn"], button[data-at="next-btn"]')
      || Array.from(document.querySelectorAll('.merchant-pagination button')).find(btn => {
        const label = `${btn.getAttribute('aria-label') || ''} ${btn.getAttribute('title') || ''} ${btn.textContent || ''}`.trim();
        return /next|다음|›|>|»/i.test(label);
      });
    const isHidden = !nextBtn || nextBtn.classList.contains('hide-btn');
    const isDisabled = nextBtn?.disabled || nextBtn?.getAttribute('aria-disabled') === 'true';
    if (!nextBtn || isHidden || isDisabled) {
      Utils.updateProgressModal({ debug: `다음 페이지 버튼 사용 불가 - ${this._ordersDiag({ targetPage, hasNextBtn: !!nextBtn, hidden: !!isHidden, disabled: !!isDisabled })}` });
      return false;
    }

    nextBtn.click();
    Utils.updateProgressModal({ debug: `${targetPage}페이지 번호창 이동 중 - ${this._ordersDiag({ targetPage })}` });
    for (let i = 0; i < 15 && !this._stopFlag; i++) {
      await this._randomDelay(180, 300);
      if (this._getCurrentPage() === targetPage) {
        Utils.updateProgressModal({ debug: `페이지 이동 확인 - ${this._ordersDiag({ targetPage, poll: i + 1 })}` });
        return true;
      }
      if (this._clickPageNumber(targetPage)) {
        Utils.updateProgressModal({ debug: `번호창에서 목표 페이지 클릭 - ${this._ordersDiag({ targetPage, poll: i + 1 })}` });
        return true;
      }
    }
    const moved = this._getCurrentPage() === targetPage;
    Utils.updateProgressModal({ debug: `페이지 이동 결과 - ${this._ordersDiag({ targetPage, moved })}` });
    return moved;
  },
```

### content/03_coupangeats.js — 호출부 `_collectOrdersPages` 내부 (현재, 3478~3502행)
```js
      if (!pageComplete) {
        this._markOrdersTransientError();
        hasError = true;
        failureNote = '수집 중 끊김 - 주문 상세 미완료';
        Utils.updateProgressModal({ debug: `페이지 미완료 주문: ${missingOrderIds.join(', ')} - 재조회 없이 백필 이월` });
        break;
      }

      await this._closeErrorPopups();

      const prevFirstOrderId = this._getFirstOrderId();
      Utils.updateProgressModal({ debug: `다음 페이지 이동 전 - ${this._ordersDiag({ currentPage, prevFirst: prevFirstOrderId || '-', totalRows: allRows.length, ids: collectedOrderIds.size })}` });
      const hasNext = await this._clickNextOrderPage();
      if (!hasNext) {
        Utils.updateProgressModal({ debug: `다음 페이지 없음/이동 실패 - ${this._ordersDiag({ currentPage, totalRows: allRows.length, ids: collectedOrderIds.size })}` });
        console.log('[쿠팡이츠] 마지막 페이지 도달');
        reachedEnd = true;
        break;
      }

      Utils.updateProgressModal({
        currentPage: currentPage + 1,
        rowCount: allRows.length,
        message: `${currentPage + 1}페이지 로딩 대기...`
      });
```

### content/03_coupangeats.js — 검증 불일치 잔여구간 재수집 조건 (참고용, 4102~4114행, 수정 금지)
```js
      const bounds = this._getOrderDateBoundaries(allRows, originalRange);
      const remaining = (!reachedEnd && bounds) ? this._calcRemainingRange(bounds, originalRange) : null;
      const madeProgress = uniqueAllCount > (this._resumeNoProgressBase || 0);
      if (remaining && madeProgress) {
        this._resumeNoProgressBase = uniqueAllCount;
        const waitMs = 90000 + Math.floor(Math.random() * 60001);
        Utils.updateProgressModal({ debug: `검증 불일치 - ${Math.ceil(waitMs / 1000)}초 대기 후 남은 구간 이어수집 | remaining=${new Date(remaining.startMs).toLocaleString()}~${new Date(remaining.endMs).toLocaleString()}` });
        await new Promise(r => setTimeout(r, waitMs));
        await this._resumeOrdersRange(remaining.startMs, remaining.endMs, shopInfo, _resumeDepth);
        return true;
      }
```

## Test Cases
1. [문법 오류 없음] 수정 후 `node --check "C:\airflow\coupang_extension_build\content\03_coupangeats.js"` → 기대: exit code 0 (문법 에러 없음)
2. [반환 타입 일관성] `_clickNextOrderPage`의 모든 `return` 경로가 `{ moved, stalled }` 형태 객체를 반환하는지 grep으로 확인: `grep -n "return " content/03_coupangeats.js` 해당 함수 범위 내에서 boolean 단독 `return true/false/moved` 잔존 여부 확인 → 기대: 잔존 없음
3. [호출부 반영] `_collectOrdersPages` 내 `hasNext` 변수명이 `nextResult`로 바뀌고 `.moved`/`.stalled` 프로퍼티로 분기하는지 확인 → 기대: `if (!hasNext)` 형태의 boolean 직접 비교 잔존 없음
4. [reachedEnd 불변조건] `reachedEnd = true;` 대입이 발생하는 모든 위치가 "진짜 끝(stalled===false)" 분기에만 있는지 확인 (정체 분기에는 `hasError = true; failureNote = ...;`만 있어야 함) → 기대: 정체 분기에 `reachedEnd = true` 없음
5. [두 파일 동기화] `coupang_extension_build`와 OneDrive 개발용 폴더의 `03_coupangeats.js` 해시 비교 → 기대: OneDrive 수정 승인 전에는 차이 발생 가능, 승인 후에는 두 파일 동일
6. [실사용 검증 — 수동] 확장 프로그램을 크롬에 재로드 후, 로그에 나온 것과 유사한 페이지 수 많은 매장(9페이지, totalRows=495 유사 규모)으로 수동 수집 1회 실행 → 진행 모달 debug 로그에서 "다음 페이지 이동 정체(재시도 실패)"와 "다음 페이지 없음/이동 실패"가 서로 다른 상황에 대해 구분되어 찍히는지 확인. 배치 정상 종료 시 여전히 "다음 페이지 없음/이동 실패"(진짜 끝)로 깔끔히 종료되는지 회귀 확인.

## Verification Loop
구현 완료 후 아래 루프를 **모든 Test Cases PASS까지** 반복한다.

```
LOOP until all PASS:
  1. Test Cases 순서대로 실행
  2. FAIL 항목 → 원인 분석
  3. 코드 수정
  4. 전체 Test Cases 재실행
종료 조건: 전체 PASS + Constraints 위반 없음
```

## Constraints
- `_clickNextOrderPage()`의 재시도는 호출부에서 최대 1회 추가(총 2회 시도)로 제한 — 무한 재시도/루프 금지
- 4104행(`remaining` 계산 조건문)은 절대 수정하지 말 것 — `reachedEnd`를 정체 케이스에서 세우지 않는 것만으로 자동 해결됨
- 기존 로그 메시지 포맷(`Utils.updateProgressModal({ debug: ... })`)과 `_ordersDiag()` 호출 패턴을 그대로 유지 — 새 로그 문구만 추가
- `coupang_extension_build`와 OneDrive 개발용 폴더 파일이 동일 파일(하드링크)일 가능성이 높으므로, 실제로 별도 파일인지 먼저 확인 후 필요시에만 양쪽 수정 (불필요한 중복 작업 금지)
- storeDeadlineExceeded 관련 로직(4062행 이하 `_resumeOrdersCurrentPage` 재시도 depth 10 로직)은 이번 수정과 무관 — 건드리지 말 것

## Do Not Ask — Decide Yourself
- 파일이 이미 존재하면: 덮어쓰기
- import가 모호하면: Reference Code의 패턴 따라가기
- 타입 힌트 여부: 해당 없음 (vanilla JS)
- 주석 여부: 최소화 (WHY만, WHAT 설명 금지) — 예: `_clickNextOrderPage` 반환 객체에 "moved/stalled 구분 이유"는 주석 없이 필드명으로 자명하게
- 변수명·함수명 스타일: camelCase, 기존 파일과 동일하게 (`nextResult`, `moved`, `stalled`)
