# 날짜 범위 이어수집 (Date Range Resume)

## Task
수동 주문 수집 도중 창을 닫거나 이동해 중단됐을 때, 미수집 날짜 구간을 자동으로 계산해
"나머지 날짜 이어수집" 버튼을 제공한다.
예) 06-01~06-15 수집 → 06-10 근방 페이지에서 중단 → 버튼 클릭 시 06-01~06-09 자동 재조회 후 수집.

## Files to Create / Modify
- `C:\airflow\coupang_extension_build\content\03_coupangeats.js` — 핵심 수정 대상
  - `_collectOrdersManualWithCheckpoints` 함수 내부 수정
  - `_getOrderDateBoundaries(rows)` 헬퍼 메서드 추가
  - `_showResumeRangeModal(remainingStartMs, remainingEndMs, shopInfo)` 메서드 추가

## Implementation Steps

### 1. `_collectOrdersManualWithCheckpoints` — 시작 전 날짜 범위 저장

`_collectOrdersManualWithCheckpoints(shopInfo)` 함수 맨 앞(기존 코드 위)에 추가:

```js
async _collectOrdersManualWithCheckpoints(shopInfo, _resumeDepth = 0) {
  const CHECKPOINT_EVERY = 100;
  let partialIndex = 0;
  let lastSavedUniqueCount = 0;

  // ▶ 원본 날짜 범위 저장 (재개 시 필요)
  const originalRange = this._readOrdersDateRange(); // { start: epochMsStr, end: epochMsStr }

  Utils.showProgressModal('쿠팡이츠 수집', '수집 시작...');
  // ... 기존 seed / _collectOrdersPages 코드 유지 ...
```

`_resumeDepth` 파라미터: 재귀 재개 횟수 추적. 1회 초과 시 재개 시도 안 함.

### 2. 중단 시 미수집 범위 계산 및 버튼 제공

기존 stopFlag 처리 코드:
```js
if (this._stopFlag && partialIndex > 0) {
  Utils.showSuccessModal('수집 중단', `${partialIndex}개 파일 저장됨`, { status: 'warning' });
  return true;
}
```

이 블록을 아래로 교체:
```js
if (this._stopFlag && partialIndex > 0) {
  // 미수집 구간 계산
  const bounds = this._getOrderDateBoundaries(allRows);
  const remaining = bounds ? this._calcRemainingRange(bounds, originalRange) : null;

  if (remaining && _resumeDepth < 1) {
    // "이어수집" 버튼 포함 모달 표시
    await this._showResumeRangeModal(remaining.startMs, remaining.endMs, shopInfo, originalRange, _resumeDepth);
  } else {
    Utils.showSuccessModal('수집 중단', `${partialIndex}개 파일 저장됨`, { status: 'warning' });
  }
  return true;
}
```

### 3. `_getOrderDateBoundaries(rows)` 헬퍼 추가

Sites['coupangeats'] 객체 안에 메서드로 추가 (기존 메서드들 뒤에 삽입):

```js
_getOrderDateBoundaries(rows) {
  // order_date 필드 예: "2026.6.10 15:30" 또는 "2026.06.10 15:30"
  const toMs = (dateStr) => {
    if (!dateStr) return null;
    const part = dateStr.split(' ')[0]; // "2026.6.10"
    const [y, m, d] = part.split('.').map(Number);
    if (!y || !m || !d) return null;
    return new Date(y, m - 1, d).getTime();
  };

  const msList = rows.map(r => toMs(r.order_date)).filter(v => v !== null && !isNaN(v));
  if (!msList.length) return null;

  return {
    minMs: Math.min(...msList),
    maxMs: Math.max(...msList),
    // 페이지네이션 방향: 첫 row vs 마지막 row 비교
    isDescending: (() => {
      const first = toMs(rows[0]?.order_date);
      const last  = toMs(rows[rows.length - 1]?.order_date);
      return first !== null && last !== null ? first >= last : true;
    })()
  };
},

_calcRemainingRange(bounds, originalRange) {
  const origStartMs = parseInt(originalRange.start, 10);
  const origEndMs   = parseInt(originalRange.end, 10);
  if (isNaN(origStartMs) || isNaN(origEndMs)) return null;

  const ONE_DAY = 86400000;

  if (bounds.isDescending) {
    // 최신순: 수집된 가장 오래된 날짜 이전이 미수집
    const remainingEndMs = bounds.minMs - ONE_DAY;
    if (remainingEndMs < origStartMs) return null; // 이미 다 수집됨
    return { startMs: origStartMs, endMs: remainingEndMs };
  } else {
    // 오래된순: 수집된 가장 최신 날짜 이후가 미수집
    const remainingStartMs = bounds.maxMs + ONE_DAY;
    if (remainingStartMs > origEndMs) return null;
    return { startMs: remainingStartMs, endMs: origEndMs };
  }
},
```

### 4. `_showResumeRangeModal(...)` 추가

```js
async _showResumeRangeModal(remainingStartMs, remainingEndMs, shopInfo, originalRange, resumeDepth) {
  const fmtDate = (ms) => {
    const d = new Date(ms);
    return `${d.getFullYear()}.${String(d.getMonth()+1).padStart(2,'0')}.${String(d.getDate()).padStart(2,'0')}`;
  };
  const label = `${fmtDate(remainingStartMs)} ~ ${fmtDate(remainingEndMs)}`;

  return new Promise((resolve) => {
    // 기존 성공 모달 오버레이 재활용 패턴
    const overlay = document.createElement('div');
    overlay.style.cssText = 'position:fixed;top:0;left:0;right:0;bottom:0;background:rgba(0,0,0,0.6);z-index:2147483647;display:flex;align-items:center;justify-content:center;';

    const modal = document.createElement('div');
    modal.style.cssText = 'background:#fff;border-radius:16px;padding:32px;min-width:400px;max-width:480px;box-shadow:0 20px 60px rgba(0,0,0,0.3);text-align:center;';
    modal.innerHTML = `
      <div style="font-size:2rem;margin-bottom:8px;">⚡</div>
      <div style="font-size:1.1rem;font-weight:700;margin-bottom:8px;">수집 중단</div>
      <div style="color:#555;margin-bottom:20px;">수집된 파일이 저장됐습니다.<br>
        <strong style="color:#e85d04;">미수집 구간: ${label}</strong>
      </div>
      <button id="__ce_resume_btn" style="background:#e85d04;color:#fff;border:none;border-radius:8px;padding:10px 20px;font-size:0.95rem;cursor:pointer;margin-right:8px;">
        이어수집 (${label})
      </button>
      <button id="__ce_close_btn" style="background:#e0e0e0;border:none;border-radius:8px;padding:10px 16px;font-size:0.95rem;cursor:pointer;">
        닫기
      </button>
    `;
    overlay.appendChild(modal);
    document.body.appendChild(overlay);

    modal.querySelector('#__ce_close_btn').onclick = () => {
      overlay.remove();
      resolve(false);
    };

    modal.querySelector('#__ce_resume_btn').onclick = async () => {
      overlay.remove();
      // 날짜 범위 재적용 후 재수집
      await this._applyOrdersDateRange(remainingStartMs, remainingEndMs);
      const prevCount = this._getExpectedOrderCount();
      await this._clickOrdersSearchButton();
      await this._waitForOrdersCountChange(prevCount, 8000);
      await this._waitForOrdersDataReady(8000);
      this._stopFlag = false; // 재개 전 플래그 초기화
      await this._collectOrdersManualWithCheckpoints(shopInfo, resumeDepth + 1);
      resolve(true);
    };
  });
},
```

## Reference Code
### `_readOrdersDateRange` / `_applyOrdersDateRange`
```js
// 현재 UI 날짜 피커에서 epoch ms 읽기
_readOrdersDateRange() {
  const s = document.querySelector('input[name="startDate"]');
  const e = document.querySelector('input[name="endDate"]');
  return { start: s?.value || '', end: e?.value || '' };
},

// hidden input + 보이는 라벨 동시 갱신 (React 우회)
async _applyOrdersDateRange(startMs, endMs) {
  const nativeSetter = Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype, 'value')?.set;
  const setHidden = (name, val) => {
    const hidden = document.querySelector(`input[name="${name}"]`);
    if (!hidden || !val) return;
    if (nativeSetter) nativeSetter.call(hidden, String(val)); else hidden.value = String(val);
    hidden.setAttribute('value', String(val));
    hidden.dispatchEvent(new Event('input', { bubbles: true }));
    hidden.dispatchEvent(new Event('change', { bubbles: true }));
  };
  setHidden('startDate', startMs);
  setHidden('endDate', endMs);
  const fmt = (ms) => { const d = new Date(parseInt(ms,10)); return `${d.getFullYear()}.${d.getMonth()+1}.${d.getDate()}`; };
  const inputs = [...document.querySelectorAll('div[data-testid="input"]')].filter(el => el.offsetParent !== null);
  if (inputs[0] && startMs) { inputs[0].textContent = fmt(startMs); inputs[0].dispatchEvent(new Event('input',{bubbles:true})); }
  if (inputs[1] && endMs)   { inputs[1].textContent = fmt(endMs);   inputs[1].dispatchEvent(new Event('input',{bubbles:true})); }
  await this._randomDelay(300, 500);
},
```

### `_collectOrdersManualWithCheckpoints` (현재 전체)
```js
async _collectOrdersManualWithCheckpoints(shopInfo) {
  const CHECKPOINT_EVERY = 100;
  let partialIndex = 0;
  let lastSavedUniqueCount = 0;

  Utils.showProgressModal('쿠팡이츠 수집', '수집 시작...');

  const seed = {
    checkpointEvery: CHECKPOINT_EVERY,
    checkpointFrom: 0,
    onCheckpoint: async (rows, uniqueCount) => {
      partialIndex++;
      lastSavedUniqueCount = uniqueCount;
      await this._downloadOrdersPartial(rows, shopInfo);
    },
  };

  const { allRows, reloaded } = await this._collectOrdersPages(shopInfo, seed);
  if (reloaded) return false;

  if (!allRows.length) {
    Utils.showSuccessModal('데이터 없음', '추출할 주문이 없습니다.', { status: 'error' });
    return false;
  }

  const uniqueCount = new Set(allRows.map(r => r.order_id).filter(Boolean)).size;
  if (uniqueCount > lastSavedUniqueCount) {
    partialIndex++;
    await this._downloadOrdersPartial(allRows, shopInfo);
  }

  if (this._stopFlag && partialIndex > 0) {
    Utils.showSuccessModal('수집 중단', `${partialIndex}개 파일 저장됨`, { status: 'warning' });
    return true;
  }

  Utils.showSuccessModal('수집 완료', `총 ${partialIndex}개 파일 저장 완료`, { status: 'ok' });
  return true;
},
```

## Test Cases
1. **ESC 중단 → 이어수집 버튼 표시**
   - https://store.coupangeats.com/merchant/management/orders/881023 열기
   - 날짜 범위 길게 설정 (예: 06-01~06-15)
   - 단축키로 수집 시작 → 2~3 페이지 후 ESC
   - 기대: "이어수집 (YYYY.MM.DD ~ YYYY.MM.DD)" 버튼 포함 모달 표시

2. **이어수집 버튼 클릭 → 날짜 피커 재설정 확인**
   - 버튼 클릭 후 Coupang UI 날짜 필터가 남은 구간으로 변경됐는지 확인
   - 기대: 재수집 시작 + progress 모달 다시 표시

3. **최신순/오래된순 방향 자동 감지**
   - `_getOrderDateBoundaries([{order_date:'2026.6.15 10:00'}, {order_date:'2026.6.10 09:00'}])` → `isDescending: true`
   - `_getOrderDateBoundaries([{order_date:'2026.6.01 10:00'}, {order_date:'2026.6.10 09:00'}])` → `isDescending: false`

4. **재귀 1회 제한**
   - 이어수집 중 재중단 시: "이어수집" 버튼 없이 "수집 중단" 기본 모달만 표시

5. **빌드 복사 후 Chrome 리로드**
   ```
   xcopy /Y "C:\airflow\coupang_extension_build\content\03_coupangeats.js" "C:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용\content\"
   ```
   Chrome 확장 관리 → 새로고침 → 테스트

## Verification Loop
```
LOOP until all PASS:
  1. Test Cases 1~4 순서대로 실행
  2. FAIL → 원인 분석 (order_date 파싱 형식, 날짜 경계 계산 오류 등)
  3. 코드 수정
  4. 전체 재실행
종료 조건: 이어수집 버튼 표시 + 날짜 피커 재설정 + 재귀 1회 제한 전부 PASS
```

## Constraints
- `_applyOrdersDateRange` 시그니처 변경 금지 — epoch ms 문자열 그대로 전달
- 재귀 깊이 `_resumeDepth` 1 초과 시 이어수집 버튼 표시 금지 (무한 루프 방지)
- `this._stopFlag = false` 재설정은 이어수집 버튼 클릭 직전에만 수행
- 기존 `Utils.showSuccessModal` 완전 수집 경로는 변경하지 않는다
- `order_date` 파싱: `"YYYY.M.D HH:mm"` 또는 `"YYYY.MM.DD HH:mm"` 둘 다 대응

## Do Not Ask — Decide Yourself
- 파일이 이미 존재하면: 덮어쓰기
- import가 모호하면: Reference Code의 패턴 따라가기
- 타입 힌트 여부: JS 파일이므로 JSDoc 불필요
- 주석 여부: 최소화 (WHY만)
- `order_date` 포맷 불확실 시: `.split(' ')[0].split('.')` 로 파싱, 예외 시 `null` 반환
