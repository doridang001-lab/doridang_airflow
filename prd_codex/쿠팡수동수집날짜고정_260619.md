# 쿠팡수동수집날짜고정

## Task
쿠팡이츠 주문서 수동 수집 시 드롭다운 순회·어제 날짜 강제 설정·체크포인트로 인한 수집 스킵 문제를 수정한다.
수동(`source === 'manual'`) 트리거 시 드롭다운/날짜 개입 없이 현재 페이지 매장+날짜 그대로 전 페이지를 수집한다.

## Files to Modify

- `C:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용\content\03_coupangeats.js`

수정 함수: `_collectOrdersMultiStore(opts = {})` (라인 1831~)

## Implementation Steps

### 1. `_collectOrdersMultiStore` 함수 최상단에 수동 수집 early return 블록 삽입

**현재 코드** (라인 1831~1834):
```javascript
async _collectOrdersMultiStore(opts = {}) {
    let matchingStores = await this._getOrdersDropdownStores();
    matchingStores = this._filterTargetStores(matchingStores, opts);

    if (matchingStores.length === 0) {
```

**변경 후** (기존 코드 앞에 삽입):
```javascript
async _collectOrdersMultiStore(opts = {}) {
    // 수동 수집: 드롭다운/체크포인트/날짜 개입 없이 현재 페이지 그대로 수집
    if (opts.source === 'manual') {
      const shopInfo = this._getShopInfo();
      Utils.showProgressModal('쿠팡이츠 수집', '현재 날짜 그대로 수집 중...');
      Utils.updateProgressModal({ debug: '📅 수동 수집: 날짜·드롭다운 설정 무시' });
      const prevCount = this._getExpectedOrderCount();
      await this._clickOrdersSearchButton();
      await this._waitForOrdersCountChange(prevCount, 8000);
      await this._waitForOrdersDataReady(8000);
      await this._collectOrdersWithValidation(shopInfo, false);
      if (window.top === window.self) {
        try {
          chrome.runtime.sendMessage({
            type: 'MULTISTORE_COMPLETE',
            payload: {
              category: 'orders',
              storeCount: 1,
              completedCount: this._stopFlag ? 0 : 1,
              stopped: !!this._stopFlag,
              storeResults: [{ storeName: shopInfo.store_name || '현재 매장', completed: !this._stopFlag, status: this._stopFlag ? 'warn' : 'ok' }]
            }
          }).catch(() => {});
        } catch (_) {}
      }
      return;
    }

    let matchingStores = await this._getOrdersDropdownStores();
    matchingStores = this._filterTargetStores(matchingStores, opts);

    if (matchingStores.length === 0) {
```

이후 기존 코드는 **그대로 유지** (배치 모드 동작 변경 없음).

## Test Cases

1. **수동 수집 진행 확인**
   - `https://store.coupangeats.com/merchant/management/orders/1007916` 직접 접속
   - 날짜 범위를 원하는 기간으로 UI에서 설정 (예: 최근 2주)
   - 단축키(`/`) 입력 → 수집 시작
   - 기대: 진행 모달에 `📅 수동 수집: 날짜·드롭다운 설정 무시` 텍스트 표시

2. **드롭다운 미개입 확인**
   - 기대: 수집 중 드롭다운 클릭 없음, 현재 페이지 매장 단독 수집

3. **설정 날짜 유지 확인**
   - 기대: UI의 날짜 필터가 어제로 바뀌지 않고 사용자가 설정한 날짜 범위 전체 페이지 수집 완료

4. **배치 모드 회귀 확인**
   - runner.html에서 배치 실행 → 기존대로 어제 날짜 설정 + 전 매장 순회 동작 확인

## Verification Loop

```
LOOP until all PASS:
  1. Test Cases 순서대로 실행
  2. FAIL 항목 → 원인 분석
  3. 코드 수정
  4. 전체 Test Cases 재실행
종료 조건: 전체 PASS + Constraints 위반 없음
```

## Constraints

- `source === 'batch'` 경로 **절대 변경 금지** — 드롭다운 순회·어제 날짜·체크포인트 로직 그대로
- `_collectOrdersWithValidation`, `_clickOrdersSearchButton` 등 기존 함수 시그니처 변경 금지
- early return 블록 이후 기존 코드(`let matchingStores = ...`)는 그대로 유지
- 파일 인코딩: UTF-8 BOM 없음 유지

## Do Not Ask — Decide Yourself

- 파일이 이미 존재하면: 덮어쓰기
- import가 모호하면: Reference Code의 패턴 따라가기
- 주석 여부: 최소화 (WHY만, WHAT 설명 금지)
- 변수명·함수명 스타일: camelCase, 기존 파일과 동일하게
