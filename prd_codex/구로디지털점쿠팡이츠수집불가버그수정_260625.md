# 구로디지털점 쿠팡이츠 수집 불가 버그 수정

## Task

`도리당 구로디지털점 (mika2969)` 계정이 orders 0/0개, CMG 드롭다운 0개로 5번 재시도해도 수집 실패한다. 원인은 `accounts.js` 등록명(`도리당 구로디지털점`)과 쿠팡이츠 HTML 실제명(`닭도리탕 전문 도리당 구로디지털단지점`) 불일치. `accounts.js` 2줄 수정 + 빌드 JS 파일 OneDrive 복사로 해결한다.

**매칭 실패 원인**:
```
target  = "도리당구로디지털점"
html    = "닭도리탕전문도리당구로디지털단지점"
html.includes(target) → false  ← "단지" 때문에 불일치
```

수정 후:
```
target  = "도리당구로디지털단지점"
html.includes(target) → true ✓
```

## Files to Create / Modify

- **수정**: `C:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용\accounts.js`
  - mika2969 항목 2줄: `구로디지털점` → `구로디지털단지점`
- **복사(덮어쓰기)**: `C:\airflow\coupang_extension_build\content\03_coupangeats.js` → OneDrive 개발용 폴더
- **복사(덮어쓰기)**: `C:\airflow\coupang_extension_build\runner.js` → OneDrive 개발용 폴더
- **수정 불필요**: `store_normalize.py` (line 20에 이미 `"도리당 구로디지털단지점" → "도리당 구로디지털점"` 매핑 존재)

## Implementation Steps

### 1. accounts.js 매장명 수정

파일: `C:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용\accounts.js`

mika2969 관련 2줄만 수정 — 다른 지점 절대 변경 금지:

```javascript
// Before (2줄)
{ store: '도리당 구로디지털점', platform: 'baemin', id: 'mika2969', pw: 's028555989!' },
{ store: '도리당 구로디지털점', platform: 'coupang', id: 'mika2969', pw: 's028555989@' },

// After (2줄)
{ store: '도리당 구로디지털단지점', platform: 'baemin', id: 'mika2969', pw: 's028555989!' },
{ store: '도리당 구로디지털단지점', platform: 'coupang', id: 'mika2969', pw: 's028555989@' },
```

### 2. 빌드 JS → OneDrive 복사

```powershell
$src = "C:\airflow\coupang_extension_build"
$dst = "C:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용"
Copy-Item "$src\content\03_coupangeats.js" "$dst\content\03_coupangeats.js" -Force
Copy-Item "$src\runner.js" "$dst\runner.js" -Force
```

### 3. Git 커밋 (빌드 디렉토리)

```powershell
git -C "C:\airflow" add coupang_extension_build/content/03_coupangeats.js coupang_extension_build/runner.js
git -C "C:\airflow" commit -m "fix: 구로디지털단지점 쿠팡이츠 수집 불가 버그 수정 (0625)"
```

## Test Cases

1. **문자열 매칭 검증** (브라우저 콘솔 또는 Node.js):
   ```javascript
   const target = "도리당 구로디지털단지점".replace(/\s+/g,'');
   const html   = "닭도리탕 전문 도리당 구로디지털단지점".replace(/\s+/g,'');
   console.log(html.includes(target)); // 기대: true
   ```

2. **accounts.js 저장 확인**:
   ```powershell
   $dst = "C:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용"
   Get-Content "$dst\accounts.js" -Encoding utf8 | Select-String "구로"
   # 기대: 두 줄 모두 '구로디지털단지점' 표시, '구로디지털점' 단독(단지 없는) 0줄
   ```

3. **JS 파일 복사 확인**:
   ```powershell
   $dst = "C:\Users\민준\OneDrive - 주식회사 도리당\Extention\doridang_collector_개발용"
   (Get-Item "$dst\content\03_coupangeats.js").LastWriteTime
   (Get-Item "$dst\runner.js").LastWriteTime
   # 기대: 현재 시각(복사 직후)
   ```

4. **배치 실행 검증** (Chrome):
   - Chrome 확장 관리 페이지에서 확장 리로드
   - runner.html 대시보드에서 구로디지털단지점 수집 목록 확인
   - 수집 완료 후 `coupangeats_orders_*구로디지털*` CSV OneDrive에 생성됨

## Verification Loop

```
LOOP until all PASS:
  1. Test Cases 1~3 실행 (자동 검증 가능)
  2. FAIL → accounts.js 인코딩(UTF-8 BOM 없음) 또는 오타 확인
  3. Test Case 4는 Chrome 배치 실행 후 수동 확인
종료 조건: Test 1~3 PASS + 배치에서 구로디지털단지점 orders/CMG 수집 성공
```

## Constraints

- `accounts.js`는 UTF-8 인코딩 유지 (BOM 없음)
- `구로디지털점` → `구로디지털단지점` 변경은 mika2969 2줄만 — 다른 계정 절대 변경 금지
- `store_normalize.py` 수정 불필요 (이미 `구로디지털단지점` 처리 포함)
- OneDrive 파일 수정 후 Chrome 확장 리로드 필수 (파일만 바꾸면 적용 안 됨)
- 빌드 디렉터리(`coupang_extension_build/`) JS 로직 변경 불필요 (이미 최신)

## Do Not Ask — Decide Yourself

- 파일이 이미 존재하면: 덮어쓰기
- accounts.js에 다른 지점의 `구로` 항목 있으면: 변경하지 말 것
- JS 파일 복사 시 인코딩: `-Force` 플래그로 그대로 복사
- 변수명·함수명 스타일: 기존 camelCase 유지
