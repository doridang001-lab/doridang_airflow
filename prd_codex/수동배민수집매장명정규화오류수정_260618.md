# 수동배민수집매장명정규화오류수정

## Task
수동 배민 CSV 업로드 시 "구로디지털 단지점"처럼 공백이 포함된 매장명이 정규화 없이 잘못된 parquet 파티션에 저장되는 버그를 수정한다. 수정 후 `DB_CollectionCompare`에서 자동 수집분과 동일한 store 키로 합산되어야 한다.

## Project Conventions
- logging: `logger = logging.getLogger(__name__)` — print 절대 금지
- import 경로: `from modules.transform.utility.XXX import ...` (절대경로)
- 파이프라인 파일 위치: `modules/transform/pipelines/db/`
- 경로 상수: `from modules.transform.utility.paths import ...` (하드코딩 금지)

## Files to Create / Modify
- **수정** `modules/transform/pipelines/db/DB_Beamin_Macro_validate.py` — `_manual_baemin_store_meta()` 정규식 + 공백 처리
- **수정** `modules/transform/pipelines/db/DB_CollectionCompare.py` — `_normalize_store_name()` store_normalize 적용

## Implementation Steps

### 1. `DB_Beamin_Macro_validate.py` — `_manual_baemin_store_meta()` 수정

현재 line 46~48:
```python
matches = re.findall(r"[가-힣A-Za-z0-9]+(?:점|지점|분점|직영점)", text)
branch = matches[-1] if matches else (text.split()[-1] if text.split() else text)
normalized = f"{brand} {branch}".strip() if brand else branch
```

변경 후:
```python
matches = re.findall(r"[가-힣A-Za-z0-9]+(?:\s+[가-힣A-Za-z0-9]+)*(?:점|지점|분점|직영점)", text)
branch = matches[-1] if matches else (text.split()[-1] if text.split() else text)
branch = re.sub(r"\s+", "", branch)  # "구로디지털 단지점" → "구로디지털단지점"
normalized = f"{brand} {branch}".strip() if brand else branch
```

변환 흐름:
- 입력: "도리당 구로디지털 단지점"
- match: "구로디지털 단지점" → strip spaces → "구로디지털단지점"
- normalized: "도리당 구로디지털단지점"
- STORE_NAME_MAP 적용: "도리당 구로디지털점"
- strip_brand: "구로디지털점" ✓

### 2. `DB_CollectionCompare.py` — `_normalize_store_name()` 수정

파일 상단 import 추가 (기존 import 블록 근처):
```python
from modules.transform.utility.store_normalize import normalize_for_join
```

현재 line 44~45:
```python
def _normalize_store_name(series: pd.Series) -> pd.Series:
    return series.astype(str).str.strip().str.replace(r"\s+", "", regex=True)
```

변경 후:
```python
def _normalize_store_name(series: pd.Series) -> pd.Series:
    stripped = series.astype(str).str.strip().str.replace(r"\s+", "", regex=True)
    return normalize_for_join(stripped)
```

효과: 기존에 잘못 저장된 parquet(`store=구로디지털단지점`)도 compare 단계에서 "구로디지털점"으로 자동 교정됨.

## Reference Code

### `modules/transform/utility/store_normalize.py`
```python
STORE_NAME_MAP: dict[str, str] = {
    "도리당 일산백석점":       "도리당 백석점",
    "도리당 구로디지털단지점": "도리당 구로디지털점",
    "도리당 충주봉방점":       "도리당 충주역점",
    "도리당 파주운정점":       "도리당 운정점",
    "도리당 인천간석점":       "도리당 인천간석중앙점",
    "도리당 간석중앙점":       "도리당 인천간석중앙점",
    "나홀로 간석중앙점":       "나홀로 인천간석중앙점",
    "도리당 서울역삼점":       "도리당 역삼점",
    "해운대 중동점":           "도리당 해운대중동점",
    "도리당 성정점":           "도리당 천안성정점",
    "나홀로 대신점":           "나홀로 부산대신점",
    "해운대중동점":            "도리당 해운대중동점",
}

_PREFIX_RE       = re.compile(r"^도리당\s*")
_SPACE_RE        = re.compile(r"\s+")
_BRAND_PREFIX_RE = re.compile(r"^(도리당|나홀로)\s+")

def _build_expanded_map(base: dict) -> dict:
    expanded = dict(base)
    for old, new in base.items():
        stripped = _PREFIX_RE.sub("", old).strip()
        if stripped and stripped not in expanded:
            expanded[stripped] = new
    return expanded

_MAP = _build_expanded_map(STORE_NAME_MAP)

def normalize(series: pd.Series) -> pd.Series:
    return series.replace(_MAP)

def normalize_for_join(series: pd.Series) -> pd.Series:
    return normalize(series).map(
        lambda s: _SPACE_RE.sub("", _PREFIX_RE.sub("", str(s)).strip())
    )

def strip_brand(series: pd.Series) -> pd.Series:
    return series.map(lambda s: _BRAND_PREFIX_RE.sub("", str(s)).strip())
```

### `modules/transform/pipelines/db/DB_Beamin_Macro_validate.py` (핵심 부분)
```python
from modules.transform.utility.store_normalize import normalize as normalize_store_names, strip_brand

def _manual_baemin_store_meta(raw_store_name: str, fallback_name: str = "") -> tuple[str, str]:
    """Returns (store_key, brand) parsed from a manual Baemin CSV source name."""
    text = str(raw_store_name or fallback_name or "").strip()
    if not text:
        return "", ""
    text = re.sub(r"\[.*?\]\s*", "", text).strip()
    brand = "나홀로" if "나홀로" in text else ("도리당" if "도리당" in text else "")
    # ← 수정 대상: line 46~48
    matches = re.findall(r"[가-힣A-Za-z0-9]+(?:점|지점|분점|직영점)", text)
    branch = matches[-1] if matches else (text.split()[-1] if text.split() else text)
    normalized = f"{brand} {branch}".strip() if brand else branch
    normalized_series = normalize_store_names(pd.Series([normalized]))
    branch_series = strip_brand(normalized_series)
    return str(branch_series.iloc[0]).strip(), brand
```

## Test Cases

1. **정규식 파싱 검증**
```python
python -c "
import sys; sys.path.insert(0, 'C:/airflow')
from modules.transform.pipelines.db.DB_Beamin_Macro_validate import _manual_baemin_store_meta
cases = [
    ('도리당 구로디지털 단지점', ('구로디지털점', '도리당')),
    ('도리당 구로디지털단지점', ('구로디지털점', '도리당')),
    ('[음식배달] 나홀로 1인 곱도리탕 가락점', ('가락점', '나홀로')),
    ('도리당 역삼점', ('역삼점', '도리당')),
]
for raw, expected in cases:
    result = _manual_baemin_store_meta(raw)
    status = 'PASS' if result == expected else f'FAIL (got {result})'
    print(f'{status}: {raw!r}')
"
```
기대: 전체 PASS

2. **normalize_for_join 동작 검증**
```python
python -c "
import sys; sys.path.insert(0, 'C:/airflow')
import pandas as pd
from modules.transform.utility.store_normalize import normalize_for_join
s = pd.Series(['구로디지털단지점', '구로디지털점', '도리당 구로디지털단지점'])
print(normalize_for_join(s).tolist())
"
```
기대: `['구로디지털점', '구로디지털점', '구로디지털점']`

3. **DB_CollectionCompare import 검증**
```python
python -c "from modules.transform.pipelines.db.DB_CollectionCompare import build_collection_compare; print('OK')"
```
기대: `OK` (ImportError 없음)

## Verification Loop
```
LOOP until all PASS:
  1. Test Cases 1~3 순서대로 실행
  2. FAIL 항목 → 원인 분석
  3. 코드 수정
  4. 전체 Test Cases 재실행
종료 조건: 전체 PASS
```

## Constraints
- `normalize_for_join`의 `_PREFIX_RE`는 "도리당"만 제거, "나홀로" 접두어는 제거하지 않음 — 나홀로 파티션은 이미 "나홀로 " 없이 저장되어 있으므로 영향 없음
- 기존 `STORE_NAME_MAP` 수정 금지 — 이 파일은 전 파이프라인이 공유하는 Single Source
- `_normalize_store_name`이 `load_baemin_macro`, `load_coupang_macro`, `_normalize_grouped` (toorder/unified) 모두에 사용됨 → 변경 후 4개 소스 모두 동일 키 포맷 유지되어야 함

## Do Not Ask — Decide Yourself
- 파일이 이미 존재하면: 덮어쓰기
- import가 모호하면: Reference Code의 패턴 따라가기
- 타입 힌트 여부: 기존 파일과 동일하게
- 주석 여부: 최소화 (WHY만, WHAT 설명 금지)
- 변수명·함수명 스타일: snake_case, 기존 파일과 동일하게
