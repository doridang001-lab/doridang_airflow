# 렌더러 타임아웃 세션복구 마커 회귀 제거

## Task
직전 변경으로 `DB_Beamin_combined.py`의 `_SESSION_ISSUE_MARKERS`에 `"timed out receiving message from renderer"`를 추가한 것이 의도와 정반대 효과를 냈다 — 렌더러 타임아웃을 "죽은 세션"으로 오분류해 드라이버를 재생성하는데, 같은 무거운 페이지에서 또 타임아웃 → 재생성 한도(`max_session_recovery_per_account=1`) 초과 → **계정 전체가 실패**로 드롭된다(사용자: "수정하고 오히려 로그인조차 못하고 있어"). 이 마커 한 줄을 제거해 이전 정상 동작으로 되돌린다.

## Project Conventions
- logging: `logger = logging.getLogger(__name__)` — print 절대 금지
- import 경로: `from modules.transform.pipelines.db.XXX import ...` (절대경로)
- 파이프라인 파일 위치: `modules/transform/pipelines/db/`
- 경로 상수: `from modules.transform.utility.paths import ...` (하드코딩 금지)

## Files to Create / Modify
- **수정**: `modules/transform/pipelines/db/DB_Beamin_combined.py` (단일 파일, 한 줄 제거)

## Implementation Steps

1. **마커 한 줄 제거** — `_SESSION_ISSUE_MARKERS` 튜플(현재 line 124-139)에서 마지막 항목만 삭제:
   ```python
       "session not created",
   -   "timed out receiving message from renderer",   # ← 이 줄만 제거
   )
   ```
   `"session not created",`는 유지하고 그 다음 렌더러 줄만 지운 뒤 닫는 `)`로 이어지게 한다.

2. **그 외 변경 금지** — `_build_dashboard_session`/`_close_driver_safely`/`_is_recoverable_session_issue` 인프라, NOW 복원(line 1093 `_run_stage("NOW 수집", ...)`), store_id 정규화·브랜드 정렬 등 working tree의 다른 변경은 **전부 그대로 보존**. 오직 1단계 한 줄만 제거.

### 제거 후 기대 동작
- 부트스트랩(`_build_dashboard_session`, line 162): 렌더러 타임아웃 시 `_is_recoverable_session_issue` → False → line 186 `"대시보드 이동 타임아웃 (계속 진행)"` 경로로 살아있는 드라이버를 반환 → 후속 `wait_for_page(ShopSelect, timeout=60)`(line 908)이 렌더링 완료 대기 → 로그인/수집 정상 복귀.
- 수집 단계(`_run_stage`): 렌더러 타임아웃이 더 이상 세션 재생성을 트리거하지 않고 generic `if fatal:` 분기로 빠져 **해당 매장만 실패 마킹** → Retry DAG가 처리.

## Reference Code

### DB_Beamin_combined.py (현재 상태 — 제거 대상 마커 + 회귀가 발생하는 부트스트랩)
```python
_SESSION_ISSUE_MARKERS = (
    "remote end closed", "connection aborted", "remotedisconnected",
    "connection refused", "max retries exceeded", "newconnectionerror",
    "invalid session id", "chrome not reachable", "disconnected",
    "tab crashed", "session deleted", "cannot connect to chrome",
    "session not created",
    "timed out receiving message from renderer",   # ← 제거 대상
)

def _is_recoverable_session_issue(exc: Exception) -> bool:
    error_text = str(exc).lower()
    return is_driver_crash_error(exc) or any(
        marker in error_text for marker in _SESSION_ISSUE_MARKERS
    )

def _build_dashboard_session(account: dict, metrics: dict, profile: dict) -> Any | None:
    account_id = account["account_id"]
    max_recovery = int(profile["max_session_recovery_per_account"])
    for attempt in range(max_recovery + 1):
        driver = _build_account_session(account, metrics)
        if driver is None:
            return None
        try:
            driver.set_page_load_timeout(45)
            driver.get("https://self.baemin.com/")
            return driver
        except Exception as exc:
            if _is_recoverable_session_issue(exc):      # 마커 제거 시 렌더러 타임아웃은 여기 False
                _close_driver_safely(driver, account_id)
                if attempt < max_recovery:
                    metrics["session_recovery_count"] += 1
                    logger.warning("초기 대시보드 세션 문제 감지, 재생성 시도: %s / %s", account_id, exc)
                    continue
                logger.warning("초기 대시보드 세션 문제 재생성 한도 초과: %s / %s", account_id, exc)
                return None
            logger.warning("대시보드 이동 타임아웃 (계속 진행): %s", exc)   # ← 렌더러 타임아웃이 가야 할 경로
            return driver
    return None
```

## Test Cases
1. [구문] `python -c "import ast; ast.parse(open('modules/transform/pipelines/db/DB_Beamin_combined.py',encoding='utf-8').read())"` → 기대: 출력 없음
2. [마커 제거 확인] `python -c "from modules.transform.pipelines.db.DB_Beamin_combined import _SESSION_ISSUE_MARKERS as m; assert 'timed out receiving message from renderer' not in m, m; print('OK', len(m))"` → 기대: `OK 13`
3. [diff 검증] `git diff modules/transform/pipelines/db/DB_Beamin_combined.py` → 기대: `_SESSION_ISSUE_MARKERS`에서 `"timed out receiving message from renderer"` 한 줄만 사라지고, 나머지 working tree 변경(NOW 복원·store_id 정규화 등)은 모두 보존
4. [실행] `DB_Beamin_Macro_Dags` 수동 트리거 후 collect_all 로그 → 기대: "초기 대시보드 세션 문제 재생성 한도 초과 → 계정 실패"가 사라지고, 렌더러 타임아웃 시 "대시보드 이동 타임아웃 (계속 진행)"으로 전환, `failed_accounts` 감소

## Verification Loop
```
LOOP until all PASS:
  1. Test Cases 1~3 순서대로 실행
  2. FAIL 항목 → 원인 분석
  3. 코드 수정
  4. 전체 Test Cases 재실행
종료 조건: 1~3 전체 PASS + Constraints 위반 없음 (4번은 Airflow 실행 환경에서 수동 확인)
```

## Constraints
- **오직 마커 한 줄만 제거.** `_build_dashboard_session` 로직, 세션복구 인프라, NOW 복원, store_id 정규화 등 다른 working tree 변경은 절대 손대지 말 것. `git checkout`/파일 통째 되돌리기 금지.
- 범위 밖: 렌더러 타임아웃 자체 완화(driver.get 재시도·대기 상향), `max_session_recovery_per_account` 상향, page load timeout 튜닝, 커밋/푸시.

## Do Not Ask — Decide Yourself
- 마커 위치가 헷갈리면: `"session not created",` 바로 다음 줄이 제거 대상
- 들여쓰기/스타일: 기존 튜플과 동일하게
- 주석: 최소화 (WHY만)
