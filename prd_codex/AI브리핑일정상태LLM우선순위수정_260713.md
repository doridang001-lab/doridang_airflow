# AI 브리핑 — 일정 상태 분류 + LLM 우선순위 수정

## Task

`Private_MorningBriefing_Dags`(→ `morning_briefing_pipeline.py`)의 브리핑에서 두 가지가 깨져 있다. (1) `📅 오늘 일정`이 전부 `## 진행`으로 몰리고 `## 피드백/결제중/테스트/세부업무`가 전부 `(없음)` — 상태 분류 실패. (2) `🎯 오늘 우선순위`가 번호(1~5)만 있고 내용이 공란 — LLM 응답 유실. 7/9자 수정(`prd_codex/브리핑캘린더상태분류수정_260709.md`)은 이미 반영돼 있으나 증상이 재발했다. 회귀했거나 Flow DOM 구조가 바뀐 것이며, **런타임 DOM 로그 근거로만 수정한다(추측 금지).**

## Project Conventions

- logging: `logger = logging.getLogger(__name__)` — print 절대 금지
- import 경로: `from modules.transform.utility.XXX import ...` (절대경로)
- 파이프라인 파일 위치: `modules/transform/pipelines/{domain}/`
- DAG 파일 위치: `dags/{domain}/`
- 스케줄 상수: `from modules.transform.utility.schedule import ...` (하드코딩 금지)
- 경로 상수: `from modules.transform.utility.paths import ...` (하드코딩 금지)
- DAG에는 오케스트레이션만, 비즈니스 로직은 `modules/transform/pipelines/`

## Files to Create / Modify

- **수정(유일 주 대상)**: `modules/transform/pipelines/strategy/morning_briefing_pipeline.py`
  - `_select_flow_calendar_filters` / `_force_toggle_flow_calendar_filters` (문제 1-a)
  - `_extract_flow_calendar_events_from_html` + weekView JS `statusOf` (문제 1-b)
  - `_llm_call` (문제 2)
- **재사용만(수정 금지 목표)**: `modules/transform/utility/qwen_client.py`의 `_response_content` import
- DAG 파일 `dags/private/Private_MorningBriefing_Dags.py`·폴더·파일명 변경 금지.

## Implementation Steps

### 1. (문제 1) DOM 증거 확보 — 코드 수정 전 필수

먼저 진단 태스크를 실행해 실제 DOM/필터 상태를 확인한다. **추측 금지, 이 로그로 (a)/(b) 분기 확정.**

```bash
docker compose exec airflow-scheduler airflow tasks test Private_MorningBriefing_Dags collect_briefing_data 2026-07-13
```

로그 두 신호를 읽는다:
- `Flow Calendar 필터 선택: … before/after className … icon …` → "업무 전체"가 켜진 채 끝났는가, 진짜 활성 class명은 무엇인가.
- `Flow Calendar raw row html sample: <tr …>` + `raw event sample` → 오늘 업무 항목 `<tr>` 형태. 상태가 `<a>` 제목에 있나(`[진행] …`), `mouseover-text`에만 있나, 별도 셀/마커 class/`data-*`에 있나.

### 2. (문제 1-a로 확정 시) 필터 활성 판정 교정

"업무 전체"(`#taskFilter [gubun="2"]`) 필터가 꺼져 상태 태그 붙은 업무가 안 실리는 경우:
- `_select_flow_calendar_filters`의 JS `isActive()` 판정을 **로그에 찍힌 실제 활성 class**로 교정해 확실히 켜지도록 한다(추측 금지).
- `_force_toggle_flow_calendar_filters`가 재시도 중 이미 올바른 필터를 **도로 끄지 않도록** 한다: blind 토글 대신 `ensureActive` 재사용, 또는 `status_source` 판정으로 정말 필요할 때만 토글.

### 3. (문제 1-b로 확정 시) 상태 추출 위치 확장

Flow가 상태를 제목이 아니라 별도 위치(예: `mouseover-text`, 마커 class, `data-*`)에 두는 경우:
- `_extract_flow_calendar_events_from_html`(및 weekView JS)가 상태를 **실제 위치**에서 읽도록 status 추출을 확장한다(예: `mouseover-text` 우선 사용, 또는 마커 class/`data-*` → 태그 매핑).
- 매핑은 **`_CALENDAR_STATUS_ALIASES` 한 곳에서만** 한다. JS 안에 상태 매핑 복제 금지 — JS는 원본 태그 문자열만 실어 보낸다.

### 4. (문제 1 공통) 판정 단일화 유지

- 상태 판정은 `_dedupe_briefing_calendar_events`에서 한 번만 확정, `_format_calendar_sections`는 `event["status"]`를 그대로 신뢰(현행 유지).
- 기본값 `"진행"` 폴백과 무태그 시 `logger.warning`은 유지.
- `_CALENDAR_EXCLUDED_STATUS_TAGS`(완료·보류)·`_CALENDAR_NON_STATUS_PREFIX_TAGS`(요청) 삭제 금지.

### 5. (문제 2) LLM 우선순위 공란 수정 — 증거 불필요, 확정

`🎯 오늘 우선순위`는 100% LLM 출력(코드가 번호를 만들지 않음). 범인은 `_llm_call`의 취약한 자체 루프: `think=False`로 호출 후 `message.content`만 추출 → gpt-oss는 답이 `thinking` 채널에 와서 유실. `resp.get(...)`을 객체에 호출해 `AttributeError` 가능. `_prioritize_primary_models`·unhealthy 스킵 미적용.

`_llm_call`을 `qwen_client` 견고 경로에 맞춘다:
- 후보를 `_prioritize_primary_models(candidates)` 순서로 순회, unhealthy 모델 스킵.
- 모델이 gpt-oss면 `think=True`로 호출.
- 응답 추출은 `qwen_client._response_content(resp, model)` 재사용(content 비면 thinking 폴백, 빈 응답이면 `ValueError` → 다음 후보).
- `num_predict` 인자 유지(Step A 150 / Step B 400).
- `_llm_call` 한 함수만 고치고 `_response_content`·`_prioritize_primary_models`·`_is_model_unhealthy`는 `qwen_client`에서 import해 재사용(qwen_client 수정 없이).

## Reference Code

### modules/transform/pipelines/strategy/morning_briefing_pipeline.py — 현재 `_llm_call` (수정 대상)

```python
def _llm_call(prompt: str, system: str, num_predict: int = 300) -> str:
    """Ollama 직접 호출 — num_predict 조절 가능."""
    from modules.transform.utility.qwen_client import get_ollama_client_with_candidates

    client, candidates = get_ollama_client_with_candidates()
    messages = [{"role": "system", "content": system}, {"role": "user", "content": prompt}]
    for model in candidates:
        try:
            resp = client.chat(
                model=model, messages=messages, stream=False, think=False,
                options={"num_predict": num_predict, "temperature": 0},
            )
            content = getattr(getattr(resp, "message", None), "content", None) or resp.get("message", {}).get("content", "")
            if content and content.strip():
                return content.strip()
        except Exception as e:
            logger.warning(f"LLM 실패 ({model}): {e}")
    return "(LLM 응답 없음)"
```

### modules/transform/utility/qwen_client.py — 재사용 대상 헬퍼

```python
def _prioritize_primary_models(model_candidates: list[str], prefer_stable: bool = True) -> list[str]:
    gpt_oss = [m for m in model_candidates if "gpt-oss" in m]
    stable = [m for m in model_candidates if "gpt-oss" not in m]
    return (stable + gpt_oss) if prefer_stable else (gpt_oss + stable)

def _is_model_unhealthy(model_name: str, unhealthy: dict[str, float] | None = None) -> bool:
    unhealthy = unhealthy or _load_unhealthy_models()
    ts = unhealthy.get(model_name)
    return bool(ts and (time.time() - ts) < UNHEALTHY_MODEL_TTL_SEC)

def _response_content(response, model_name: str) -> str:
    # content 비면 thinking 폴백, 둘 다 비면 ValueError
    if hasattr(response, "message"):
        msg = response.message
        content = getattr(msg, "content", "") or ""
        thinking = getattr(msg, "thinking", "") or ""
    else:
        msg = response.get("message", {})
        content = msg.get("content", "") or ""
        thinking = msg.get("thinking", "") or ""
    if not content.strip() and thinking.strip():
        content = thinking
    if not content.strip():
        raise ValueError(f"{model_name} 응답 content가 비어 있습니다")
    return content
```

### morning_briefing_pipeline.py — 현재 `_select_flow_calendar_filters` 활성 판정(문제 1-a 수정 지점)

```python
# JS isActive(): 아래 class 후보를 확인 — 실제 활성 class는 Step 1 로그로 확정
#   ' on ' / ' active ' / ' selected ' / ' all-checked ' (icon .js-common-radio 포함)
# ensureActive(el): isActive면 클릭 안 함(멱등). before/after className을 반환해 logger로 남김.
# scheduleAll = #scheduleFilter .js-filter-button[gubun="0,1"]
# taskAll     = #taskFilter .js-filter-button[gubun="2"]   ← 이게 꺼지면 [진행] 업무가 안 실림
```

## Test Cases

1. **순수 함수 회귀(세부업무 분리 + 완료 제외)** — prd 260709 Test Case 1 재사용:
   실제 제목 12건을 `_dedupe_briefing_calendar_events` → `_format_calendar_sections`에 통과 → 진행 2/세부업무 1/테스트 1/피드백 1/결제중 1, `[완료]` 미포함, 5개 섹션 존재. → 기대: `PASS`
2. **무태그 경고 로그**: `[요청] [전략기획부] …` 1건 → status `진행`, status_source `default`, WARNING 1줄. → 기대: `PASS`
3. **DAG import 무결성**: `python -c "import importlib; importlib.import_module('modules.transform.pipelines.strategy.morning_briefing_pipeline'); print('PASS')"` → 기대: ImportError 없음
4. **실수집(핵심 통과 기준)**: `docker compose exec airflow-scheduler airflow tasks test Private_MorningBriefing_Dags collect_briefing_data 2026-07-13` → 기대: `raw event sample`의 status_source가 전부 `default`가 아니고 실제 상태를 반영, 필터 로그상 "업무 전체" 활성 유지.
5. **브리핑 출력**: `docker compose exec airflow-scheduler airflow tasks test Private_MorningBriefing_Dags generate_briefing 2026-07-13` → 기대: `📅 오늘 일정`이 5개 섹션으로 갈라지고, `🎯 오늘 우선순위`에 실제 한국어 우선순위 텍스트가 채워짐(공란/번호만 아님). 로그에 `(LLM 응답 없음)`·`LLM 실패`·`응답이 비어 있습니다` 없음.

## Verification Loop

구현 완료 후 아래 루프를 **모든 Test Cases PASS까지** 반복한다.

```
LOOP until all PASS:
  1. Test Cases 1→5 순서대로 실행
  2. FAIL 항목 → 원인 분석 (Step 1 로그 근거 재확인)
  3. 코드 수정
  4. 전체 Test Cases 재실행
종료 조건: 전체 PASS + Constraints 위반 없음
```

Test Case 4가 FAIL이면(여전히 전부 default/`[요청]`) 활성 class 후보가 실제와 다른 것이다 — 추측하지 말고 Step 1 로그의 `before` className을 근거로 판정 조건을 고친다.

## Constraints

- 수정 파일은 `morning_briefing_pipeline.py` 하나(문제 2도 이 파일 `_llm_call`에서 해결). `qwen_client.py`는 import 재사용만, 수정 금지.
- DAG 파일·폴더 구조·파일명 변경 금지.
- 상태 매핑은 Python `_CALENDAR_STATUS_ALIASES` 한 곳에서만. JS에 상태 매핑 복제 금지.
- `_CALENDAR_EXCLUDED_STATUS_TAGS`(완료·보류)·`_CALENDAR_NON_STATUS_PREFIX_TAGS`(요청) 삭제 금지.
- 기본값 `"진행"` 폴백 유지하되 무태그 시 조용히 넘어가지 말고 warning.
- weekView fallback 코드 삭제 금지 — 정합성만 맞춘다.
- Selenium 필터 클릭은 멱등(두 번 호출해도 같은 상태).
- `print` 금지 — `logger` 사용.
- **Step 1(DOM 로그 확보) 없이 문제 1 코드를 추측으로 고치지 말 것.** 이게 7/9 재발의 핵심 교훈이다.

## Do Not Ask — Decide Yourself

- 파일이 이미 존재하면: 덮어쓰기
- import가 모호하면: Reference Code의 패턴 따라가기
- 타입 힌트 여부: 기존 파일과 동일하게 (`str | None`, `list[dict]`)
- 주석 여부: 최소화 (WHY만, WHAT 설명 금지)
- 변수명·함수명 스타일: snake_case, private helper는 `_` 접두, 기존 파일과 동일하게
- 활성 class 판정이 애매하면: 로그의 before/after className을 근거로 삼는다(추측 금지)
