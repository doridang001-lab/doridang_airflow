# 토더계정누락알림이메일텔레그램전환미발송원인정리

## Task
`dags/sales/Sales_Employee_Extract_Dags.py`의 `check_toder_null_values()`는 토더(TORDER) ID/PW가 비어있는 매장을 발견하면 `send_email_alert()`로 HTML 메일을 `MAIL_CMJ_PM`에게 보낸다. 이 함수는 이 DAG 파일에만 있는 하드코딩된 Gmail SMTP app password를 쓰고, 실패 시 `except Exception` 안에서 `print`만 하고 조용히 넘어가서 "왜 안 오는지" 알 방법이 없다. 본문도 HTML인데 `MIMEText(body, 'plain', ...)`로 보내 태그가 그대로 노출되는 문제도 있다. 이 알림을 프로젝트에 이미 있는 `modules/transform/utility/notifier.py`의 `send_telegram`/`send_telegram_chunks`로 교체해 텔레그램으로 보낸다.

## Project Conventions
- logging: `logger = logging.getLogger(__name__)` — print 절대 금지 (단, 이 파일은 기존에 전면 print 스타일이므로 이번 최소수정 범위에서 파일 전체 print→logging 전환은 하지 않음, 새로 추가하는 코드도 기존 print 스타일에 맞춘다)
- import 경로: `from modules.transform.utility.XXX import ...` (절대경로)
- DAG 파일 위치: `dags/{domain}/`
- 텔레그램 발송: `from modules.transform.utility.notifier import send_telegram, send_telegram_chunks` — 자격증명은 Airflow Variable(`TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID`, env fallback)에서 내부적으로 resolve되므로 이 함수를 호출하는 쪽에서 별도 설정 불필요
- 실패는 예외로 승격 (`raise AirflowException(...)`) — 이 DAG는 이미 2026-07-15 PRD(`Sales_Employee_Extract_Dags_silent_failure_fix_260715.md`)로 이 부분 수정 완료됨. 이번 작업은 그와 별개인 "토더 계정 없음" 비즈니스 알림 경로(태스크는 성공으로 끝나고 알림만 별도 발송)를 다룸 — `raise` 승격 대상 아님

## Files to Create / Modify
- **수정**: `dags/sales/Sales_Employee_Extract_Dags.py` (유일한 수정 대상)

## Implementation Steps

### Step 1 — import 정리 (파일 상단, 6-31행)
- 제거: `import smtplib`(14행), `from email.mime.text import MIMEText`(15행), `from email.mime.multipart import MIMEMultipart`(16행)
- `modules.transform.utility.mail_recipients` import(27-31행)에서 `MAIL_CMJ_PM`, `resolve_mail_recipients` 제거. `resolve_manager_mail`은 파일 내 다른 곳(담당자 이메일 매핑)에서 계속 쓰이므로 유지.
- 추가: `from modules.transform.utility.notifier import on_failure_callback, on_retry_callback, send_telegram, send_telegram_chunks` (기존 `on_failure_callback, on_retry_callback` import 라인을 이걸로 교체)
- `MAIL_CMJ_PM`, `resolve_mail_recipients`, `smtplib`, `MIMEText`, `MIMEMultipart`는 이 파일 안에서 `send_email_alert`/`check_toder_null_values` 외 다른 곳에 쓰이지 않음(grep 확인 완료) — 제거해도 안전.

### Step 2 — `send_email_alert()` 함수 삭제 (62-90행)
이 함수 전체를 삭제한다. 사용처는 `check_toder_null_values()`의 마지막 호출 한 곳뿐(Step 3에서 같이 교체됨).

### Step 3 — `check_toder_null_values()` 재작성 (93-144행)
- 컬럼 존재 체크(95-96행), `null_mask` 계산(99-100행), `null_stores` 추출(102행)은 그대로 유지.
- 컬럼이 없어서 조용히 `return`하는 95-96행에 진단 로그 한 줄 추가: `print(f"[토더계정 알림] 스킵: 토더ID/토더PW 컬럼 없음")` 후 `return`. (다음에 또 "알림이 안 온다"는 문제가 생기면 컬럼 매핑 문제인지 바로 구분 가능하게 하기 위함)
- HTML 테이블(108-141행) 대신 한국어 평문 텍스트 메시지를 조립한다. `notifier.py`의 `on_retry_callback`/`on_failure_callback`이 쓰는 스타일과 통일:
  ```python
  lines = [f"⚠ 토더(TORDER) 계정정보 누락 알림 ({len(null_stores)}건)", ""]
  for _, row in null_stores.iterrows():
      toder_id = str(row['토더ID']).strip() if pd.notna(row['토더ID']) else '(없음)'
      toder_pw = str(row['토더PW']).strip() if pd.notna(row['토더PW']) else '(없음)'
      lines.append(f"- {row['호점']} / {row['매장명']} / {row['담당자']}: 토더ID={toder_id}, 토더PW={toder_pw}")
  lines.append("")
  lines.append("구글시트에서 해당 매장의 토더 ID/PW를 확인하고 등록해주세요.")
  message = "\n".join(lines)
  ```
- 143-144행(`subject = ...`, `send_email_alert(subject, html_body, MAIL_CMJ_PM)`)을 `send_telegram_chunks(message)` 호출로 교체한다. (매장 수가 많아지면 메시지가 길어질 수 있어 4000자 자동분할되는 `send_telegram_chunks` 사용 — 단건이면 내부적으로 청크 1개로 처리되어 `send_telegram`과 동일하게 동작)

### Step 4 — 호출부(310행)는 변경 없음
`check_toder_null_values(df)` 호출은 그대로 둔다.

## Reference Code

### modules/transform/utility/notifier.py (재사용할 텔레그램 발송 함수, 이미 존재·수정 불필요)
```python
def _get_telegram_creds() -> tuple[str, str]:
    token = ""
    chat_id = ""
    try:
        from airflow.models import Variable
        token = Variable.get("TELEGRAM_BOT_TOKEN", default_var="")
        chat_id = Variable.get("TELEGRAM_CHAT_ID", default_var="")
    except Exception:
        pass
    return token or os.getenv("TELEGRAM_BOT_TOKEN", ""), chat_id or os.getenv("TELEGRAM_CHAT_ID", "")


def send_telegram(text: str) -> bool:
    token, chat_id = _get_telegram_creds()
    if not token or not chat_id:
        logger.warning("Telegram credentials missing; skip send")
        return False
    try:
        payload = urllib.parse.urlencode({"chat_id": chat_id, "text": text}).encode()
        url = f"https://api.telegram.org/bot{token}/sendMessage"
        req = urllib.request.Request(url, data=payload, method="POST")
        with urllib.request.urlopen(req, timeout=10):
            pass
        logger.info("Telegram alert sent")
        return True
    except Exception as e:
        logger.warning("Telegram send failed (ignored): %s", e)
        return False


def send_telegram_chunks(text: str, limit: int = 4000) -> bool:
    lines = (text or "").split("\n")
    chunks: list[str] = []
    buf: list[str] = []
    cur = 0
    for line in lines:
        while len(line) > limit:
            if buf:
                chunks.append("\n".join(buf))
                buf = []
                cur = 0
            chunks.append(line[:limit])
            line = line[limit:]
        add = len(line) + (1 if buf else 0)
        if cur + add > limit:
            chunks.append("\n".join(buf))
            buf = [line]
            cur = len(line)
        else:
            buf.append(line)
            cur += add
    if buf:
        chunks.append("\n".join(buf))
    ok = True
    for chunk in chunks:
        ok = send_telegram(chunk) and ok
    return ok
```

### dags/sales/Sales_Employee_Extract_Dags.py (수정 대상 현재 코드, 전체)
```python
import smtplib
from email.mime.text import MIMEText
from email.mime.multipart import MIMEMultipart
...
from modules.transform.utility.notifier import on_failure_callback, on_retry_callback
from modules.transform.utility.mail_recipients import (
    MAIL_CMJ_PM,
    resolve_manager_mail,
    resolve_mail_recipients,
)

def send_email_alert(subject, body, to_email):
    """토더 ID/PW null값 감지 시 메일 알림"""
    try:
        recipients = resolve_mail_recipients(to_email)
        if not recipients:
            print(f"[메일발송 스킵] 수신자 없음 - {subject}")
            return False
        smtp_server = "smtp.gmail.com"
        smtp_port = 587
        sender_email = "airflow.alarm@gmail.com"
        sender_password = "bgtu jxdz grxj xvwu"  # Gmail App Password

        msg = MIMEMultipart()
        msg['From'] = sender_email
        msg['To'] = ", ".join(recipients)
        msg['Subject'] = subject
        msg.attach(MIMEText(body, 'plain', 'utf-8'))

        with smtplib.SMTP(smtp_server, smtp_port) as server:
            server.starttls()
            server.login(sender_email, sender_password)
            server.send_message(msg)

        print(f"[메일발송] {recipients} - {subject}")
        return True
    except Exception as e:
        print(f"[메일발송 실패] {to_email}: {e}")
        return False


def check_toder_null_values(df_original):
    """토더 ID/PW null값 있는 매장 확인 및 메일 알림"""
    if '토더ID' not in df_original.columns or '토더PW' not in df_original.columns:
        return  # ← 진단 로그 없이 조용히 스킵 (Step 3에서 로그 추가)

    null_mask = (df_original['토더ID'].isna() | (df_original['토더ID'].astype(str).str.strip() == '')) | \
                (df_original['토더PW'].isna() | (df_original['토더PW'].astype(str).str.strip() == ''))

    null_stores = df_original[null_mask & df_original['매장명'].notna()][['호점', '매장명', '담당자', '토더ID', '토더PW']].copy()

    if len(null_stores) > 0:
        print(f"\n[토더계정 알림] null값 감지: {len(null_stores)}건")

        html_body = """
        <html>
        <body style="font-family: Arial, sans-serif;">
        <h3 style="color: #d9534f;">⚠ 토더(TORDER) 계정정보 누락 알림</h3>
        <p>다음 매장에서 토더 ID/PW가 등록되지 않았습니다.</p>
        <table border="1" cellpadding="10" style="border-collapse: collapse; margin-top: 20px;">
        <tr style="background-color: #f5f5f5;">
            <th>호점</th>
            <th>매장명</th>
            <th>담당자</th>
            <th>토더ID</th>
            <th>토더PW</th>
        </tr>
        """
        for _, row in null_stores.iterrows():
            toder_id = str(row['토더ID']).strip() if pd.notna(row['토더ID']) else '(없음)'
            toder_pw = str(row['토더PW']).strip() if pd.notna(row['토더PW']) else '(없음)'
            html_body += f"""
        <tr>
            <td>{row['호점']}</td>
            <td>{row['매장명']}</td>
            <td>{row['담당자']}</td>
            <td>{toder_id}</td>
            <td>{toder_pw}</td>
        </tr>
            """
        html_body += """
        </table>
        <p style="margin-top: 20px; color: #666;">구글시트에서 해당 매장의 토더 ID/PW를 확인하고 등록해주세요.</p>
        </body>
        </html>
        """
        subject = f"[도리당 매장관리] 토더(TORDER) 계정정보 누락 - {len(null_stores)}건"
        send_email_alert(subject, html_body, MAIL_CMJ_PM)


# 호출부 (310행, 변경 없음)
def load_employee_from_gsheet(**context):
    ...
    check_toder_null_values(df)
    ...
```

## Test Cases
1. [구문 확인] `python -c "import ast; ast.parse(open(r'dags/sales/Sales_Employee_Extract_Dags.py', encoding='utf-8').read())"` → 기대: 에러 없음
2. [DAG import] `python -c "from dags.sales.Sales_Employee_Extract_Dags import dag"` → 기대: ImportError 없음, `send_telegram`/`send_telegram_chunks` import 정상
3. [잔여 참조 확인] `grep -n "send_email_alert\|MAIL_CMJ_PM\|resolve_mail_recipients\|smtplib\|MIMEText\|MIMEMultipart" dags/sales/Sales_Employee_Extract_Dags.py` → 기대: 결과 없음 (전부 제거됨)
4. [단위 동작] 토더ID/PW가 비어있는 행이 있는 더미 DataFrame으로 `check_toder_null_values(df)` 직접 호출(단, `send_telegram_chunks`를 monkeypatch해 실제 발송 대신 호출 인자만 캡처) → 기대: 캡처된 텍스트에 매장별 `호점 / 매장명 / 담당자: 토더ID=..., 토더PW=...` 라인이 감지된 매장 수만큼 포함됨
5. [컬럼 없음 케이스] `토더ID`/`토더PW` 컬럼이 없는 DataFrame으로 호출 → 기대: 예외 없이 조기 return, `[토더계정 알림] 스킵: 토더ID/토더PW 컬럼 없음` 로그 출력
6. [실제 실행, 가능하면] Airflow에서 `airflow tasks test <dag_id> load_employee_from_gsheet <date>` 실행, 토더 누락 매장이 실제로 있으면 텔레그램 메시지 수신 확인 (`TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` Variable 필요)

## Verification Loop
```
LOOP until all PASS:
  1. Test Cases 1→6 순서대로 실행
  2. FAIL 항목 → 원인 분석
  3. 코드 수정
  4. 전체 Test Cases 재실행
종료 조건: 전체 PASS + Constraints 위반 없음
```

## Constraints
- `check_toder_null_values`의 감지 로직(null_mask, 대상 컬럼)은 바꾸지 않는다 — 알림 채널만 이메일→텔레그램으로 바꾼다.
- 파일 전체의 `print`→`logging` 전환, `호점`/헤더 감지 로직 변경은 이번 스코프 밖(별도 PRD `Sales_Employee_Extract_Dags_silent_failure_fix_260715.md`에서 이미 처리됨).
- 시트의 실제 컬럼명이 `토더ID`/`토더 ID` 중 무엇인지는 라이브 시트 없이 확인 불가 — 이번 변경으로 "컬럼 없음" 스킵이 로그로 드러나므로, 다음 실행 로그에서 스킵 메시지가 뜨면 209행 `column_mapping`에 매핑 추가는 후속 조치로 넘긴다(이번 PRD 범위 아님).
- `send_telegram`/`notifier.py` 자체는 수정하지 않는다 — 이미 검증된 공용 함수.

## Do Not Ask — Decide Yourself
- 파일이 이미 존재하면: 덮어쓰기
- import가 모호하면: Reference Code의 패턴 따라가기
- 타입 힌트 여부: 기존 파일과 동일하게 (타입 힌트 없음)
- 주석 여부: 최소화 (WHY만, WHAT 설명 금지)
- 변수명·함수명 스타일: snake_case, 기존 파일과 동일하게
